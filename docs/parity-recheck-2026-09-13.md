# Cross-client parity recheck — 13 September 2026

Every one of the 134 major and minor items in the parity audit was re-read
against the LIVE code by 14 agents, one batch each. Every CLOSED verdict was
then handed to a separate adversarial agent whose job was to overturn it.

**0 CLOSED verdicts were overturned.** That is the number that says the recheck
is worth trusting — the verifier was told to look for a reason to reopen and
found none in any of the 8.

The audit's own per-item headers are now STALE in places: several items list a
client as missing that has since grown the feature, and at least one (M4) lists
the wrong client as the offender. The `evidence` field below is the current
reading with file and line; the audit text is not.

## The count

| verdict | items | what it means |
|---|---|---|
| OPEN | 98 | not built on at least one client |
| PARTIAL | 28 | built, but the clients disagree on behaviour or threshold |
| CLOSED | 8 | genuinely at parity, and it survived the adversarial pass |

## Which client is behind

| client | items missing it |
|---|---|
| Apple TV | 73 |
| Web | 64 |
| Fire TV | 52 |
| Roku | 48 |

Apple TV is the weakest client and Roku the strongest, but no client is ahead on everything — 48 items are missing on Roku.

## How much work

| size | items |
|---|---|
| trivial | 15 |
| small | 60 |
| medium | 44 |
| large | 7 |
| none | 8 |

126 items are unfinished (OPEN or PARTIAL). Of those, **75 are trivial or small**,
44 are medium and only **7 are large**. This is a long list of short jobs, not a
rewrite — and the 7 large ones are the only items that need planning rather than
an afternoon.

### The 7 large ones, by name

These are the only items that need a plan rather than an afternoon. All 7 are OPEN.

| id | what |
|---|---|
| M16 | Account linking (Trakt, MovieBox) exists only on Roku |
| M22 | The trailer is a small picture-in-picture box on Roku and full-bleed on the other three |
| M40 | Language failover — skipping a release with no English audio — differs on all four |
| M44 | Music and Podcasts exist only on the web |
| M65 | Roku Home carries eleven shelves no other client builds |
| M86 | The four clients search four different sets of back ends, so one query returns four different results |
| M98 | Theme choice exists only on Fire TV |


## OPEN — 98

### M1 — "Hide this shelf" exists on the Roku and the Fire TV and nowhere else

- **Missing on:** Apple TV, Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/HomeFragment.kt:885`

ROKU HAS IT: /Users/markususche/Desktop/roku channels/components/MainScene.brs:8407 `t = m.home.callFunc("HideFocusedRow", invalid)`; components/screens/HomeScreen.brs:358 `function HideFocusedRow() as String`, :371 `SetRowHidden(t, true)`; HomeScreen.xml:58 declares the interface function. FIRETV HAS IT: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/HomeActivity.kt:394 `val hidden = homeFragment()?.hideSelectedRow()`; HomeFragment.kt:885 `fun hideSelectedRow(): String?`, :893 `SourceFilters.setRowHidden(requireContext(), title, true)`, read at :235 `hiddenRowTitles = SourceFilters.hiddenRows(requireContext())` and honoured at :320, :873, :907, :937, :972, :982; SourceFilters.kt:131 `fun hiddenRows(ctx)` / :135 `fun setRowHidden(...)` (persisted). TVOS MISSING: rg for hiddenRow|hideRow|rowHidden|hideShelf|hiddenShelves|notInterested|hiddenBands|hideBand over /Users/markususche/Desktop/blazing-tvos/Sources/ returns zero matches; HomeView.swift's only 'hidden' hits (:485, :550, :1296, :1827, :1833) are about the locker shelf and trailer masks. WEB MISSING: rg for notInterested|hiddenRows|hideRow|rowHidden|removeRow|dismissRow|hiddenSet over /Users/markususche/Desktop/blazing-web/*.js and index.html returns zero matches; the only hide-ish hits are route toggles (app.js:1990 `homeView.hidden = !browseRoute`).

### M2 — "Invite someone to this household" exists only on Roku

- **Missing on:** Fire TV, Apple TV, Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/SettingsScreen.brs:449`

ROKU HAS IT (mint): /Users/markususche/Desktop/roku channels/components/screens/SettingsScreen.brs:378 `showInvitePanel({ loading: true })` / :379 `m.top.action = { name: "invite_household" }`; components/MainScene.brs:1120-1121 `else if a.name = "invite_household"` -> `runPartyTask({ kind: "invite" })`; source/lib/PartyApi.brs `POST /household/invite {deviceId} -> { ok, code, expiresAt, error }`; render contract at SettingsScreen.brs:449-469 onInviteState_ with :459 `m.inviteExpiry.text = "Expires " + FriendlyExpiry(...)`, FriendlyExpiry at :474. FIRETV STILL REDEEM-ONLY: ProfileGateActivity.kt:626 comment '"Type a code" — the six-character invite from the owner's phone'; ProfileClient.kt:735 `POST /devices/join`; rg for households/.*invite|invite/create|createInvite|mintInvite|newInvite|"Invite someone" over the whole firetv source tree returns zero matches. TVOS STILL REDEEM-ONLY: /Users/markususche/Desktop/blazing-tvos/Sources/PairView.swift:82 `Text("Have an invite code?")`, :154 'Enter the 6-character code from your invite'; RootView.swift:1490 '"Type a code" — the 6-character invite entry, `/devices/join`'; no mint call anywhere in Sources/. WEB STILL REDEEM-ONLY: /Users/markususche/Desktop/blazing-web/profile.js:2400 `async function redeemInviteCode()`, :2427 `request('/accounts/invite/check?code=...')`; no POST that creates a code.

### M3 — "Jump to newest" appears on three different thresholds and is missing on tvOS

- **Missing on:** Apple TV, Roku, Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:1862`

TVOS STILL HAS NO JUMP CONTROL: rg for 'Jump to newest'|jumpToNewest|newestSeason|Newest over /Users/markususche/Desktop/blazing-tvos/Sources/*.swift returns zero matches; the seasons strip is DetailSheet.swift:827-838 `if !model.seasons.isEmpty { ... ForEach(model.seasons) { group in SeasonPill(...) } }` — one pill per group and nothing leading it. THRESHOLDS STILL DISAGREE: roku /Users/markususche/Desktop/roku channels/components/screens/DetailsScreen.brs:1870 `n.title = "Jump to newest"` is created unconditionally as the first chip inside buildSeasonChips (no season-count test anywhere between :1860 and :1875); firetv DetailActivity.kt:1862 `if (seasons.size > 3) {` :1866 `text = "Jump to newest"`; web app.js:3178 `if (newest && seasons.length > 1) {` :3181 `jump.textContent = 'Jump to newest'`.

### M4 — "More like this" uses different data: Roku shows recommendations only, Fire TV merges in TMDB 'similar'

- **Missing on:** Fire TV
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/DetailsScreen.brs:1115`

The audit's header ("Has it: Fire TV — Missing: Roku, Web, Apple TV") is now stale: web and tvOS both grew the row and both built it on `recommended` alone exactly as the fix line instructed. But the actual defect — Fire TV merging TMDB `similar` — IS STILL SHIPPING. FIRETV STILL WRONG: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:879-881 `// Recommended and Similar come from two TMDB endpoints ... Merge and de-duplicate them.` / `val moreLikeThis = (rich.recommended + rich.similar)` / `.distinctBy { "${it.type}:${it.id}" }`, rendered at :884 `relatedRows.addView(posterRow("More like this", moreLikeThis))`. ROKU CORRECT: /Users/markususche/Desktop/roku channels/components/screens/DetailsScreen.brs:1098-1113 'RECOMMENDED, and ONLY recommended', :1115 `kept = CapTo12(RichArr5(r.recommended))`. WEB NOW CORRECT: /Users/markususche/Desktop/blazing-web/app.js:3615 `function renderRelated(rich)`, :3616 `const items = ((rich && rich.recommended) || [])`, :3627 `openRichSection(detailRelated, 'More like this')` — reached from :3420 `renderRelated(rich);`. TVOS NOW CORRECT: /Users/markususche/Desktop/blazing-tvos/Sources/RichMeta.swift:208-210 `var relatedCandidates: [Related] { recommended.filter { ... } }` (recommended only, no `similar`), consumed at DetailSheet.swift:369 `let candidates = rich?.relatedCandidates ?? []` and rendered at DetailSheet.swift:911-916 `if !model.related.isEmpty { ... sectionTitle("MORE LIKE THIS")`.

### M6 — A finished title is marked Watched on Fire TV only, and the three clients that clear Continue Watching disagree on when

- **Missing on:** Roku, Apple TV, Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/Progress.kt:45`

FIRETV STILL THE ONLY ONE: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/Progress.kt:45 `const val WATCHED_AT_PERCENT = 90` (with the 90-not-80 rationale at :39-44). ROKU STILL CLEARS ONLY: components/MainScene.brs:1023-1027 `else if m.lastTitle <> invalid and s.progress >= 95` / `print "[cw] finished "; m.lastTitle.name; " — clearing"` / `RemoveContinue(m.lastTitle.id)` — no FleetAdd 'watched' on that branch; components/MainScene.brs:7345-7377 onPlaybackFinished fires telemetry, tears down the watch party and calls playNextEpisode/goBack, and never writes Watched. Roku's 95 vs Fire TV's 90 also still unreconciled. TVOS STILL HAS NO THRESHOLD BRANCH: /Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:871-874 the comment still claims 'Past 97% the thing is finished — it comes OUT of the list', but :875-889 `func recordProgress(...)` only guards `position >= 10`, computes `percent`, fills a ContinueEntry and calls `ProgressSync.save(...)` unconditionally; Progress.swift:66-110 ProgressSync.save has no removal branch and ProgressStore.swift:154-170 upsert/upserted always re-inserts the entry. WEB STILL DOES NEITHER: app.js:5658-5667 the 'ended' handler emits `reason: 'finished'` telemetry then calls autoNextEpisode() — nothing clears Continue Watching or POSTs the Watched list; app.js:5385-5396 `function startSync(meta)` POSTs `{imdbId, position, duration, profileId}` on a 30s interval with no finish branch; the only Watched write is the manual button at app.js:35 `{ list: 'watched', button: '#detail-watched', ... out: 'Mark watched' }`.

### M8 — A new PIN is typed twice only on Fire TV

- **Missing on:** Roku, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/PinActivity.kt:199`

FIRETV STILL THE ONLY ONE: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/PinActivity.kt:199 `private fun submitSet() {`, :206 `titleView.text = "Confirm your new PIN"`, :219 `statusText.text = "Those didn't match — try again"`, dispatched from :150 `if (entered.length == PIN_LEN) { if (setMode) submitSet() else submit() }`. ROKU STILL SINGLE ENTRY: /Users/markususche/Desktop/roku channels/components/MainScene.brs:5585-5595 `sub openProfilePinDialog()` creates ONE KeyboardDialog (`d.message = "Enter a 4-digit PIN (numbers only)."`, `d.buttons = ["Save", "Cancel"]`); :5599-5615 `sub onProfilePinSelected()` validates with :5608 `if not IsFourDigitPin(pin)` and then goes straight to `submitProfilePatch({ pin: pin })` at :5614 — no second dialog chained through wasClosed. IsFourDigitPin at :5619-5623 is format-only. TVOS STILL SINGLE FIELD: /Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:2410 `SecureField("New 4-digit PIN", text: $newPIN)` is the only PIN input in FleetProfileEditView (the confirm-free :2422-2426 onChange just trims to 4 digits), and :2480-2483 `private var canSave: Bool { !name.trimming...isEmpty && (newPIN.isEmpty || newPIN.count == 4) }` tests length only; the create form at :2242 `SecureField("4-digit PIN", text: $pin)` is likewise single-entry (canSave test at :2269).

### M9 — A persistent profile badge sits over Home on the Fire TV and the Apple TV only

- **Missing on:** Roku
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:1253`

FIRETV HAS IT: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/HomeActivity.kt:49 `installProfileBadge()` called from onCreate, :93-100 `private fun installProfileBadge() { val profile = ProfileClient.activeProfile ?: return; val host = findViewById<FrameLayout>(R.id.profile_host); val size = UiKit.dp(host, 48); val badge = ImageView(this).apply { isFocusable = true ... }`. TVOS HAS IT: /Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:1253-1259 `if let profile = profileSession.activeProfile { ActiveProfileBadge(profile: profile) { profileSession.chooseAnotherProfile() } ... .frame(maxWidth: .infinity, alignment: .topLeading) }`, the view itself at :1439 `private struct ActiveProfileBadge: View`. ROKU STILL MISSING: /Users/markususche/Desktop/roku channels/components/MainScene.xml:167-173 declares only `<Group id="navBar">` containing `navPanel` + `navItems`, then :192 `<Group id="drawer" visible="false">` and the toast rectangles — no avatar or profile node; rg for profileBadge|activeProfileBadge|avatarNode|profilePill over roku channels/components/ returns zero matches; the only way in stays a menu row, components/MainScene.brs:1118-1119 `else if a.name = "switch_profile"` -> `openProfileGate(true)`. WEB: persistent one-click text control, profile.js:765 `ui.connect.textContent = profile ? \`${profile.name} ▾\` : 'Sign in';` injected at :3356 `topbar.insertBefore(ui.connect, searchButton);` — the fix line says leave it alone.

### M10 — A profile cannot be renamed from the browser

- **Missing on:** Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:2379`

WEB STILL HAS NO RENAME SURFACE: /Users/markususche/Desktop/blazing-web/profile.js grew a full parental sheet (showParental at :1145, renderParental at :1183) but its rows are rating cap, kids, adult, :1246 `row('Profile picture', () => showIconPicker(profile), { id: 'bp-parental-icon' })`, :1252 Set/Change PIN, :1260 Remove PIN, :1268 Delete, :1290 Done — no Name row. The file says so itself at :1136-1138: 'Roku's openProfileEditMenu (MainScene.brs:4802-4838) is the same list again with Rename and Profile Color, which the browser does not have surfaces for'. The wire half is already there and unused: :1310 `if (typeof change.name === 'string') body.name = change.name;` inside applyParental, and rg shows no caller anywhere passing a name. ROKU HAS IT: components/MainScene.brs:5183 `buttons = ["Rename", "Icon", "Profile Color", ...]`, :5235 `openProfileRenameDialog()`, :5297-5311 `sub openProfileRenameDialog()` / `d.title = "Rename Profile"` / `d.ObserveField("buttonSelected", "onProfileRenameSelected")`. FIRETV HAS IT: ProfileGateActivity.kt:1654 `val nameBtn = editHubButton("Name") { showEditNameForm(p, unlockToken) }`, :1759 `private fun showEditNameForm(...)`. TVOS HAS IT: /Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:2379 `TextField("Profile name", text: $name)` in FleetProfileEditView, guarded by canSave at :2480-2483 and saved through that form.

### M11 — A real no-internet screen exists only on the Fire TV

- **Missing on:** Roku, Apple TV, Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/OfflineActivity.kt:36`

firetv HAS IT (live): client/app/src/main/java/com/nyctailblazers/blazingtv/OfflineActivity.kt:36 `class OfflineActivity : AppCompatActivity()`, text "NO INTERNET" / "Blazing can't reach the network"; reached from HomeFragment.kt:230 `activity?.let(OfflineActivity::showIfOffline)`; registered AndroidManifest.xml:87. ROKU STILL MISSING: components/screens/HomeScreen.brs:638 `m.status.text = "No catalogs loaded - check the network."` is still the only state; `ls components/screens/ | rg -i 'offline|network'` returns nothing; no GetLinkStatus call exists anywhere (source/lib/DeviceCaps.brs:52 only comments that roDeviceInfo has no GetLinkQuality). TVOS STILL MISSING: Sources/HomeView.swift:144 `status = "Could not reach the library."`; repo-wide `rg NWPathMonitor Sources/` returns zero hits; no offline view file in Sources/. WEB STILL MISSING: app.js:2923 `message.textContent = 'Nothing is available right now. Try again soon.';`; `rg "navigator.onLine" app.js index.html profile.js settings.js` returns nothing, and `rg -i offline index.html` returns nothing (sw.js:254 only mentions the word in a cache comment).

### M12 — A second query fired while the first is still running is ignored on tvOS

- **Missing on:** Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:190`

TVOS STILL MISSING: Sources/Sections.swift:449 `guard !q.isEmpty, !running else { return }` in run(), and Sections.swift:485 the identical guard in runAI(); both followed by `running = true` / `defer { running = false }` (:450-451, :486-487). No Task handle, no cancel, no request counter anywhere in the file. Line numbers shifted from the audit's 434/459 but the code is unchanged. FIRETV HAS IT: SearchActivity.kt:190 `searchJob?.cancel()` at the top of runSearch, with `searchJob = lifecycleScope.launch {` at :199. WEB HAS IT: app.js:1674 `const request = ++searchRequest;` and app.js:1692 `if (request !== searchRequest) return;`. ROKU: the audit's secondary note is still accurate — components/MainScene.brs:1760 `m.search.searchResults = { ok: true, results: merged, videos: tube.Count() }` publishes no `query` key, so the guard at components/screens/SearchScreen.brs:138 `if r.query <> invalid and ThemeNz(r.query) <> "" ...` is still inert (its own comment at :133-137 says so).

### M14 — AI Search does not exist in the web client

- **Missing on:** Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:205`

WEB STILL MISSING: `rg -rn 'ai-search|aiSearch|ai_search' --glob '*.js' --glob '*.html'` over blazing-web returns ZERO hits, as does `rg -i 'gemini|describe a plot|ai search'` over app.js/index.html/profile.js/settings.js/sources.js/livetv.js/caps.js. The search form is unchanged: index.html:388 `<form id="search-form" class="search-form" role="search">`, :389 mic button, :393 the one `<input id="search-input">`, :394 `<button class="primary-button" type="submit">Search</button>` — no AI control. The fan-out is still Emby + fleet movie + fleet series + searchSiteCatalogs at app.js:1685-1691, with fleetSearch defined at app.js:1682-1683. DONORS ALIVE: firetv SearchActivity.kt:205 `if (aiSearchEnabled && query.length >= AI_MIN_QUERY)` → POST `"$aiBase/api/ai-search"` at :224; tvos Sources/FleetAPI.swift:301 `static func aiSearch(query:)` → :302 `BlazingAPI.base + "/api/ai-search"`.

### M15 — AI Search returns one result on Fire TV and a whole grid on tvOS

- **Missing on:** Fire TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/FleetAPI.swift:325`

FIRETV STILL MISSING: SearchActivity.kt:232 `listOf(` wrapping a single element — :233-241 `hydrated ?: AddonClient.Meta(id = id, type = type, name = hit.optString("title")…)` — so the AI branch can only ever produce one card. No AddonClient.searchEnriched call in that branch (searchEnriched is only reached in the ordinary path at :267/:270). The RatingClient.visibleTo call is still there at :249-251 over that one-element list. TVOS HAS IT: Sources/FleetAPI.swift:325 `let results = (try? await BlazingAPI.search(type: type, query: term)) ?? []` then :330 `guard let exact = results.first(where: { $0.id == imdbId }) else { return results }` and :331 `return [exact] + results.filter { $0.id != imdbId }`.

### M16 — Account linking (Trakt, MovieBox) exists only on Roku

- **Missing on:** Fire TV, Apple TV, Web
- **Fix size:** large
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/SettingsScreen.brs:304`

ROKU STILL THE ONLY DONOR: components/screens/SettingsScreen.brs:49 `a.title = "Unlink Trakt"`, :51 `a.title = "Link Trakt account"`, :56 `d.title = "Link MovieBox account"`; dispatch at :361 `m.top.action = { name: "trakt_unlink" }`, :366 `{ name: "trakt_link" }`, :374 `{ name: "mbp_link" }`; device-code overlay sub showCodePanel() at :304-311 and onTraktState() at :312-341 (renders "Go to " + s.url + " and enter:" and "Waiting for you to approve it…"); ACCOUNTS panel state at :250-260 ("MovieBox resolves trailers. Trakt syncs progress."). FIRETV STILL MISSING: `rg -rn 'trakt|Trakt' client/app/src/main/java/` returns ONE hit and it is unrelated — KodiClient.kt, the word inside a menu-word regex `"search|downloads?|favorites?…"`. No mbp_link/link action anywhere; only 'mbp:' id handling in CardMeta.kt and DwellPreview.kt. TVOS STILL MISSING: `rg -rn 'trakt|Trakt' Sources/` returns zero hits; only 'MovieBox' id prose at Sources/CardMeta.swift. WEB STILL MISSING: `rg -n 'trakt|Trakt|MovieBox|mbp_link' app.js profile.js settings.js sources.js index.html` returns zero hits.

### M18 — Adult catalogues can be queried from Search on Fire TV only

- **Missing on:** Roku, Web, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/AddonClient.kt:439`

FIRETV STILL THE ONLY ONE: client/app/src/main/java/com/nyctailblazers/blazingtv/AddonClient.kt:439 `val adult = if (ProfileClient.canShowAdult()) "&adult=1" else ""` and :440 `val txt = if (fleet.isBlank()) null else Net.getText("$fleet/search/$type?q=$q$adult")`, with the three-condition rule spelled out at :433-438. ROKU STILL MISSING: source/lib/AddonClient.brs:200 `function SearchCatalog(mediaType as String, query as String) as Object` / :201 `url = CinemetaBase() + "/catalog/" + mediaType + "/top/search=" + UrlEscape(query) + ".json"` — still Cinemeta direct, never the fleet /search route; no `adult` token anywhere in that file. TVOS STILL MISSING: Sources/BlazingAPI.swift:148 `static func search(type:query:)` → :150-151 `URL(string: "\(metaBase)/catalog/\(type)/top/search=\(q).json")` where metaBase is :11 `https://v3-cinemeta.strem.io`; the only `adult` hits in that file are a title-text blocklist regex at :85-90 and :106-110, not a request flag. WEB STILL MISSING: app.js:1683 `fetchJSON(`${FLEET_BASE}/search/${type}?q=${encodeURIComponent(query)}&limit=20`)` — no adult parameter; the comment at app.js:1698-1700 still says the fleet routes default them off.

### M19 — An outage and an empty result set read as the same message on Fire TV and tvOS

- **Missing on:** Fire TV, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-web/app.js:1758`

WEB HAS IT (reference, unchanged): app.js:1758-1759 `const reachable = movies.status === 'fulfilled' || series.status === 'fulfilled' || embyRows.length > 0 || siteRows.length > 0;` then :1760-1762 `status.textContent = reachable ? `Nothing found for “${query}”.` : 'Could not reach search. Try again in a moment.';`, with the three-states comment at :1749-1757. ROKU HAS IT: components/screens/SearchScreen.brs:128 `m.status.text = "Search failed."` on an invalid payload, separate from :182 `m.status.text = "Nothing found."`. FIRETV STILL MISSING: SearchActivity.kt:267-278 launches the three legs as `async(Dispatchers.IO) { AddonClient.searchEnriched(...) }` / `mrWorldPremiereSearch(...)` with no failure signal, and the only terminal branch is :311-313 `if (!added) { results.clear(); addMessageRow("Search", "No matches", "Try a shorter title or a different spelling.") }` — any empty outcome, outage included. The browse path still gets the better treatment at :259 `addMessageRow("Browse", "Categories could not load", "Check the network and try again.")`. TVOS STILL MISSING: Sources/Sections.swift:463-465 `let all = ((try? await movies) ?? []) + ((try? await series) ?? []) + ((try? await mrWorldPremiere) ?? [])` swallows every throw, so :479 `status = found.isEmpty ? "Nothing matched \"\(q)\"." : …` always renders "Nothing matched".

### M20 — An owner PIN pad (seven digits) exists only in the browser

- **Missing on:** Roku, Fire TV, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-web/profile.js:2602`

WEB STILL THE ONLY ONE: profile.js:27 `const OWNER_PIN_LENGTH = 7;`, :1596 `function openOwnerPin(from = 'pending')`, :1629 `async function openOwnerPinFromGate()`, :1672 `const result = await request('/devices/self-approve', {`, :1536 `ui.pinName.textContent = state.ownerMode ? 'Owner PIN'`, and the variable-length pattern at :2602 `const pinLength = () => (state.ownerMode ? OWNER_PIN_LENGTH : 4);` used at :2605, :726 and :3783. ROKU STILL FIXED AT 4: components/screens/PinPad.brs:11-13 `function PadLength() as Integer / return 4 / end function`, and the file header line 1 still reads "PinPad — four digits"; `rg -rn 'self-approve|Owner PIN' components/ source/` returns zero hits. FIRETV STILL FIXED AT 4: PinActivity.kt:250 `private const val PIN_LEN = 4`, used at :147, :150, :158; no self-approve path. Its comment survives at ProfileGateActivity.kt:326-327 ("The remote cannot fix an account state, so there is nothing else to offer; the owner acts on the web or a phone") — so under the audit's 'web-only by design' resolution Fire TV alone is done, and that sentence has NOT been repeated in Roku or tvOS. TVOS STILL FIXED AT 4: Sources/RootView.swift:2692 `ForEach(0..<4, id: \.self) { index in`; `rg 'self-approve|selfApprove|Owner PIN' Sources/` returns zero hits.

### M21 — An uncached debrid pick is retried on the Roku and treated as a failure everywhere else

- **Missing on:** Fire TV, Web, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/PlayerScreen.brs:995`

ROKU HAS IT: components/screens/PlayerScreen.brs:995 `function WaitingForCache() as Boolean`, :997 `if m.current.uncached <> true then return false`, :998 `if m.cacheWaits >= 20 then return false`, :1001 shows "Not cached yet - the debrid service is fetching it. / Retry N of 20, checking every 15s. Back to cancel.", and it is reached from :284 (`if m.current <> invalid and m.current.uncached = true then return` in the stall watchdog), :382 and :485 (`if WaitingForCache() then return` ahead of every dead verdict). FIRE TV STILL MISSING: PlayerActivity.kt:50-57 `private data class Candidate(url, mimeType, headers, drmScheme, licenseUrl, licenseHeaders)` — no cached/downloadRequired field, so the flag never reaches the player; the only special case in onPlayerError is PlayerActivity.kt:503 `val rateLimited = http?.responseCode == 429` and :509 `if (!tryNextCandidate(reason)) giveUp()`. The flag exists upstream only as AddonClient.kt:75 `val downloadRequired: Boolean = false` consumed at SourceFilters.kt:357 `if (f.noUncached && stream.downloadRequired) return "uncached"` — a skip, not a wait. WEB STILL MISSING: no "uncached"/"not cached" token anywhere in app.js; every player failure ends at app.js:4560/:4573/:4604 `giveUpOnCandidates(session, playFailureReason('This stream ... Try another source.'))`; app.js:4182 `const retryable = !err.status || err.status === 408 || err.status === 429 || err.status >= 500` is the SOURCE-LIST fetch, not playback. TVOS STILL MISSING: Sources/StreamRanker.swift:82 `case "uncached": return "NOT INSTANT"` is a label only; Sources/SourcePrefs.swift:172 `if prefs.noUncached, tag == .pack { return "uncached" }` is a skip; grep of Sources/PlayerView.swift for uncached/cached returns no branch — its only verdicts are .dead and the stall monitor (PlayerView.swift:1069 `if state == .error || (ended && !monitor.hasPlayed)`).

### M23 — Apple TV offers 4 profile pictures where the other three offer 20

- **Missing on:** Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/ProfileGateActivity.kt:117`

STILL OPEN, unchanged, both arrays. /Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:2188 `private let avatars = ["🦊", "🤖", "🦁", "🚀"]` (create form) used at :2233-2235 `HStack(spacing: 16) { ForEach(avatars, id: \.self) { glyph in FleetAvatarChoice(...) } }`; RootView.swift:2344 the SAME four-entry array declared again (edit form) used at :2396-2398 in a second HStack. Line numbers moved from the audit's 1766/1914 but the code is identical. No 20-entry table exists anywhere in the tvOS repo: grep for 🦄/🐯/🐼 across Sources/ and Shared/ returns nothing. The correction the audit noted is still true — RootView.swift:2085 `private var customGlyph: String?` and :2097-2098 render any short non-ASCII avatar, so a Fire TV pick still DISPLAYS, it just cannot be CHOSEN here. THE OTHER THREE ALL HAVE 20 IN THE SAME ORDER: firetv ProfileGateActivity.kt:117-120 `private val ICON_CHOICES = listOf("🦄","🐯","🐸","🦊","🐼","🐵","🐙","🦁","🐳","🐨","🐰","🦖","🐧","🐷","🐻","🎮","⚽","🚀","🎨","👑")`; web profile.js:49-52 the identical frozen array; roku components/screens/ProfileGate.brs:589-610 `function GateIconTable()` with the same 20 in the same order and :615-617 `function GateIconCols() as Integer / return 5`.

### M24 — Books, Music and Podcasts are searched only in the browser, and only for a mature profile

- **Missing on:** Roku, Fire TV, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-web/app.js:1374`

WEB STILL THE ONLY ONE: app.js:1374 `async function loadMediaSearch(query, request)`, :1380 `host.hidden = !ratingAllowed('mature') || !profileId;`, :1383 `await Promise.all(['books', 'music', 'podcasts'].map(async (kind) => {` … :1389 `window.BlazingProfile.mediaRequest(\`/media/${kind}?q=...&limit=8\`)`; called from runSearch at app.js:1675 `loadMediaSearch(query, request);`. ROKU STILL VIDEO-ONLY: components/tasks/AddonTask.brs:636 `function doSearch(req as Object) as Object` fans out over SearchCatalog("movie"), SearchCatalog("series"), TmdbSearch, MbpSearch, AnimeSearch, MwpSearch (:640-668) and returns `{ kind: "search", results: results, query: q }` at :679 — no /media/books, /media/music or /media/podcasts call exists anywhere under components/ or source/ (grep returns nothing). FIRE TV STILL VIDEO-ONLY: SearchActivity.kt:267-278 `val movies = async(...) { AddonClient.searchEnriched("movie", query) }` / series / :274 mrWorldPremiereSearch, and the only rows added are "Movies", "Series" (:292) and "MrWorldPremiere" (:308) — even though Fire TV already has the client half at BooksClient.kt:156 `val url = "${fleetBase()}/media/books?q=..."`. TVOS STILL VIDEO-ONLY: Sources/Sections.swift:455-457 `async let movies = BlazingAPI.search(type: "movie", query: q)` / series / mrWorldPremiereSearch and :459-462 concatenates only those three — books live apart in the reading hub at Sources/ReadingView.swift:783 `enum BooksAPI` and :1017 `let answer = await BooksAPI.search(q)`, unreachable from the main search.

### M27 — Every tile on the tvOS Games grid has an empty action - nothing can be opened

- **Missing on:** Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/GamesScreen.brs:356`

TVOS UNCHANGED — the empty closure is still there. Sources/Sections.swift:516-522: `LazyVGrid(columns: columns, alignment: .leading, spacing: 26) { ForEach(items) { g in` / `// Nothing to open: this is a catalogue, not a console.` / `PosterTile(art: g.poster, caption: g.name) {} } }`. The tile IS still focusable — Sources/Chrome.swift:76 `let action: () -> Void` and :94-99 `Button(action: action) { PosterFace(...) } .buttonStyle(.plain) .focused($focused)` — so the grid still offers a press it will not answer. The only change since the audit is a header hint, Sections.swift:511 `hint: "Browse only — a Roku and an Apple TV cannot run these"`, which is text, not a fix: the tiles still take focus. No GameDetail view exists (no file matching *Game* under Sources/, and grep for "GameDetail" across Sources/ returns nothing). FIRE TV'S HALF OF THIS ITEM IS NOW CLOSED — it has a RAWG catalogue AND a detail: GamesCatalogClient.kt:24-26 `GET /games/catalog?page=...` and `GET /games/catalog/<id>`, :104 `val (status, body) = get("${base()}/games/catalog/${enc(gameId)}")`, opened from RetroGamesActivity.kt:623 `private fun openCatalogDetail(game: GamesCatalogClient.Game)` and :634 `val detail = withContext(Dispatchers.IO) { GamesCatalogClient.detail(game.id) }`, with the grid at :588 `private fun posterCard(game: GamesCatalogClient.Game)`. ROKU AND WEB REFERENCES STILL STAND: components/screens/GamesScreen.brs:356-365 `sub onChosen()` … `m.top.chosen = { action: "open", id: ..., name: ..., poster: ... }` into components/screens/GameDetailScreen.brs:1-3 (poster, meta, description, screenshots, optional trailer); web games.js:120 `button.addEventListener('click', () => openDetail(id));`.

### M28 — Favourites / starring a channel exists only on Roku and Apple TV

- **Missing on:** Fire TV, Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/LiveView.swift:17`

STILL OPEN ON BOTH. FIRE TV: grep -i for favourite|favorite across every .kt in app/src/main/java/com/nyctailblazers/blazingtv/ matches ONE file, KodiClient.kt, and nothing in the Live TV path. LiveTvActivity.kt:88-95 is still the four-case click listener with no star — `setOnItemViewClickedListener { _, item, _, _ -> when (item) { is ChannelItem -> playChannel(item.channel); GuideEntry -> startActivity(Intent(requireContext(), GuideActivity::class.java)); RetryEntry -> loadRows(); else -> { /* loading card */ } } }` — no setOnKeyListener, no favourites ListRow; rows are built only from the fleet's groups at :145-169. WEB: grep -ci for favourite|favorite in livetv.js returns 0 — channelCard() has no star control and renderGroups() has no Favourites chip. THE TWO MODELS ARE BOTH LIVE. tvOS: Sources/LiveView.swift:17 `final class LiveFavourites: ObservableObject` (keys on channel id, stores the whole record) wired in at :337 `@StateObject private var favourites = LiveFavourites()`. Roku: source/lib/LiveTV.brs:320-321 `function ... return "★ Favourites"`, :477 `favs: GetFavourites()`, :699 favourites-first ordering; components/screens/LiveTvScreen.brs:379 `m.hint.text = "OK watches   •   * stars a favourite   •   LEFT for the groups"`, :504 `fav: IsFavourite(c.name)`, :986-994 `function toggleFavouriteHere()` / `on = ToggleFavourite(name)`.

### M29 — Fire TV Home carries four destination-tile shelves no other client has

- **Missing on:** Fire TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/HomeScreen.brs`

ALL FOUR SHELVES ARE STILL THERE — this is an item where FIRE TV is the client that must change (remove them), and it has not. HomeFragment.kt:540-545 `addRow("Reading", listOf(AddonClient.Meta(MANGA_ID, "manga", "Browse Manga", null, ...), AddonClient.Meta(COMICS_ID, "manga", "Browse Comics", null, ...)))` — the audit's "Manga" row, renamed "Reading" and now TWO tiles instead of one; :550-552 `val liveAdapter = addRow("Live TV", listOf(AddonClient.Meta(LIVE_TV_ID, "livetv", "Live TV & Guide", null)))`; :564 `addRow("Games", gameTiles + retroTile)`; :572-590 `addRow("Settings", buildList { add(AddonClient.Meta(SETTINGS_ID, "settings", "Settings & Updates · ${BuildConfig.CHANNEL.uppercase()}", null)) ... })` — which has GROWN since the audit, now also carrying "Manage Kodi add-ons" and "Library" tiles. Its own comment at :567-570 concedes the point: "THE SETTINGS ROW IS A THIRD DOOR". THE OTHER THREE STILL HAVE NOTHING LIKE IT: grep of roku components/screens/HomeScreen.brs for "Browse Manga"/"Games"/"Settings"/"Live TV" returns nothing; grep of /Users/markususche/Desktop/blazing-tvos/Sources/HomeView.swift for the same strings returns nothing; web builds only trending/fresh/manifest rows via app.js:2877 `async function bootFromShelves(request = homeRequest)`, called at :2757 and :2861.

### M30 — Fire TV Live TV has no channel search

- **Missing on:** Fire TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/LiveView.swift:374`

STILL OPEN, unchanged. FIRE TV: LiveClient.kt:85-92 still declares the parameter — `fun channels(ctx: Context, group: String? = null, country: String? = null, q: String? = null, skip: Int = 0, limit: Int = 100, ...)` — and builds it at :107 `q?.takeIf { it.isNotBlank() }?.let { append("&q=").append(enc(it)) }`, but LiveTvActivity.kt never passes it: :155-157 `LiveClient.channels(liveContext, group = g.id, country = LIVE_COUNTRY, limit = 50)` and :160 `LiveClient.channels(liveContext, group = g.id, limit = 50)` are the only two calls, and the fragment's click listener at :88-95 has four cases (ChannelItem, GuideEntry, RetryEntry, loading) with no search entry point and no text field anywhere in the file. The only q= caller in the app is still GuideActivity.kt:287 with a fixed term. THE OTHER THREE ALL HAVE IT. Roku: components/screens/LiveTvScreen.brs:1 header "the channel list, the guide, and channel search", :70-78 the search panel nodes (`m.searchPanel = m.top.FindNode("searchPanel")`, `m.searchBtn.ObserveField("buttonSelected", "onSearchPressed")`, `m.searchOpen = false`), :85 the 900ms debounce. Web: livetv.js:17 documents `GET /live/channels?...[&q=]`, :50-52 "Search asks the SERVER (q=) … q=cnn -> 38, q=bbc -> 248", :295-296 `form: document.getElementById('livetv-search-form'), input: document.getElementById('livetv-search-input')`. tvOS (the shape to copy — submit on OK, no debounce): Sources/LiveView.swift:80 `@Published var query = ""`, :243 `FleetAPI.liveChannels(group: wanted, query: q, skip: 0)`, :374 `TextField(searchPrompt, text: $draft)`, :383 `.onSubmit { model.query = draft }`.

### M31 — Fire TV and web have no Adult destination at all; the Roku and Apple TV bars carry an Adult chip

- **Missing on:** Fire TV, Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/MainScene.brs:7818 (bar chip) + components/screens/AdultScreen.xml; /Users/markususche/Desktop/blazing-tvos/Sources/NavBar.swift:227 (gate) + Sources/RootView.swift:1209 (route)`

firetv MISSING: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/NavBarView.kt:60 `enum class Dest(val label: String) {` — the enum runs HOME/MOVIES/SHOWS/ROADMAPS/LIVE/READING/LIBRARY/GAMES/SEARCH/SETTINGS with no ADULT; NavBarView.kt:43 `ADULT is not here either, and that one is a GAP, not a design choice`; NavBarView.kt:130 `- ADULT, because this client cannot honour it. There is no Dest.ADULT, no`; entries() at NavBarView.kt:148-171 ends `dest(Dest.SEARCH); dest(Dest.SETTINGS)` with no Adult chip. Repo-wide grep for `Dest.ADULT` or `AdultActivity` across client/app/src/main/java/com/nyctailblazers/blazingtv/ returns only that one comment line — no adult screen exists. web MISSING (documented hold): /Users/markususche/Desktop/blazing-web/index.html:145-149 `Adult is the one still missing, and it stays missing until it has a gate: profile.js computes the effectiveAllowAdult signal (line 183) but nothing renders an adult section yet`; chips end at index.html:150-153 (livetv, youtube, games, search). ROKU HAS IT: /Users/markususche/Desktop/roku channels/components/MainScene.brs:7818 `m.navOrder.Push("adult") : labels.Push("Adult") : icons.Push("lock")`; drawer row MainScene.brs:1875; route MainScene.brs:1628-1638; components/screens/AdultScreen.xml exists. TVOS HAS IT: /Users/markususche/Desktop/blazing-tvos/Sources/NavBar.swift:28 `case movies, shows, anime, roadmaps, library, live, youtube, games, adult, search, settings`; NavBar.swift:131 barOrder includes .adult; NavBar.swift:227 `Section.barOrder.filter { $0 != .adult || profileSession.maySeeAdult }`; Sources/RootView.swift:1209 `case .adult:    AdultView(shell: shell)`.

### M32 — Fire TV guards against deleting the last profile in the house; Roku deliberately does not

- **Missing on:** Roku
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/ProfileGateRules.kt:28 and ParentalSettingsActivity.kt:213; tvOS port already done at /Users/markususche/Desktop/blazing-tvos/Sources/ParentalControls.swift:28,:359,:640`

ROKU STILL MISSING: /Users/markususche/Desktop/roku channels/components/MainScene.brs:5654-5655 `' No "last remaining profile" guard here on purpose — see the header on / ' ProfileDelete in ProfilesApi.brs.`; the button is pushed unconditionally at MainScene.brs:5208 `buttons.Push("Delete Profile")` inside openProfileEditMenu, and openProfileDeleteConfirm at :5657-5663 always offers `d.buttons = ["Delete", "Cancel"]`. No `profileCount`/`canDelete` symbol exists anywhere in MainScene.brs. AUDIT IS STALE ON THE OTHER TWO — tvOS and web have BOTH gained delete AND the guard since 11 Sep. firetv HAS IT: client/app/src/main/java/com/nyctailblazers/blazingtv/ProfileGateRules.kt:28 `fun canDelete(profileCount: Int): Boolean = profileCount > 1`, gated at ParentalSettingsActivity.kt:213 `if (ProfileGateRules.canDelete(profileCount)) {`. tvos HAS IT: /Users/markususche/Desktop/blazing-tvos/Sources/ParentalControls.swift:28 `static func canDelete(profileCount: Int) -> Bool { profileCount > 1 }`, used at ParentalControls.swift:359, guarded again in the action at ParentalControls.swift:640 `guard ParentalRules.canDelete(profileCount: profileCount) else { return }`, and in the second form at Sources/RootView.swift:2454 `if ParentalRules.canDelete(profileCount: profileCount) {` with the comment at :2451 `// NEVER DRAWN ON THE LAST PROFILE IN THE HOUSE.`. web HAS IT: /Users/markususche/Desktop/blazing-web/profile.js:144 `const canDelete = (profileCount) => profileCount > 1;` applied at profile.js:1269 `if (canDelete(state.profiles.length)) {`.

### M33 — Fire TV has no Play control at all until the sources resolve

- **Missing on:** Fire TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/DetailsScreen.brs:1367-1375 (PlayLabel) and :1482 (always-first play action); web equivalent /Users/markususche/Desktop/blazing-web/app.js:3290-3291 + :3345`

firetv STILL MISSING: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:1342 `primaryAction.removeAllViews()` inside ensurePlaybackGate (function starts :1323), and again at :1410 in blockForRating; the slot stays empty until DetailActivity.kt:1553 `if (best != null) {` where the Button is constructed :1555-1566, added at :1567 `primaryAction.addView(b)` and grabs focus at :1568 `b.requestFocus()`. grep for `Finding best stream` across the firetv package returns nothing, so no placeholder label was added; the only `isEnabled = false` hits in DetailActivity.kt are :792 (list buttons) and :1348/:2080 (backToEpisodes), none a Play placeholder. ROKU HAS IT: /Users/markususche/Desktop/roku channels/components/screens/DetailsScreen.brs:1482 `actions = [{ kind: "play", label: PlayLabel() }]` — always first; PlayLabel() at :1367-1375 returns `"Finding best stream…"` as its default. WEB HAS IT: /Users/markususche/Desktop/blazing-web/app.js:3290-3291 `detailPlay.hidden = false; detailPlay.disabled = true;` synchronous in openDetail, enabled at app.js:3345 `detailPlay.disabled = false;`. TVOS HAS IT: /Users/markususche/Desktop/blazing-tvos/Sources/DetailSheet.swift:754 `SheetButton(title: "Play", icon: "play.fill", primary: true) {` — unconditional inside the action HStack.

### M35 — Fire TV silently restricts Live TV to US channels; no other client does

- **Missing on:** Roku, Web, Apple TV
- **Fix size:** medium
- **Copy from:** `The slot exists on all three: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/LiveClient.kt:88,:106 is the donor shape. Needs a Markus decision first (move to Settings on Fire TV vs drop it) — do not just delete the constant.`

UNCHANGED ON ALL FOUR. firetv: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/LiveTvActivity.kt:26 `private const val LIVE_COUNTRY = "US"` with the header at :22-25 still saying `Hard-coded for now … the moment that stops being true this belongs in Settings`; used at LiveTvActivity.kt:156 `LiveClient.channels(liveContext, group = g.id, country = LIVE_COUNTRY, limit = 50,)` with the unfiltered fallback at :159-161. The parameter exists at LiveClient.kt:88 `country: String? = null` and is appended at LiveClient.kt:106. No Settings row for it anywhere in the package. THE OTHER THREE STILL PASS NO COUNTRY: roku — grep for `country` across /Users/markususche/Desktop/roku channels/source/lib/LiveTV.brs and components/screens/LiveTvScreen.brs returns zero hits; web — /Users/markususche/Desktop/blazing-web/livetv.js mentions country only as a response field (:18) and as card display text (:596-600), never as a request param; tvos — /Users/markususche/Desktop/blazing-tvos/Sources/FleetAPI.swift has no country arg on liveChannels and Sources/LiveView.swift:666 uses `channel.country` only to build a caption. Fire TV also still has no status line naming which list is on screen (LiveTvActivity.kt:140-171 builds rows with no banner).

### M36 — Fleet-supplied external subtitles reach only the Roku and the Fire TV

- **Missing on:** Apple TV, Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/SubtitleClient.kt:41-49 + PlayerActivity.kt:336 (fetch); /Users/markususche/Desktop/roku channels/components/screens/PlayerScreen.brs:196-223 (attach before play)`

tvos STILL MISSING: grep across /Users/markususche/Desktop/blazing-tvos/Sources/ for `addPlaybackSlave`, `Slave`, `/subtitles`, `subtitlesFor` returns ZERO hits. Sources/PlayerView.swift contains the word subtitle exactly once, in a comment at :244. The B18 track panel that landed since the audit only reads what is already inside the file: Sources/VLCControls.swift:97 `@Published var subtitleTracks: [PlayerTrack] = []` populated at :121-129 from the VLC media player's own track list, selected at :166 `func selectSubtitles(_ track: PlayerTrack?)`. So the audit's line `no track API in Sources/VLCControls.swift` is now stale, but the FLEET half is still absent. web STILL MISSING: grep for `subtitles/` across /Users/markususche/Desktop/blazing-web (node_modules excluded) hits only settings.js:93, which says so out loud — `subtitle files at all — grep '/subtitles/' in this repo finds nothing`. No `<track>` element and no `createElement('track')` anywhere in the repo; stream-preferences.js:115-118 reads only `hls.subtitleTracks` and `video.textTracks`. ROKU HAS IT: /Users/markususche/Desktop/roku channels/components/screens/PlayerScreen.brs:196 `sub ApplyExternalSubtitles(content as Object)` setting `content.SubtitleTracks = valid` at :220, called at :163 on every attempt. FIRETV HAS IT: client/app/src/main/java/com/nyctailblazers/blazingtv/SubtitleClient.kt:41 `fun forTitle(type: String, id: String): List<Track>` hitting `${base()}/subtitles/$t/…` at :49; called from PlayerActivity.kt:336 `val found = try { SubtitleClient.forTitle(subType, subId) } catch …` into `externalSubs` (PlayerActivity.kt:64).

### M38 — How much of the 39,000-channel index you can actually reach differs wildly per client

- **Missing on:** Fire TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/LiveView.swift:266-283 (walk with skip counted from the server's page size)`

firetv STILL CAPPED AT 50 AND NEVER PAGES: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/LiveTvActivity.kt:156 `LiveClient.channels(liveContext, group = g.id, country = LIVE_COUNTRY, limit = 50,)` and the fallback at :160 `LiveClient.channels(liveContext, group = g.id, limit = 50)`. The client half exists and is unused — LiveClient.kt:90 `skip: Int = 0` and :104 `append("?skip=").append(skip).append("&limit=").append(limit)` — grep for `skip` in LiveTvActivity.kt returns nothing. TVOS HAS THE WALK: /Users/markususche/Desktop/blazing-tvos/Sources/LiveView.swift:266 `private func walk(key: String, group: String?, query: String, from start: Int)` — :267 `var skip = start`, :274 `if skip >= total { break }`, :276 fetches the next page, :280 `skip += page.channels.count` (counted from what the server sent, per the note at :262-265), :283 sets `\(rows.count) of \(total) loaded…`. ROKU'S 8,000 IS A DELIBERATE, DOCUMENTED MEMORY CEILING — leave it: /Users/markususche/Desktop/roku channels/source/lib/LiveTV.brs:99-113 `8000 STAYS, and it stopped being a "covers everything" number and became a MEMORY CEILING … browsing now reaches about a FIFTH of the index`, function LiveTvFleetBudget() returns 8000 at :114-116, and the screen reports the server's own total. Web is unrestricted.

### M39 — Junk playlist rows ("##### CBS ALABAMA #####", "- NO EVENT STREAMING -") are filtered only in the browser

- **Missing on:** Roku, Fire TV, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-web/livetv.js:197-208 (isPlaceholder, ~10 lines, no dependencies)`

WEB STILL THE ONLY ONE: /Users/markususche/Desktop/blazing-web/livetv.js:197-208 `function isPlaceholder(channel) { … if (/no event streaming/i.test(name)) return true; … if (/^[#\-=*\s]{3,}.*[#\-=*\s]{3,}$/.test(name)) return true; }` with the pattern provenance at :190-196, applied at livetv.js:864 `keep.push(...rows.filter((c) => !isPlaceholder(c)…`, :1077 and :1098 `if (!id || isPlaceholder(row)) continue;`, and exported at :1385. ROKU MISSING: /Users/markususche/Desktop/roku channels/source/lib/LiveTV.brs:146 `if nm = "" or cid = "" then continue for` — empty name/id only, inside the budget loop whose counter is bumped after the test; the search path repeats the same bare test at LiveTV.brs:258. grep for `Placeholder`, `#####`, `NO EVENT`, `junk` across source/lib/LiveTV.brs and components/screens/LiveTvScreen.brs returns zero hits. FIRETV MISSING: client/app/src/main/java/com/nyctailblazers/blazingtv/LiveClient.kt:114-127 `Json.objects(arr).map { c -> LiveChannel(id = …, name = c.optString("name").ifBlank { c.optString("id") }, …) }` — a straight map with no name predicate; grep for `Placeholder`/`#####`/`NO EVENT` in LiveClient.kt and LiveTvActivity.kt returns nothing. TVOS MISSING: /Users/markususche/Desktop/blazing-tvos/Sources/LiveView.swift:294-313 `private func publish(_ found: [Channel], replacing: Bool)` filters only on the parental predicate (`let found = found.filter { allowed($0) }`, :306) and de-dups by id (:307); the only `placeholder` token in the file is a SwiftUI AsyncImage argument at :686.

### M40 — Language failover — skipping a release that has no English audio — differs on all four

- **Missing on:** Web, Apple TV
- **Fix size:** large
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/PlayerActivity.kt:803-830 (settleLanguage, including the und rule at :813-817) with MAX_LANGUAGE_SKIPS at :1096`

web STILL MISSING: /Users/markususche/Desktop/blazing-web/stream-preferences.js:143-145 `if (!manualAudio && preferences.audio === 'english') { const index = found.audio.findIndex(english);` — it selects an English track only when the file already carries one; there is no skip, no candidate advance, and no fallback toast anywhere in the file (grep for `skip` in stream-preferences.js returns nothing). tvos STILL MISSING: no automatic language logic in /Users/markususche/Desktop/blazing-tvos/Sources/PlayerView.swift or Sources/VLCControls.swift — every `language` hit is display or manual selection (VLCControls.swift:121/:125 build PlayerTrack, :145 oneAudioTrackAdvice, :160 `print("[play] audio -> …")` after a user press). FIRETV IS THE REFERENCE: client/app/src/main/java/com/nyctailblazers/blazingtv/PlayerActivity.kt:803 `private fun settleLanguage(tracks: …)`, the und rule at :813-817 `"und" is NOT evidence of a foreign track` / `.filter { it.isNotBlank() && it != "und" }`, the bounded skip at :822-825 `if (allForeign && languageSkips < MAX_LANGUAGE_SKIPS && current + 1 < candidates.size) { languageSkips++; tryNextCandidate(); return }`, `MAX_LANGUAGE_SKIPS = 3` at :1096, called from :629 `if (playable) settleLanguage(tracks)`. ROKU HAS IT BUT UNBOUNDED, AND STILL BEHIND THE TWO-TRACK GATE: /Users/markususche/Desktop/roku channels/components/screens/PlayerScreen.brs:1029 `sub SelectEnglishTracks()`, :1031 `if tracks <> invalid and tracks.Count() > 1` (a single-track dub is never examined), :1051-1053 `print "[play] no English audio among "…: m.top.tryNext = m.top.tryNext + 1` with no cap.

### M42 — Marking a source dead is a mouse-only right-click on web, and three different gestures elsewhere

- **Missing on:** Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/DetailsScreen.brs:2584 (key-reachable) or /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:1740 (long-press)`

Web unchanged, only the line numbers moved. /Users/markususche/Desktop/blazing-web/app.js:4170-4176 is still the whole mark-dead path: `row.addEventListener('contextmenu', (event) => { event.preventDefault(); if (!deadLinks.includes(s.url)) deadLinks.push(s.url); markDeadLink(s.url); ... })`. I read the whole row builder (app.js:4109-4180): the only two listeners on a stream row are `row.addEventListener('click',...)` at :4146 and that contextmenu at :4170 — no visible Remove/Report control, no pointerdown/touchstart long-press, no keydown. `rg -n 'markDead|dead' /Users/markususche/Desktop/blazing-web/dpad.js` returns nothing, so no remote key is bound to it either. The one global handler, app.js:6526, is Back/Escape only (`if (!isBack) return;` at :6535). The three TV clients all still have it: roku components/screens/DetailsScreen.brs:2584-2596 (`if key = "replay" or key = "instantreplay"` → :2591 `MarkBadLink(DetNz(st.url))` → :2592 'Marked as the wrong film. It will not be offered again.'); firetv DetailActivity.kt:1740-1743 `setOnLongClickListener { onMarkDead(); true }` wired at :1784 `onMarkDead = { markStreamDead(s) }` → :1813 `markStreamDead`; tvos Sources/Sidecar.swift:116-121 `.contextMenu { Button(role: .destructive) { onMarkDead(s) } label: { Label("Mark Dead Link", systemImage: "xmark.bin") } }`. blazing-web ships to webOS/Tizen, so this is still unreachable from a remote.

### M44 — Music and Podcasts exist only on the web

- **Missing on:** Roku, Fire TV, Apple TV
- **Fix size:** large
- **Copy from:** `/Users/markususche/Desktop/blazing-web/media-library.js:5 (and tvos Sources/ReadingView.swift BooksShelfView as the shelf component to extend)`

Unchanged. Web still has both: /Users/markususche/Desktop/blazing-web/media-library.js:5 `const KINDS = ['books', 'music', 'podcasts'];`, :172 `request('/media/${kind}/${id}/${kind === 'music' ? 'tracks' : 'episodes'}')`, :357 the per-kind search placeholder, and index.html:189 `<button type="button" data-view="podcasts">Podcasts</button>` with the host at index.html:447 and the script at :1130. ROKU: `rg -ni 'podcast' components/ source/` returns ZERO hits; the only 'music' hit in the whole channel is a Live TV group name in source/lib/LiveTV.brs:87. TVOS: `rg -ni 'podcast' Sources/` returns ZERO hits — ReadingView.swift still holds books only. FIRETV: the single hit is still Catalogs.kt:218 `"blazing-edu-podcasts", "blazing-edu-kids" -> "tv"`, a slug inside `mediaTypeForSlug`, i.e. a media-type map entry and not a music/podcast screen. Note the audit's own instruction stands: confirm with Markus before building three of these, it may be a deliberate web-only choice.

### M45 — My Locker lands at a different height on Home on every client

- **Missing on:** Roku, Fire TV, Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/HomeView.swift:554 (second band, right behind the leading fresh row)`

All four still disagree, and each is at the same line the audit named. ROKU last of the fresh rows: /Users/markususche/Desktop/roku channels/components/screens/HomeScreen.brs:52 `m.freshOrder = ["in-theaters", "new-seasons", "because", "top-rated", "locker"]`, consumed forward at :307-313 `for each slug in m.freshOrder ... out.Push(row)` — so locker draws LAST. Worth flagging to the fixer: the comment immediately above it (HomeScreen.brs:49-51) now claims the opposite — 'The locker leads the live shelves ... should be the first thing on Home' — so the file contradicts itself. FIRETV first, above every discovery shelf: HomeFragment.kt:432 `val (lockerRow, lockerAdapter) = addLoadingRow("My Locker")` inside the `ProfileGateRules.grownUp` gate at :431, and the shelf loop only starts at :448 `for (shelf in HomeDiscovery.shelves)`. TVOS second: Sources/HomeView.swift:554-557 `if row.shelf.id == model.freshRows.first?.shelf.id, locker.visible, profileSession.maySeeLocker, !locker.files.isEmpty { out.append(lockerBand) }` with the empty-freshRows fallback at :561-565. WEB after the LAST fresh shelf: locker.js:185-186 `const fresh = rows.querySelectorAll('[data-fresh-shelf="true"]'); const anchor = fresh.length ? fresh[fresh.length - 1].nextSibling : rows.firstChild;`.

### M46 — No Trailer button on the web title detail sheet

- **Missing on:** Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/DetailSheet.swift:765`

Web still has no Trailer control. /Users/markususche/Desktop/blazing-web/index.html:981-1000 is the title sheet's `.detail-actions` and it holds #detail-play, #detail-save, #detail-collection, #detail-watched, #detail-upscale, #quality-select and #detail-source-link — no trailer button. (The B31 work added #detail-collection and #detail-watched since the audit, so the list grew but not with a Trailer.) The only 'Play trailer' in the file is still index.html:793 `<button id="game-detail-trailer-btn" ...>Play trailer</button>`, inside the GAME detail dialog. `rg -n 'detail-trailer|startDetailTrailer|resolveTrailerUrl' app.js index.html` shows the ambient path only: index.html:973 `<div id="detail-trailer" class="detail-trailer" aria-hidden="true">`, app.js:5501 `async function startDetailTrailer(meta)` called from app.js:3350, and app.js:2354 `async function resolveTrailerUrl(meta)` whose only other caller is the card-hover preview at app.js:2448 — so a real trailer URL is already resolved and nothing exposes it full-screen. All three TVs still have the button: roku components/screens/DetailsScreen.brs:1484 `actions.Push({ kind: "trailer", label: "Trailer" })`; firetv DetailActivity.kt:1192 `text = "▶  Trailer"` (intent at :1231); tvos Sources/DetailSheet.swift:765-766 `SheetButton(title: "Trailer", icon: "film", primary: false, action: onTrailer)` wired at RootView.swift:1269.

### M48 — Only Apple TV ranks the candidate streams; the other three take whatever came back first

- **Missing on:** Roku, Fire TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/LiveView.swift:555 (note: Fire TV must NOT demote needsHeaders — it can send referrer/userAgent; health tier only)`

Unchanged on both blamed clients. TVOS still the only ranker: /Users/markususche/Desktop/blazing-tvos/Sources/LiveView.swift:555-561 `static func ranked(_ sources: [LiveSource]) -> [LiveSource] { sources.enumerated().sorted { ... return ka == kb ? a.offset < b.offset : ka < kb }.map(\.element) }` with the key at :564-567 `let health = source.healthy == true ? 0 : (source.healthy == nil ? 1 : 2); return health * 2 + (source.needsHeaders ? 1 : 0)`. It is reached from BOTH tvOS call sites — LiveView.swift:523 `let ranked = Self.ranked((try? await FleetAPI.liveStreams(ch.id)) ?? [])` and LiveGuideGrid.swift:394 `let ranked = LiveView.ranked(...)`. ROKU still first-non-empty: source/lib/LiveTV.brs:349-354 `for each s in j.streams / candidate = LiveTvFleetStream(s, name) / if candidate = invalid then continue for / ... return candidate` inside `LiveTvFleetPlay` (:338) — no sort, no health read. FIRETV still no sort: LiveClient.kt:132-147 `fun streams(ctx, channelId)` does `Json.objects(arr).mapNotNull { ... }` straight through, and the KDoc at :131 still only CLAIMS 'best first'. WEB needs nothing, confirmed: livetv.js:939 `const ticket = await liveFetch('/live/ticket/...')` — the server resolves and the client never sees candidates. The audit's own UNVERIFIABLE note stands: whether /live/stream sorts by health is a blazing-fleet question and that repo is not in this pass.

### M49 — Only Fire TV puts a finished title into the Watched list

- **Missing on:** Roku, Web, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/PlayerActivity.kt:551 (Roku's write already exists at components/tasks/RichMetaTask.brs:148)`

Still Fire TV only, though all three others now have the list client the audit said they were waiting for — so this is a call site on each, not plumbing. FIRETV: PlayerActivity.kt:551-553 `if (!isLive) { intent.getStringExtra("id")?.let { markFinished(it) } }` on the real-end branch at :545, guarded by `finishedRecorded` (:82). ROKU: components/MainScene.brs:1023-1025 the >=95% branch is still `print "[cw] finished ..."` then `RemoveContinue(m.lastTitle.id)` and nothing else — no FleetAdd. `rg -n 'FleetAdd' components/ source/` returns exactly two lines: the definition source/lib/Fleet.brs:350 and the single call site components/tasks/RichMetaTask.brs:148, which is the detail-sheet toggle. WEB: the `ended` handler at app.js:5658-5667 fires `telemetry('play_end', {... reason: 'finished'})` then `autoNextEpisode()` — no list write. The list client exists (lists.js:53 `const WATCHED = 'watched'`, :316 `async function add(list, meta)`) but `rg -n 'BlazingLists\.(add|toggle)' app.js` returns ONE hit, app.js:1140 `const nowIn = await window.BlazingLists.toggle(list, meta);` — the detail button. TVOS: Sources/SavedLists.swift:266 `static func add(_ item: SavedListItem, to list: SavedList, ...)` exists, but `rg -n 'to: \.watched'` finds no completion caller; the only completion-shaped code is RootView.swift:875 `func recordProgress(...)`, which writes ContinueEntry via ProgressSync.save (:889) and, per its own comment at :872-874, only takes the title OUT of Continue Watching past 97%.

### M50 — Only Fire TV shows anything before you type; Roku, web and tvOS open on an empty screen

- **Missing on:** Roku, Web, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:157 if browse wins; /Users/markususche/Desktop/roku channels/components/screens/SearchScreen.brs:87 is the stated rule if the empty screen wins`

Unchanged on all four, and it is still a DECISION and not a port — nobody has settled which rule wins. FIRETV still browses: SearchActivity.kt:99 `val BROWSE = listOf(` under the comment at :84 'CATEGORIES TO BROWSE BEFORE YOU TYPE.', shown at :143 `if (!browseShown) showBrowse()` (onResume), built at :157 `private fun showBrowse()` with the pairs consumed at :166 `for ((type, genre) in BROWSE)`, and returned to at :192 `if (!browseShown) showBrowse()` below the character threshold. ROKU still deliberately empty, with Markus quoted in-file: components/screens/SearchScreen.brs:86-100 — `m.items = []` at :91 and :100 `m.status.text = "Type to search." + Chr(10) + "Results appear as you type."`, comment at :87-89 'when you go to search, don't put the movie posters, load the movie posters when somebody types'; the same decision is repeated at components/MainScene.brs:1483-1484 'NO PREFILL'. TVOS empty: Sources/Sections.swift:368-369 `@State private var items: [MetaItem] = []` / `@State private var status = "Type a title."` in SearchView (:360) — nothing fetched on appear. WEB empty: app.js:1474-1487 `function clearSearchResults()` blanks #search-media-results, #search-chip-row, #search-facets, #search-status and #search-results, called on entry at app.js:1429. Because the empty screen is Markus's own stated instruction quoted twice in the Roku source, the cheaper resolution is removing showBrowse from Fire TV (SearchActivity.kt:143 and :192) rather than porting it to three clients — but the audit is right that this needs Markus to pick before any code moves.

### M51 — Only Fire TV tests for an active profile at the door of the Search screen

- **Missing on:** Roku, Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:28`

firetv HAS it: client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:28 `if (requireProfileOrGate(this)) return`, defined at ProfileGateActivity.kt:63. tvos HAS it: Sources/RootView.swift:1097-1098 `if profileSession.activeProfile == nil { FleetProfileGate(session: profileSession) }`. roku MISSING: components/MainScene.brs:1481-1488 `sub onHomeActionNamed(name as String) / if name = "search" ... navTo("search")` — no profile test; navTo at :7628-7631 is just push+show; source/lib/Profiles.brs:148-152 ProfileMaxRating() still fails closed to "general". web MISSING: app.js:2109-2112 `if (route === 'search') { bindSearch(); setTimeout(() => $('#search-input').focus(), 0); }` — no gate; bindSearch at app.js:1418-1434 and runSearch at app.js:1655 never read state.profileId.

### M52 — Only Roku searches YouTube; the other three have no video results at all

- **Missing on:** Fire TV, Web, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/MainScene.brs:3236`

roku HAS it: components/MainScene.brs:3235-3236 `runTask({ kind: "search", query: a.query })` AND `runYouTubeTask({ kind: "search", query: a.query, limit: 12 })`; components/screens/SearchScreen.brs:151 `{ label: "YouTube", items: [] }`; MainScene.brs:3244-3248 a youtube mediaType plays instead of opening details. firetv MISSING: SearchActivity.kt:267-278 still only movies/series/mrWorld (`async(Dispatchers.IO)` x3 at :267, :270, :273); a case-insensitive grep of SearchActivity.kt for youtube returns nothing — even though YouTubeClient.kt:127 `fun search(query: String, limit: Int = 24)` exists. web MISSING: app.js:1685-1690 the fan-out is `Promise.allSettled([BlazingEmby.search, fleetSearch('movie'), fleetSearch('series'), searchSiteCatalogs(query)])` — no YouTube leg; youtube.js:1055 exposes window.BlazingYouTube, mounted separately at app.js:2086. tvos MISSING: Sources/Sections.swift:456-458 `async let movies / series / mrWorldPremiere` only; and there is NO YouTube search call to borrow — Sources/FleetAPI.swift:275 is the only youtube route and it is `youtubeHome(per:)`, so tvOS needs the search call written as well as wired.

### M53 — Only Roku searches the anime library; the other three cannot find an anime title by name

- **Missing on:** Fire TV, Web, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/tasks/AddonTask.brs:659`

roku HAS it: components/tasks/AddonTask.brs:659 `for each s in AnimeSearch(q)` merged into doSearch; source/lib/AddonClient.brs:27 `function AnimeSearch(query as String)`; components/screens/SearchScreen.brs:150 `{ label: "Anime", items: [] }`. firetv MISSING: SearchActivity.kt:267-278 three legs only, no anime/kitsu reference in the file; the call to borrow is still there unused at AnimeContent.kt:51-56 `private fun resolveKitsuId(title: String) ... "${Catalogs.KITSU}/catalog/series/kitsu-anime-list/search=" + URLEncoder.encode(clean,"UTF-8")`. web MISSING: app.js:1685-1690 fan-out has no anime leg; anime-room.js:5 `const KITSU = 'https://anime-kitsu.strem.fun'` and anime-room.js:153 `json(`${KITSU}/catalog/anime/kitsu-anime-list/search=${encoded}.json`)` is a separate search inside the Anime room only. tvos MISSING: Sources/Sections.swift:456-458 no anime leg, and no kitsu call exists anywhere in Sources/ to borrow.

### M54 — Only the Roku can resume playback from the Home hero; the other three can only open the detail page

- **Missing on:** Fire TV, Apple TV, Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/HomeScreen.brs:2436`

roku HAS it: components/screens/HomeScreen.brs:1479 `if item.resume = true then m.heroWatchLabel.text = "Resume" else m.heroWatchLabel.text = "Watch Now"`; :2436-2442 `if item.resume = true` sets `m.top.chosen = { action: "resume", id, mediaType, name, poster ... }` with the comment "A Continue Watching card resumes the stream it was watching, rather than sending you back to Details". web MISSING: index.html:346 `<button type="button" id="home-hero-play" class="primary-button">Play</button>` — label is still Play; app.js:1050 `openDetail(homeHeroMeta, { focusPlay: true });` is the whole handler (bound at app.js:1021). tvos MISSING: Sources/HomeView.swift:691-697 the Continue Watching BandItem's action is `shell.openDetail(DetailTarget(id: item.id, type: item.type, name: item.name, poster: item.poster))`; Sources/RootView.swift:621-624 `func openDetail(_ target: DetailTarget) { detailGate?.cancel(); detail = target; ... }` only presents the sheet; Sources/HomeView.swift:2002 the hero pill is still `HeroButton(title: "View Details", icon: "info.circle", primary: true, action: openDetail)`. firetv MISSING: HeroCardPresenter.kt:216 `text = "View Details"` with :220 `isFocusable = false`; HomeFragment.kt:143 the item click listener falls through to :176 `else -> openDetail(meta)` and :659-661 `private fun openDetail(meta) { startActivity(DetailActivity.intent(...)) }`.

### M55 — Only the Roku tells the aggregator a stream is dead

- **Missing on:** Fire TV, Web, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/tasks/AddonTask.brs:1543`

roku HAS it end to end: components/screens/PlayerScreen.brs:760 `sub emitDead(why as String)` and :777 `m.top.deadStream = {...}`, reached from :287, :428, :447, :463, :499; components/MainScene.brs:181 `m.player.ObserveField("deadStream", "onDeadStream")` and :839-855 forwards `kind: "reportdead"`; components/tasks/AddonTask.brs:25-27 handles it first, and :1543-1560 `sub doReportDead` does `url = base + "/dead/" + mt + "/" + vid + ".json"` / `HttpPostJson(url, { url: Nz(req.url) }, invalid, 6000)`. firetv MISSING: a recursive case-insensitive grep of client/app/src/main for reportdead|report-dead|report_dead|deadstream|dead-link returns ONE hit, an unrelated comment in AddonClient.kt — there is no report-dead call anywhere. tvos MISSING: Sources/RootView.swift:387-394 `func markDead(_ stream: RankedStream) { ... DeadLinks.mark(id); reRankSidecar() }` and Sources/SourcePrefs.swift:240-256 `enum DeadLinks { private static let key = "dead_links" ... defaults.set(dead, forKey: key) }` — UserDefaults on this device only. web MISSING: app.js:311-331 `function markDeadLink(url) { ... writeDeadLinks(map) }` and app.js:302-304 `localStorage.setItem(DEAD_LINKS_STORE, ...)` — this browser only.

### M56 — Only the browser marks a Kids or PIN-protected profile on the picker row; Roku computes the badge and never draws it

- **Missing on:** Roku, Fire TV, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-web/profile.js:943`

web HAS it: profile.js:943 `const meta = profile.disabled ? 'Not available' : profile.isKids ? 'Kids' : profile.hasPin ? 'PIN required' : '';` and :946 `if (profile.hasPin) button.append(element('span', 'bp-profile-tag', 'PIN'));`. roku STILL COMPUTES AND DISCARDS: components/screens/ProfileGate.brs:282-290 `function GateBadge(p) ... bits.Push("PIN") / "Kids" / "Off" ... return bits.Join("  ·  ")`; :249 `entries.Push({ kind: "profile", label: p.name, badge: GateBadge(p), profile: p })`; buildOneCard's returned model at :552-564 is `{ kind, profile, avatar, name, isProfile, pencilPill, pencilGlyph, focusOnlyName, disabled, focusY, backdrop }` — no badge field, and a grep of the file for `e.badge` returns zero reads. firetv MISSING: ProfileGateActivity.kt:1077-1093 the row is avatar art plus one TextView name, `visibility = View.INVISIBLE` at :1084, made visible only on focus at :1093 — no Kids/PIN meta. tvos MISSING: Sources/RootView.swift:2113-2122 the row VStack is `Text(profile.name)` plus, only when disabled, `Text("Turned off by the owner")`; hasPin appears only in the accessibility label at :2076 `profile.hasPin ? "\(profile.name), PIN required" : profile.name`.

### M57 — Only the web client searches the household's own Emby server

- **Missing on:** Roku, Fire TV, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-web/app.js:1686`

web HAS it: app.js:1686 `window.BlazingEmby ? window.BlazingEmby.search(query) : Promise.resolve([])` is the first leg of the Promise.allSettled fan-out, and app.js:1731-1739 puts the Emby rows first in the dedupe (`for (const [raw, meta] of [...embyRows, ...catalogRows, ...siteRows])`) so the local copy wins. roku MISSING (wiring only): components/tasks/EmbyTask.brs:24 `else if kind = "search"` and :96 `m.top.response = { kind: "search", ok: true, items: EmbySearch(q, mediaType), q: q }` exist, and source/lib/Emby.brs:105 `url = EmbyFleetBase() + "/emby/search?q=" + UrlEscape(q)` exists — but the ONLY runEmbyTask call site in the whole scene is components/MainScene.brs:295 `runEmbyTask({ kind: "rows", limit: 12 })`, so the search branch is dead. firetv MISSING: EmbyClient.kt:167 `fun search(query: String, mediaType: String? = null): List<Item>` exists; SearchActivity.kt:267-278 has no Emby leg and no Emby reference in the file. tvos MISSING: Sources/EmbyAPI.swift:163 `static func search(_ query: String, type: String? = nil) async -> [Item]` exists; Sources/Sections.swift:456-458 does not call it.

### M60 — Pressing an episode does four different things

- **Missing on:** Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:1983`

The audit's own CORRECTION line is right and the title is wrong — three clients behave identically and only tvOS differs. roku: components/screens/DetailsScreen.brs:162 `m.episodes.ObserveField("rowItemSelected", "onEpisodeChosen")`, :2153 `sub onEpisodeChosen()`, :1341 `sub restoreSeasonBlock()` — the source list replaces the season blocks. firetv: DetailActivity.kt:1968 `showEpisodeStreams(ep)` from the tile's click listener, :1983 `private fun showEpisodeStreams(ep: AddonClient.Episode, autoPlay: Boolean = false)`. web: app.js:3199 `button.addEventListener('click', () => choose(episode));` into app.js:3145 `function choose(episode)`. tvos STILL MISSING the chooser: Sources/DetailSheet.swift:849 `EpisodeTile(episode: ep) { onPlayEpisode(ep) }`; Sources/RootView.swift:1272-1274 `onPlayEpisode: { [d = shell.detail!] ep in shell.playEpisode(ep, showName: d.name) }`; Sources/RootView.swift:411-420 `func playEpisode(_ episode: EpisodeVideo, showName: String) { detail = nil; ... playBest(title: label, type: "series", id: episode.id, poster: episode.thumbnail ?? "") }` — the sheet is dismissed and the ranker's pick starts, with no source list presented.

### M62 — Progress save cadence and minimum differ on every client, and the web never writes a final position

- **Missing on:** Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/PlayerActivity.kt:926-933 (15s ticker plus a write on pause) and :982 (a write on the way out)`

Unchanged since the audit. web: /Users/markususche/Desktop/blazing-web/app.js:5385-5396 `function startSync(meta) { stopSync(); syncInterval = setInterval(() => { if (!video.duration || video.paused) return; ... }, 30000); }` — still a 30 second interval and a paused player still returns without writing. app.js:5012-5027 `function closePlayer()` calls `stopSync();` at :5019 and then `video.removeAttribute('src')` at :5022 with NO final POST. `rg -n "sync/progress" app.js` returns exactly one POST site, app.js:5391, and `rg -n "addEventListener\('pause'" app.js *.js` returns nothing. TV clients still have it: firetv client/app/src/main/java/com/nyctailblazers/blazingtv/PlayerActivity.kt:926-927 `saveProgress(); progressTicker.postDelayed(this, 15_000)`; :933 saveProgress() on pause; :982 saveProgress() after `progressTicker.removeCallbacks(progressTick)` on the way out. tvos Sources/PlayerView.swift:387 `onProgress(position, duration)`, :709 and :723 the same on the exit/VLC paths; minimum 10s at Sources/RootView.swift:876 `guard let origin = item.origin, position >= 10 else { return }`. The four minimums still disagree (roku none, firetv 10s, tvos 10s, web none).

### M63 — Raising a rating cap re-asks for the PIN on Fire TV; Roku reuses one token taken at the door

- **Missing on:** Roku
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/ParentalSettingsActivity.kt:120 and :223`

Roku's rating-cap dialog is unchanged. /Users/markususche/Desktop/roku channels/components/MainScene.brs:5462 `sub onProfileRatingSelected()`; :5479 `f.maxRating = ratings[idx]`; :5480 `submitProfilePatch(f)` — no token test, no PIN re-entry. submitProfilePatch at :5287 `runProfileTask({ kind: "patch", id: Nz4(m.editTarget.id), fields: fields, unlockToken: m.editToken })`, and m.editToken is minted once at :5170 `m.editToken = unlockToken` inside the edit-session open. WORTH NOTING, AND IT IS NOT THE SAME THING: a raise pre-check WAS added since the audit, but only on the ADULT toggle — MainScene.brs:5540 `raising = (m.editTarget.allowAdult <> true) or (LCase(Nz4(m.editTarget.maxRating)) <> "adult")`; :5541 `if idx = 0 and raising and Nz4(m.editToken) = ""` — and even there it REFUSES with a message ("Set a PIN first") rather than re-entering the PinPad. The cap dialog at :5449-5459 (openProfileRatingDialog) has no equivalent at all. firetv still has the model: client/app/src/main/java/com/nyctailblazers/blazingtv/ParentalSettingsActivity.kt:120 `if (raising) requireUnlockThen(p) { token -> apply(maxRating = r, unlockToken = token) {} }`; :223 `private fun requireUnlockThen(p: ProfileClient.Profile, onUnlocked: (String) -> Unit)`. tvos/web still have no rating control, so nothing to re-gate.

### M64 — Results are grouped into labelled rows on Roku and Fire TV, and dumped into one flat grid on web and tvOS

- **Missing on:** Web, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/SearchScreen.brs:147-176 (buckets, empty groups skipped, count in the label)`

Unchanged, and the audit's suggested web helper no longer exists. web: /Users/markususche/Desktop/blazing-web/app.js:1524 `function renderSearchCards()`; :1535 `$('#search-results').replaceChildren(...cards.map(buildCard));` — one merged grid. buildResultRow() has since been DELETED: app.js:1770 `/* buildResultRow() lived here. It had exactly one caller ... B22 replaced that grid */`, so the audit's fix note ("buildResultRow() already exists at app.js:1729") is now out of date. tvos: /Users/markususche/Desktop/blazing-tvos/Sources/Sections.swift:406 `LazyVGrid(columns: columns, alignment: .leading, spacing: 26) {`; :407 `ForEach(items) { item in` — a single grid over the merged `items`; run() at :447 concatenates movies + series + mrWorldPremiere into one `all` array with no type grouping. Grouping still present on the two TV clients: roku components/screens/SearchScreen.brs:147 `groups = [` (five buckets); :160 `for each g in groups`; :163 `row.title = g.label + "  ·  " + Stri(g.items.Count()).Trim()`. firetv client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:291 `results.add(ListRow(HeaderItem(if (type == "movie") "Movies" else "Series"), a))`; :308 `results.add(ListRow(HeaderItem("MrWorldPremiere"), a))`.

### M65 — Roku Home carries eleven shelves no other client builds

- **Missing on:** Fire TV, Apple TV, Web
- **Fix size:** large
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/HomeScreen.brs:743-786 (built locally from rows already in hand, no extra network call)`

Nothing was ported. roku: /Users/markususche/Desktop/roku channels/components/screens/HomeScreen.brs:743 `function BuildEditorialHome(rows as Object) as Object`, called at :607; the eight pushes are at :747 "Visually Pleasing", :750 "Action Sci-Fi & Fantasy", :753 "Romantic Comedies", :756 "Get in on the Action", :759 "Channels & Apps", :761 "Top 10 TV Shows", :763 "Top 10 Movies", :764 "Watch in Order"; plus components/MainScene.brs:2450 and :2536 homeTitle "Because you watched". A ripgrep for all nine of those titles across /Users/markususche/Desktop/firetv, /Users/markususche/Desktop/blazing-tvos and /Users/markususche/Desktop/blazing-web returns ZERO shelf hits (only unrelated drawer rows: firetv DrawerView.kt:158 addHeading("CHANNELS & APPS"), tvos Sources/Drawer.swift:228 DrawerHeading("CHANNELS & APPS"), web index.html:265 `<p class="drawer-section">Channels &amp; Apps</p>`, and the Watch Party module files). The other three are still three shelves each: firetv client/app/src/main/java/com/nyctailblazers/blazingtv/HomeDiscovery.kt:13-17 `val shelves = listOf(Shelf("in-theaters"...), Shelf("new-seasons"...), Shelf("top-rated"...))`; tvos Sources/HomeView.swift:84-100 `private static let freshShelves: [FreshHomeShelf] = [...]` three entries; web app.js:41-60 `const FRESH_HOME_SHELVES = Object.freeze([...])` three entries. The audit's cheaper alternative was not taken either: a ripgrep of /Users/markususche/Desktop/blazing-addon for the band titles returns nothing. NOTE the audit itself says this is Markus's call, not a code fix.

### M66 — Roku carries a second "Edit Profiles" list row that no other client has, and it can edit a disabled profile the pencil refuses

- **Missing on:** Roku
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/ProfileGate.brs:866 — the same `c.disabled = true` test, applied inside openProfileEditPicker at components/MainScene.brs:5088`

Both halves still true. roku: components/screens/ProfileGate.brs:273 `entries.Push({ kind: "editprofiles", label: "Edit Profiles", badge: "" })`. roku: components/screens/ProfileGate.brs:862-868 `function canEditFocused() as Boolean` with :866 `if c.disabled = true then return false` — the pencil's guard. roku: components/MainScene.brs:5079 `sub openProfileEditPicker()`; :5088-5091 `names = [] / for each p in profs / names.Push(Nz4(p.name)) / end for` — every profile in payload.profiles, no `p.disabled` test anywhere in the sub (it ends at :5104), and :5116 `beginProfileEdit(profs[idx])` takes the pick straight through. So the two doors still disagree. The other three still have exactly one door: firetv client/app/src/main/java/com/nyctailblazers/blazingtv/ProfileGateActivity.kt:851-852 `profiles.forEach { p -> list.addView(profileRow(p)) }` then `list.addView(addProfileRow())` — no manage row; tvos Sources/RootView.swift:1965 `FleetAddProfileRow(isFocused: focusedProfileID == "add", action: onCreate)` as the only extra row; web profile.js:992 the single `'Add profile'` tile.

### M67 — Roku moves the focus onto the results by itself; no other client does

- **Missing on:** Fire TV, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/SearchScreen.brs:203-206 and :225-231`

Unchanged. roku still has it: components/screens/SearchScreen.brs:203-206 `if m.top.visible and QueryText() = m.firedQuery and m.focusedForQuery <> m.firedQuery` then `moved = focusResults()` / `if moved then m.focusedForQuery = m.firedQuery` — both guards (once per query, and only while the box still holds the fired text); :225-231 `function focusResults() as Boolean ... m.results.SetFocus(true)`. firetv MISSING: `rg -n "requestFocus|setSelectedPosition|selectedPosition" client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt` returns ZERO hits; the adapter fill at :291 and :308 is followed by nothing that moves focus. tvos MISSING: `rg -n "FocusState|focused" Sources/Sections.swift` returns ZERO hits; run() at Sections.swift:447 assigns items and reseeds the hero only. web: app.js:1430 `$('#search-input').focus();` on route open is the only focus call; runSearch never moves focus (the audit itself excuses web as a pointer UI).

### M68 — Rotten Tomatoes / Metacritic style rating chips only exist on the two TV clients

- **Missing on:** Web, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:839-841 and :964 ('<source>  <value>')`

Unchanged on both missing clients, and tvOS is still decoding the data and throwing it away. web: /Users/markususche/Desktop/blazing-web/app.js:5785 `function renderRatingChips(meta)`; :5799-5802 `add(meta.certification, 'detail-chip-cert'); add(meta.imdbRating ? ...); add(meta.runtime); if (meta.genres && meta.genres.length) add(meta.genres.join(' · '));` — certificate, IMDb, runtime, genres only. `rg -n "\.ratings|Rotten|Metacritic" app.js *.js` finds nothing in the detail path (the only hit is games.js:223, an unrelated game metascore). Called at app.js:3308 and :3334, both with the catalog `meta`, never the rich payload. tvos: Sources/DetailSheet.swift:492-500 `private var chips: [String]` — certification, releaseInfo, IMDb, runtime, 3 genres, and nothing else. The data IS decoded and unused: Sources/RichMeta.swift:21 `var ratings: [Rating] = []` and :156 `ratings = (try? c.decodeIfPresent([Rating].self, forKey: .ratings)) ?? []`; `rg -n "ratings" Sources/*.swift` shows no render site. Still present on the TV pair: roku components/screens/DetailsScreen.brs:882 `sub buildChips(r as Object)`; :894-905 the per-rating loop four to a row; :908-909 the certification appended last. firetv client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:839-841 `ratingChips.removeAllViews(); for (r in rich.ratings) ratingChips.addView(ratingChip(r))`; :964 `private fun ratingChip(r: RichMetaClient.Rating)`.

### M69 — Search-as-you-type on Roku and Fire TV; submit-only on web and tvOS — and the two live ones use different thresholds

- **Missing on:** Web, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:148-149, :191, :200 (2 characters / 300ms — the pair the audit recommends standardising on)`

Nothing changed on any of the four. roku: components/screens/SearchScreen.brs:33-35 `m.debounce = CreateObject("roSGNode", "Timer") / m.debounce.duration = 0.9 / m.debounce.repeat = false`; :84 `if Len(q) >= 1` starts it; :112-121 onDebounceFired fires `m.top.chosen = { action: "search", query: q }`. Still 1 character at 900ms. firetv: client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:148-149 `override fun onQueryTextChange(newQuery: String): Boolean { runSearch(newQuery); return true }`; :191 `if (query.length < 2) {`; :200 `delay(300)`. Still 2 characters at 300ms — the two thresholds are still unreconciled. web MISSING: app.js:1425 `form.addEventListener('submit', runSearch);` is the only trigger apart from the mic result at :1459-1460 `$('#search-input').value = transcript; runSearch();`; no `input` listener exists. tvos MISSING: Sources/Sections.swift:387 `.onSubmit { Task { await run() } }` on the Title field; there is no `.onChange` on `query` anywhere in the file.

### M70 — Signing in with an email address exists only on Roku and web

- **Missing on:** Fire TV, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/MainScene.brs:4881 (openSignInEmailDialog — two chained single-field KeyboardDialogs, the shape both other remotes need)`

Unchanged. roku HAS it: components/screens/ProfileGate.brs:1271 `{ kind: "signin", label: "Sign in here", badge: "" }`; components/MainScene.brs:4753 routes it to `openSignInEmailDialog()`; components/MainScene.brs:4881 `sub openSignInEmailDialog()`; also reachable at :495 and :1127. web HAS it: /Users/markususche/Desktop/blazing-web/profile.js:2081 `const result = await request('/accounts/login', {` and :2135 `const result = await request('/accounts/register', {`. firetv MISSING: `rg -n "/accounts/" client/app/src/main/java` returns nothing (the only "Sign in" string in the tree is BooksClient.kt:541, an unrelated 401 message); ProfileGateActivity.kt:614-619 shows the pair wall's only two doors, `tvButton(getString(R.string.gate_retry))` and `tvButton(getString(R.string.pair_type_code)) { showCodeEntry(deviceId) }`. tvos MISSING: `rg -n "accounts/login|accounts/register|/accounts/|Sign in" Sources/` returns ZERO hits; Sources/PairView.swift:82 `Text("Have an invite code?")`, :84-86 `FleetGateAction(title: "Type a code", ...)` and :88-90 `FleetGateAction(title: "Try again", ...)` are the only alternatives to the QR.

### M75 — The English-translation toggle on manga pages exists only on the Roku

- **Missing on:** Fire TV, Web, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/MangaReaderScreen.brs:119-127 and :319-328`

Unchanged. ROKU has it: components/screens/MangaReaderScreen.brs:33 `m.translate = false`; :119-127 the page-URL builder ending `if m.translate then return base + "&tl=1"`; :319-321 `if key = "info"` / `m.translate = not m.translate` with the notice 'English: ON  ·  first page is slow, then instant'. The server honours it: /Users/markususche/Desktop/blazing-addon/server.js:1885 `const wantTranslation = String(req.query.tl || '').trim().toLowerCase()` (and :1880-1884 explains `tl=<lang>` renders one page in another language). NOBODY ELSE: `rg -n "tl=1"` returns zero hits in /Users/markususche/Desktop/firetv/client/app/src, zero in /Users/markususche/Desktop/blazing-web/*.js + index.html, zero in /Users/markususche/Desktop/blazing-tvos/Sources + Shared. The only 'translat' hits outside Roku are unrelated: firetv none in MangaReaderActivity.kt, web index.html:309 is a node `translation=[0,0]`, tvos Sources/EmbyLibraryView.swift:150 is a prose word. Fire TV's MangaReaderActivity.kt is 364 lines with no key handler beyond page turns.

### M76 — The Fire TV manga chapter list has chapter-range chunk buttons that no other client offers

- **Missing on:** Roku, Web, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/MangaActivity.kt:401 and :703`

Unchanged. FIRETV has it: client/app/src/main/java/com/nyctailblazers/blazingtv/MangaActivity.kt:703 `private const val CHAPTERS_PER_PAGE = 100`; :401 `val chunks = chapters.chunked(CHAPTERS_PER_PAGE)`; :618 the same for comic issues. ROKU does not: components/screens/MangaChaptersScreen.xml:89-93 is one `<MarkupList id="chapters" ... numRows="7"/>` with no button strip, and MangaChaptersScreen.brs:241-243 (`range = "chapter " + first` / `"chapters " + first + "-" + last`) is only a per-ROW label, not a jump control; the only jump in the file is `m.list.jumpToItem = m.firstReadable` at :136 and :197. WEB does not: /Users/markususche/Desktop/blazing-web/manga.js:325 fetches `/chapters?limit=2000` and :348 `dlist.replaceChildren(...list.map((c) => chapterRow(manga, c)))` renders every row into the one dialog; no 'chunk' or 'PER_PAGE' token anywhere in manga.js. TVOS does not: Sources/ReadingView.swift:354-359 `ScrollView { LazyVStack { ForEach(model.chapters) { ... } } }` over the whole list, header at :339 prints `"\(model.chapters.count) chapters"`; grep for chunk|chaptersPerPage|CHAPTERS_PER|stride in ReadingView.swift returns nothing.

### M77 — The Fire TV manga reader has no continuous-scroll mode, and the web opens on a different default

- **Missing on:** Fire TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/MangaReaderScreen.brs:308-317 (key binding) and /Users/markususche/Desktop/blazing-tvos/Sources/ReadingView.swift:387-388 (strip renderer)`

Fire TV still paged-only. /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/MangaReaderActivity.kt is 364 lines total; :18 `import androidx.viewpager2.widget.ViewPager2`, :38 `private lateinit var viewPager: ViewPager2`, :85 the OnPageChangeCallback, :275 `MangaAdapter : RecyclerView.Adapter` — grep for strip|continuous|MEDIA_PLAY_PAUSE|mode in that file returns nothing but the ViewPager/RecyclerView hits. The other three have it: roku components/screens/MangaReaderScreen.brs:308-317 `if key = "play"` / `setReadingMode(not m.scroll)` with the 'Scrolling · hold DOWN to read on' notice; tvos Sources/ReadingView.swift:21 `case paged, continuous`, :387-388 `} else if model.flow == .continuous { ContinuousStrip(pages: model.pages, step: $model.stripStep) }`, :425 `.onPlayPauseCommand`; web /Users/markususche/Desktop/blazing-web/manga.js:475 `const readingMode = () => (window.BlazingReadingMode?.get('strip') === 'strip' ? 'strip' : 'paged')`. The default divergence is also unchanged and is still a Markus decision, not a code fix: manga.js:469-474 documents 'MANGA OPENS ON THE STRIP' on purpose, while tv-comics-reader.js:55 `window.BlazingReadingMode?.get()` calls get() bare and stays paged, and tvOS Sources/ReadingView.swift:84 defaults `?? .paged`.

### M78 — The Fire TV search screen carries no top nav bar and no drawer

- **Missing on:** Fire TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/HomeActivity.kt:50-51 (call sites) and :77 / :155 (definitions to lift into a base activity)`

Unchanged. `rg -n "installNavBar|installDrawer"` over /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/ returns exactly four code hits, all in one file: HomeActivity.kt:50 `installNavBar()`, HomeActivity.kt:51 `installDrawer()`, and the two definitions at HomeActivity.kt:77 and :155 (the fifth hit, HomeFragment.kt:568, is a comment). SearchActivity.kt:29-60 is still the bare shell: :30 `val root = android.widget.FrameLayout(this)`, :34 a second FrameLayout as the fragment container, :40-50 the AI Search ToggleButton, :58-60 the fragment transaction — no bar, no drawer. The chip target already exists: NavBarView.kt:148 `private fun entries(showLibrary: Boolean)` and :182 `fun build(showLibrary: Boolean)`. The other three keep nav on Search: roku components/MainScene.brs:7948 `full = ["player","mangaReader","comicsReader","bookReader","gate","pinpad","invitecodepad","party","streams"]` — 'search' is not in the hide list, and :7820 pushes 'search' into m.navOrder; tvos Sources/RootView.swift:1244 `NavBar(current: $shell.section)` mounted in the root stack; web /Users/markususche/Desktop/blazing-web/index.html:133 the header chips sit outside the view sections and app.js:2004 only toggles a view's `hidden`.

### M79 — The Home hero disappears entirely when nothing is part-watched (Apple TV, Fire TV)

- **Missing on:** Fire TV, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/HomeScreen.brs:695-724 (PromoteHeroRow)`

Note the audit's own header line is inverted — its 'Has it: Fire TV, Apple TV' contradicts its body and its fix note ('Apple TV and Fire TV should change'); the body and the code agree, so I recheck against the body. Unchanged on both. TVOS: /Users/markususche/Desktop/blazing-tvos/Sources/HomeView.swift:847-849 `if continueItems.isEmpty { Color.clear.frame(height: 128) }` and :866-867 `if !continueItems.isEmpty { ContinueHero(items: continueItems, cardHero: cardHero) }` — an empty Continue Watching yields a 128pt spacer and no hero; there is no fallback subject taken from the bands. FIRETV: client/app/src/main/java/com/nyctailblazers/blazingtv/HomeFragment.kt:307-311 `if (shown.isNotEmpty()) { cwAdapter.setItems(shown, null) } else { rowsAdapter.remove(cwRow) }` — the HeroListRow is deleted, and :314 `val (heroRow, heroAdapter) = addLoadingRow("Featured")` is an ordinary ListRow (addHeroRow/HeroListRow is only built at :924-934 for the cw row), with :333 `if (visible.isEmpty()) rowsAdapter.remove(heroRow)`. Donors intact: roku components/screens/HomeScreen.brs:695 `function PromoteHeroRow(rows as Object) as Object`; web /Users/markususche/Desktop/blazing-web/app.js:759 `function claimHeroRow(section, metas)` called from six sites (:2617, :2638, :2706, :5362, :6011).

### M80 — The Library chip is profile-gated on Roku and Fire TV, ungated on Apple TV and web

- **Missing on:** Apple TV, Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/NavBarView.kt:164 with HomeActivity.kt:83-84`

Gate still missing on two clients; only the label half is fixed. TVOS: Sources/NavBar.swift:130-131 `static var barOrder: [Section] { [.movies, .shows, .anime, .roadmaps, .library, .live, .youtube, .games, .adult, .search, .settings] }` — .library unconditional; :142-144 drawerOrder likewise contains .library; :226-227 `private var visibleSections: [Section] { Section.barOrder.filter { $0 != .adult || profileSession.maySeeAdult } }` — the adult filter is still the only one; Sources/Drawer.swift:166 `Section.drawerOrder.filter { $0 != .adult || profileSession.maySeeAdult }` — same. The LABEL sub-issue IS closed: Sources/NavBar.swift:159 `case .library: return "Library"` (the audit's 'Watchlist' cite is stale; 'My Library' is now .debrid at :160). WEB: /Users/markususche/Desktop/blazing-web/index.html:133 `<button type="button" data-view="library">Library</button>` and a second copy at index.html:186, neither gated; app.js:2004 `libraryView.hidden = route !== 'library'` is the only thing touching it. profile.js has no 'library' token at all (its only hits are prose/error strings at :1863, :3799, :3839-3865), so the gate signal it already computes is not wired to the chip. Donors intact: roku components/MainScene.brs:7805-7812 `if manga` ... `m.navOrder.Push("mylists") : labels.Push("Library")` with mangaAllowedNow() feeding `manga` at :1839, and the drawer row at :1868 `if manga then addDrawerRow(c, "My Library", ...)`; firetv NavBarView.kt:164 `if (showLibrary) dest(Dest.LIBRARY)` fed by HomeActivity.kt:83-84 `val grownUp = ProfileGateRules.grownUp(ProfileClient.activeProfile)` / `bar.build(showLibrary = grownUp)`, drawer at HomeActivity.kt:158-159 and DrawerView.kt:140.

### M81 — The PIN lockout is reported four different ways

- **Missing on:** Apple TV, Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/PinActivity.kt:228-232`

roku HAS the countdown: "/Users/markususche/Desktop/roku channels/components/screens/PinPad.brs":146-150 `m.lockTimer = CreateObject("roSGNode", "Timer")` / `m.lockTimer.duration = 1` / `m.lockTimer.repeat = true` / ObserveField "onCountdownTick"; :181 `setStatus("Too many wrong tries. Try again in " + Stri(mins).Trim() + ":" + pad + ...)`; :163 fallback "Try again in 15 minutes." | firetv HAS it: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/PinActivity.kt:228 `lockoutTimer = object : CountDownTimer(remaining, 1000L) {`; :232 `statusText.text = "Too many attempts — try again in ${secs / 60}:${(secs % 60).toString().padStart(2, '0')}"` | tvos STILL MISSING: /Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:2762-2763 `case .locked:` / `status = "PIN is temporarily locked. Try again later."` — the `until:` payload is bound but discarded. Note the audit's doubt is resolved: the deadline DOES survive the decode — Sources/FleetAPI.swift:583 `case locked(until: String?)` and :1175 `if decoded.lockedUntil != nil { return .locked(until: decoded.lockedUntil) }`, plus :986 `let lockedUntil: String?`. So the timer can be built with no API change. grep for Timer.publish/countdown/timeRemaining in Sources/RootView.swift = 0 hits. | web STILL MISSING: /Users/markususche/Desktop/blazing-web/profile.js:1502-1505 and again at :3136-3140 `const date = new Date(lockedUntil); const when = ... date.toLocaleString(); setStatus(`This profile is locked until ${when}.`, 'error');` — absolute wall clock, no ticking label.

### M82 — The Read-manga action has four different gates and four different labels

- **Missing on:** Roku
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:1246-1260`

roku STILL UNGATED AT BUILD TIME: "/Users/markususche/Desktop/roku channels/components/screens/DetailsScreen.brs":1435-1436 `if spec.list = "watchlist" and IsAnimeSeries()` / `actions.Push({ kind: "manga", label: "Read episode manga" })` — the only tests are the list slot and the id prefix; the comment at :1434 admits it ("It is shown only for anime ids and MainScene applies the profile gate"). DetailsScreen.brs has zero `allowManga` hits, so the screen has no profile fact to gate on. The refusal is still on press only: /Users/markususche/Desktop/roku channels/components/MainScene.brs:6530-6533 `if not mangaAllowedNow()` / `showToast("Manga is hidden by this profile's rating limit.")`. IsAnimeSeries() at DetailsScreen.brs:1442-1450 still tests id prefixes only. | The other three still hide it: firetv DetailActivity.kt:1246 `if (!ProfileGateRules.grownUp(ProfileClient.activeProfile)) return` and :1252-1254 `if (!animeSourced) return`; web /Users/markususche/Desktop/blazing-web/app.js:3236 `if (meta.isAnime && !state.profileIsKids && ratingAllowed('mature') && window.BlazingManga) {`; tvos /Users/markususche/Desktop/blazing-tvos/Sources/DetailSheet.swift:512 `private var mangaAllowed: Bool { profileSession.mayRead }` and :768 `if model.selectedEpisode != nil && mangaAllowed {`. | FOUR LABELS STILL DIVERGE: roku DetailsScreen.brs:1436 "Read episode manga"; firetv DetailActivity.kt:1260 `text = "Read the manga"`; web app.js:3241 `button.textContent = 'Read manga';`; tvos DetailSheet.swift:771-772 `? "Finding Manga…" : "Read \(model.selectedEpisode?.code ?? "Manga")"`. | The episode rule also still splits: tvos hides (DetailSheet.swift:768), web toasts (app.js:3244 `if (!context) return showToast('Choose an episode first.', 'error');`), roku toasts (MainScene.brs:6524 "Choose an episode first." and :6536-6538).

### M83 — The Watched list feeds a Home shelf on Roku only

- **Missing on:** Fire TV, Apple TV, Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/MainScene.brs:2423-2536`

roku HAS it: "/Users/markususche/Desktop/roku channels/components/MainScene.brs":2420 comment `' Build the real "Because you watched ..." shelf`; :2423 `sub runHomePersonalRow(cw as Object)`; :2437-2438 `if id <> "" then blocked["id:" + LCase(id)] = true` / `if name <> "" then blocked["name:" + LCase(name)] = true`; :2450 and :2473 `{ slug: "because", homeTitle: "Because you watched", items: [] }`; :2536 `homeTitle: "Because you watched " + Nz4(m.homePersonalTitle)`; :2499 `blocked = m.homePersonalBlocked`; call sites at :2347, :3640, :8138, :9017, :9143. (Nuance vs the audit text: the live builder seeds from ContinueRow() and blocks the Continue-Watching ids, not from a fleetLists.watched array — the shelf exists, its input is CW.) | firetv MISSING: `grep -rin 'because you watched' /Users/markususche/Desktop/firetv/client/app/src/main/java/` = 0 hits; no equivalent shelf in HomeDiscovery.kt or HomeFragment.kt (grep for 'More like|Recommended for|watchedIds|lists.watched' = 0 hits). | web MISSING: `grep -rin 'because you watched' *.js *.html` in /Users/markususche/Desktop/blazing-web = 0 hits. | tvos MISSING: `grep -rin 'because you watched' Sources/ TopShelf/` in /Users/markususche/Desktop/blazing-tvos = 0 hits; Sources/HomeView.swift has Continue Watching only (:1094 `"CONTINUE WATCHING  ·  \(percent)% watched"`).

### M84 — The browser PIN pad does not auto-submit; it has a Verify button the TVs do not have

- **Missing on:** Web
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:2719-2722`

web STILL MISSING: /Users/markususche/Desktop/blazing-web/profile.js:2604-2608 `function addDigit(digit) { if (state.busy || !/^\d$/.test(digit) || state.pinDigits.length >= pinLength()) return; state.pinDigits.push(digit); renderPin(); }` — pushes and renders, nothing else. The Verify button is still the only pad route through: :3640-3643 `ui.verify = element('button', 'bp-verify', 'Verify');` / `ui.verify.disabled = true;`, wired at :3753 `ui.verify.addEventListener('click', verifyPin);`, enabled only at full length at :726 and :1556 `ui.verify.disabled = state.busy || state.pinDigits.length !== pinLength();`. The audit's nuance still holds — a physical Enter submits at :3783 `} else if (event.key === 'Enter' && state.pinDigits.length === pinLength()) {`. pinLength() is still variable at :2602 `const pinLength = () => (state.ownerMode ? OWNER_PIN_LENGTH : 4);`, so the auto-submit must key off it. | All three TVs still auto-submit: roku "/Users/markususche/Desktop/roku channels/components/screens/PinPad.brs":214 `if Len(m.digits) = PadLength() then submit()`; firetv PinActivity.kt:150 `if (entered.length == PIN_LEN) { if (setMode) submitSet() else submit() }`; tvos /Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:2719-2722 `.onChange(of: entered) { _, pin in guard pin.count == 4, !busy else { return }; Task { await submit(pin) } }`.

### M85 — The floor below which a viewing is not recorded differs, and two clients have none

- **Missing on:** Roku, Web
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/PlayerActivity.kt:1023`

firetv HAS the floor: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/PlayerActivity.kt:1023 `if (pos < 10_000) return`. | tvos HAS it: /Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:876 `guard let origin = item.origin, position >= 10 else { return }`. | roku STILL HAS NONE: "/Users/markususche/Desktop/roku channels/components/MainScene.brs":993-994 `if s.action = "start" or s.action = "stop" or s.action = "pause"` / `if m.lastTitle <> invalid and s.progress < 95` then SaveContinue(entry) at :1015 — the only test is the 95% ceiling; `entry.positionSecs` is written at :1009 but never compared to anything. The audit's warning still applies verbatim: the comment at :989-991 records that the old `>2%` rule was removed on purpose ("which on a two-hour film is about two and a half minutes — so short viewings never appeared at all"); a 10-second absolute floor is not that rule. | web STILL HAS NONE: /Users/markususche/Desktop/blazing-web/app.js:5385-5396 `function startSync(meta) { stopSync(); syncInterval = setInterval(() => { if (!video.duration || video.paused) return; ... POST /api/sync/progress ... }, 30000); }` — guards only on duration and paused; `video.currentTime` is posted unfiltered.

### M86 — The four clients search four different sets of back ends, so one query returns four different result sets

- **Missing on:** Apple TV, Fire TV, Web, Roku
- **Fix size:** large
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/AddonClient.kt (searchEnriched, called at SearchActivity.kt:268) — closest shape today; tvOS is furthest behind at /Users/markususche/Desktop/blazing-tvos/Sources/Sections.swift:455-457`

Still four different fan-outs, and no unified fleet route was ever built. | roku (widest): "/Users/markususche/Desktop/roku channels/components/tasks/AddonTask.brs":648 `for each s in TmdbSearch(q)`, :654 `for each s in MbpSearch(q)`, :659 `for each s in AnimeSearch(q)`, :666 `for each s in MwpSearch(q)` on top of the Cinemeta pair. | firetv: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:268 `AddonClient.searchEnriched("movie", query)`, :271 `AddonClient.searchEnriched("series", query)`, :274 `AddonClient.mrWorldPremiereSearch(` — three sources. | web: /Users/markususche/Desktop/blazing-web/app.js:1646 `async function searchSiteCatalogs(query) {`, :1690 `searchSiteCatalogs(query),` inside the fan-out array, :1614 `const SEARCH_CATALOG_LIMIT = 6;`, :1753 the Emby leg, :1357 comment `/search/movie and /search/series each fan out to Cinemeta, TMDB and the 36...`. | tvos STILL THE NARROWEST: /Users/markususche/Desktop/blazing-tvos/Sources/Sections.swift:455-457 `async let movies = BlazingAPI.search(type: "movie", query: q)` / `async let series = BlazingAPI.search(type: "series", query: q)` / `async let mrWorldPremiere = BlazingAPI.mrWorldPremiereSearch(query: q)` — and those resolve to Cinemeta only: Sources/BlazingAPI.swift:148-153 `URL(string: "\(metaBase)/catalog/\(type)/top/search=\(q).json")` and :159-164 `"\(base)/catalog/movie/mwp-search/search=\(q).json"`. No TMDB, no MovieBox, no anime, no Emby, no fleet index. | The server-side fix is NOT in place: /Users/markususche/Desktop/blazing-addon/server.js exposes only :1267 `app.get('/search/book'`, :1405 `app.get('/search/game'` and :3872 `app.post('/api/ai-search'` — there is no single merged title-search route for the clients to converge on.

### M87 — The genre facet rail exists only in the browser

- **Missing on:** Roku, Fire TV, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-web/app.js:1499-1530`

web HAS it: /Users/markususche/Desktop/blazing-web/app.js:1499-1501 `function renderSearchFacets() { ... const genres = [...new Set(searchAllCards.flatMap((c) => (c.genres || []).map(canonicalGenre)))].sort();`; :1526-1527 `? searchAllCards.filter((c) => (c.genres || []).map(canonicalGenre).includes(searchActiveGenre)) : searchAllCards;` — filters the already-fetched array, no re-query; :1493 `const GENRE_ALIASES = { 'science fiction': 'Sci-Fi', 'sci fi': 'Sci-Fi' };`; :1765-1766 `searchAllCards = cards; renderSearchFacets();`; markup at index.html:401 `<nav id="search-facets" class="search-facets" aria-label="Filter by genre" hidden></nav>`. | roku MISSING: "/Users/markususche/Desktop/roku channels/components/screens/SearchScreen.xml":135-152 is still a bare `<RowList ... </RowList>` with no filter control; `grep -n genre components/screens/SearchScreen.brs` = 0 hits. | firetv MISSING as a result filter: SearchActivity.kt:91-93 the comment `These are Cinemeta's own genre facets ... fetched as /catalog/{type}/top/genre={g}.json` and :166-179 `for ((type, genre) in BROWSE) { ... AddonClient.catalog(Catalogs.CINEMETA, type, "top/genre=$genre") ... }` — that is the EMPTY-QUERY browse shelf set (:156 `/** The empty-query screen: browsable genre rows ... */`), not a narrowing control over a result set. | tvos MISSING: /Users/markususche/Desktop/blazing-tvos/Sources/Sections.swift:406-418 `LazyVGrid(columns: columns, alignment: .leading, spacing: 26) { ForEach(items) { item in PosterTile(...) } }` renders `items` straight with no filter; `grep -in genre Sources/Sections.swift` = 0 hits.

### M88 — The gesture and the threshold that start an AI search are different on all three clients that have one

- **Missing on:** Web, Roku, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:44-51,76,205`

Still three different gestures and web still has none. | roku hidden gesture UNCHANGED: "/Users/markususche/Desktop/roku channels/components/screens/SearchScreen.brs":352-353 `if key = "options"` / `if not m.results.isInFocusChain() and words.Count() > 8` then `m.top.chosen = { action: "aisearch", query: QueryText() }` at :356 — nine words minimum, no visible control; the only hint is :212-213 `if words.Count() > 8` / `m.status.text = line + Chr(10) + "Describe a plot instead? Press * to AI Search"`, which appears only after results exist. | firetv visible toggle UNCHANGED: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:44 `isChecked = false`, :50-51 `toggle.setOnCheckedChangeListener { _, isChecked -> frag.aiSearchEnabled = isChecked }`, :76 `private val AI_MIN_QUERY = 5`, :205 `if (aiSearchEnabled && query.length >= AI_MIN_QUERY) {`. | tvos UNCHANGED and still swallows the short query: /Users/markususche/Desktop/blazing-tvos/Sources/Sections.swift:389 `TextField("AI Search", text: $aiQuery)`, :396 `.onSubmit { Task { await runAI() } }`, :485 `guard !q.isEmpty, !running else { return }` is the only gate; :487 `let raw = (try? await FleetAPI.aiSearch(query: q)) ?? []` swallows the 400 and :493 renders it as `"Nothing matched ..."`. | server floor UNCHANGED: /Users/markususche/Desktop/blazing-addon/server.js:3874 `if (!query || query.length < 5) return res.status(400).json({ error: 'query too short' })`. | web STILL HAS NO AI CONTROL: /Users/markususche/Desktop/blazing-web/index.html:387-404 is the whole search view (form, mic, input, chip row, status, facets, results) with no AI element; `grep -n 'ai-search|aiSearch|AI Search|ai_search' app.js index.html` = 0 hits.

### M92 — The saved-list screen is profile-gated on Fire TV and completely ungated on the web

- **Missing on:** Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/ListsActivity.kt:42 (route refusal) and HomeActivity.kt:83-84 (chip hidden)`

Fire TV still gates: client/app/src/main/java/com/nyctailblazers/blazingtv/ListsActivity.kt:42 "if (requireGrownUpOrExit(this, \"Library\")) return"; chip is conditional at HomeActivity.kt:83-84 "val grownUp = ProfileGateRules.grownUp(ProfileClient.activeProfile)" / "bar.build(showLibrary = grownUp)" and the drawer at HomeActivity.kt:158-159 "d.build(showReadingAndLibrary = grownUp)". Web is still ungated at HEAD 59fceab: index.html:133 and index.html:186 are both unconditional <button type="button" data-view="library">Library</button>; app.js:2004 "libraryView.hidden = route !== 'library';" and app.js:2108 "if (route === 'library') renderLibrary();" — showRoute() (app.js:1967) contains no cap check for this route. updateNavigation() (app.js:1179-1192) only toggles .active/aria-current and never hides a chip. grep over app.js/profile.js finds no grownUp/ProfileGateRules equivalent; the only cap state that exists is app.js:713 "profileCap: null," and app.js:721 "profileIsKids: false," — both computed (app.js:6202-6203, 6226-6227) but never consulted for the library chip or route. Filtering does happen INSIDE the screen only: app.js:1833 ".filter((meta) => meta && ratingAllowed(meta.contentRating));" inside renderLibrary (app.js:1793).

### M93 — The tvOS second manga reader has no way out of it

- **Missing on:** Apple TV
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/ReadingView.swift:442`

Still no exit on Apple TV. /Users/markususche/Desktop/blazing-tvos/Sources/MangaReaderView.swift is 66 lines and its only key handler is :39-45 ".onMoveCommand { direction in … case .right … case .left … }" — there is no .onExitCommand anywhere in the file (grep 'onExitCommand' over Sources/ lists 25 hits and none is MangaReaderView.swift). The view is LIVE, not dead code: Sources/RootView.swift:1421-1423 ".fullScreenCover(item: $shell.readingBook) { target in … MangaReaderView(id: target.id, name: target.name)", reached from Sources/RootView.swift:187 "readingBook = DetailTarget(id: id, type: type, name: title, poster: poster)" inside playBest()'s "if type == \"book\"" branch (:180). The RootView Menu backstop does NOT cover it: Sources/RootView.swift:1388 ".onExitCommand(perform: shell.topOverlay == nil ? nil : …)" and topOverlay (Sources/RootView.swift:901-911) lists only preparing/party/sidecar/drawer/detail/filter — readingBook is not one of them, so the handler is nil while the reader is up. Other clients still exit fine: roku components/screens/MangaReader.brs:85-107 onKeyEvent handles only left/right and returns handled=false, so back falls through to the scene; web index.html:729 '<button type="button" class="comic-close" aria-label="Close the reader">Close</button>' plus manga.js:629 "} else if (key === 'Escape' || key === 'Backspace') {"; firetv MangaReaderActivity.kt exits via the activity stack. tvOS's own full reader still has three exits: Sources/ReadingView.swift:372, :442 ".onExitCommand { model.reading = false }", :1365 ".onExitCommand { dismiss() }".

### M94 — The wait before playback: Roku and Apple TV take over the screen and can be cancelled, Fire TV and the web leave you on the detail page

- **Missing on:** Fire TV, Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/Preparing.swift:21-74 (plate + title + moving line + focusable Cancel)`

Unchanged on both blamed clients. ROKU still takes over: components/MainScene.brs:3406 "m.player.preparing = { on: true, title: title, note: \"Finding the best stream\" }" (cleared at :8333 "m.player.preparing = { on: false }"), drawn by components/screens/PlayerScreen.xml:144 <Label id="prepTitle" …> and :150 <Label id="prepNote" …>. APPLE TV still takes over with a cancel: Sources/Preparing.swift:23 "let onCancel: () -> Void", :45 Text("Finding the best copy…"), :52 "Button(action: onCancel) {" … :55 Text("Cancel"), :73 ".onExitCommand(perform: onCancel)". FIRE TV still a line of text on the page: client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:1989 "status.text = \"Finding streams…\"" inside showEpisodeStreams (:1983) before "lifecycleScope.launch {" at :1992; same at DetailActivity.kt:262 (initial status TextView) and EpisodesActivity.kt:235 — no overlay view and no cancel control anywhere in those files. WEB still a line of text on the open dialog: app.js:4345 "detailStatus.textContent = 'Checking direct streams…';" inside playSelected() (app.js:4307), with only an invisible generation counter for cancellation (app.js:4306 "let playRequest = 0;", :4310 "const request = ++playRequest;"); grep for 'preparing'/'Preparing'/'prep-' over app.js, index.html and styles.css returns nothing.

### M96 — The web never writes Continue Watching to the fleet — it only reads it

- **Missing on:** Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/Progress.swift:80-109 (payload shape + PUT); /Users/markususche/Desktop/blazing-web/app.js:5300-5307 already builds the same URL for the read`

Still true at HEAD 59fceab. The only progress write in the browser is a POST to the ADD-ON: app.js:5385-5396 "function startSync(meta) { … fetch(`${API_BASE}/api/sync/progress`, { method: 'POST', … body: JSON.stringify({ imdbId: meta.id, position: video.currentTime, duration: video.duration, profileId }) }) … }" with app.js:4 "const API_BASE = 'https://addon.lyreosai.com';". grep for "method: 'PUT'" across app.js, profile.js and every other web .js finds exactly one hit and it is unrelated (settings.js:586, the add-on secret). The READ half already talks to the fleet, which is the asymmetry the item names: app.js:5300-5307 "function fleetProgressEntries(profileId) { const credentials = storedDeviceCredentials(); … `${FLEET_BASE}/profiles/${encodeURIComponent(profileId)}/progress` … { headers: { 'X-Device-Token': credentials.token } } }" used by loadContinueWatching at app.js:5321 "let entries = await fleetProgressEntries(profileId).catch(() => []);" with the add-on as fallback at :5322. Both halves of the fix still sit in the same file: storedDeviceCredentials at app.js:5176. The three TVs all write: roku components/MainScene.brs:1031 "runProfileTask({ kind: \"progressput\", id: ProfileActiveId(), items: ContinueForFleet() })" (handled at components/tasks/ProfileTask.brs:35); firetv Progress.kt:392 and :413 "ProfileClient.putProgress(ctx, profileId, fleetPayload(...))"; tvos Sources/Progress.swift:91 "req.httpMethod = \"PUT\"" against Sources/Progress.swift:176 "FleetAPI.base + \"/profiles/\\(enc)/progress\"". NOTE: this is the same single defect as M100-adjacent item M97 below — one change to startSync closes the write half of both.

### M97 — The web player writes progress to a different backend than the three TV clients

- **Missing on:** Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/Progress.swift:43-109 (read-merge-write, PUT to /profiles/:id/progress)`

Both halves still wrong on the web, and this is the same root defect as M96 with the resume-read added. WRITE: app.js:5391 "fetch(`${API_BASE}/api/sync/progress`, { method: 'POST', …" inside startSync (app.js:5385), API_BASE = 'https://addon.lyreosai.com' (app.js:4) — never the fleet. RESUME READ: app.js:4984-4996, started right after "startSync({ id: state.selected?.id });" at :4984 — "fetch(`${API_BASE}/api/sync/progress/${state.selected.id}?profileId=${profileId}`)" at :4987, i.e. the add-on, so the Resume button (:4992-4995) reads a store the TVs never write. Meanwhile the Continue Watching row reads the FLEET first and the add-on only as fallback (app.js:5321-5322), so the browser's own writes need not come back to it — exactly the split the item describes. FLEET_BASE exists at app.js:5 and storedDeviceCredentials() at app.js:5176, so nothing is missing but the repoint. TV donors: tvos Sources/Progress.swift:91 "req.httpMethod = \"PUT\"" with the read-merge at :43-78 and the URL builder at :174-177; roku components/MainScene.brs:1031 progressput; firetv Progress.kt:392 ProfileClient.putProgress (implementation at ProfileClient.kt:599). Overlap warning for whoever fixes: M96 and M97 are one code change at startSync plus, for M97 only, the second change at app.js:4987.

### M98 — Theme choice exists only on Fire TV

- **Missing on:** Roku, Web, Apple TV
- **Fix size:** large
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/ThemeManager.kt:15-51 (palette list + currentId fallback) and MainActivity.kt:308-331 (picker + recreate)`

Unchanged on all three. FIRE TV still the only donor: client/app/src/main/java/com/nyctailblazers/blazingtv/MainActivity.kt:76 "root.addView(buildThemeForm(), fullWidthParams())", :308 "private fun buildThemeForm(): LinearLayout = LinearLayout(this).apply {", :312 text = "Theme", :316 "val current = ThemeManager.currentId(this@MainActivity)", :319 "ThemeManager.palettes.forEach { palette ->", :323 "if (ThemeManager.set(this@MainActivity, palette.id)) recreate()"; ThemeManager.kt:15 "val palettes = listOf(" holds FOUR — "blazing" (:33), "ember" (:38), "ocean" (:43), "violet" (:48). ROKU: no theme row — components/screens/SettingsScreen.brs builds its playback rows at :91-107 (Minimum quality / Minimum seeders / Maximum size / Instant sources only / Skip HEVC / Skip AV1 / English audio only) and :109-110 (Play next episode automatically) with no palette row; every "theme" hit in that file is a ThemeColor() call (:239,:242,:247,:250,:253,:292,:296); grep for themeId/ThemeSet/setTheme across source/ and components/ returns no setter. WEB: app.js:2815 "if (uiConfig.theme) document.documentElement.setAttribute('data-theme', uiConfig.theme);" is the only writer and it comes from the server; grep over settings.js and index.html for a theme select/picker/button returns nothing. APPLE TV: Sources/Models.swift:13 "let theme: String?" decoded from the server home-config at :26 "theme = try? c.decode(String.self, forKey: .theme)"; no palette list and no picker — grep for palette/themeId/ThemeManager across Sources/ finds only Models.swift, BlazingAPI.swift:33 (the route comment) and a DetailSheet comment.

### M102 — Voice search is missing on tvOS only

- **Missing on:** Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:124-135 (permission-then-hand-back); web app.js:1445-1460 for the hide-when-unsupported rule`

tvos (STILL MISSING): /Users/markususche/Desktop/blazing-tvos/Sources/Sections.swift:379-396 — SearchView's only inputs are two plain `TextField("Title", text: $query)` and `TextField("AI Search", text: $aiQuery)`. A grep of Sources/ and Shared/ for speech|dictation|microphone|SFSpeech|Siri returns only three unrelated comment hits: Sources/TrackChoice.swift:111 ("voice over in russian"), Sources/VLCControls.swift:17 and Sources/PlayerView.swift:176 (Siri-remote gesture notes). No Speech framework import, no dictation control. roku (HAS): components/screens/SearchScreen.brs:59 `kb.voiceEnabled = true`, :60 `kb.voiceEntryType = "alphanumeric"`. firetv (HAS): SearchActivity.kt:124-125 `checkSelfPermission(... RECORD_AUDIO)` then `requestPermissions(arrayOf(Manifest.permission.RECORD_AUDIO), REQ_MIC)`, handed back at :135 `setSpeechRecognitionCallback(null)`. web (HAS): index.html:389 `<button type="button" id="search-mic" class="search-mic" aria-label="Search by voice" hidden>`, wired at app.js:1445-1450 `function bindSearchMic()` → `window.SpeechRecognition || window.webkitSpeechRecognition`, `mic.hidden = false` only when the constructor exists.

### M104 — Web episode rows carry no thumbnail, no synopsis and no air date

- **Missing on:** Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/DetailSheet.swift:1298-1330 (EpisodeTileFace — thumbnail, code+aired, name, clamped overview in that order)`

web (STILL MISSING): /Users/markususche/Desktop/blazing-web/app.js:3187-3199 — the row is still `const button = el('button', 'episode-chip')` with only `el('span','episode-chip-number')` = `E${episode.episode}` and `el('span','episode-chip-title')`; the only other surface is the `<select>` at app.js:3202-3208 printing `S{n} E{n} · title`. CORRECTION to the audit's fix note: the data is NOT already in the episode objects — app.js:441-464 `function safeEpisodes(raw)` returns exactly `{id, season, episode, absoluteEpisode, title}` (:456-462) and drops thumbnail, overview and released on the floor, so the fix needs those three fields added to safeEpisodes first. roku (HAS): components/screens/DetailsScreen.xml:475 `id="episodes"`, :477 `itemComponentName="EpisodeCard"`. firetv (HAS): DetailActivity.kt:1947 `ImageLoader.into(ep.thumbnail, thumb)`, :1956-1957 `if (!ep.overview.isNullOrBlank()) ... text = ep.overview`. tvos (HAS, the model to copy): Sources/DetailSheet.swift:1298 `private struct EpisodeTileFace`, :1306 `if let t = episode.thumbnail, let u = URL(string: t)`, :1324-1325 `if !episode.aired.isEmpty { Text(episode.aired) }`.

### M105 — tvOS auto-plays a film the moment the detail sheet opens; the other three deliberately removed that

- **Missing on:** Apple TV
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/DetailsScreen.brs:1208-1235 (auto-start deleted, episode-select path kept); /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:1570-1579`

tvos (STILL AUTO-PLAYS — line numbers moved from the audit's 551-553 to 641-643): /Users/markususche/Desktop/blazing-tvos/Sources/DetailSheet.swift:641-643 inside `.task(id: ratingVerdict)` (:632): `if target.type == "movie", !isSourceOnly, !autoStarted { autoStarted = true; onPlay() }`; the else branch at :644-655 still says the hero trailer runs "ONLY when nothing else is about to take the screen", so a film still never gets a trailer. The block is now gated behind `guard ratingVerdict == .allowed` at :633, so the rating-gate half of the audit's concern IS fixed — the auto-start itself is not. The comment at :610-612 makes the intent explicit: "A FILM STARTS ITSELF". roku (does NOT): components/screens/DetailsScreen.brs:1208 `' NOTHING STARTS ITSELF WHEN A PAGE OPENS`, with only `m.autoPlayEpisode` surviving (:1229 comment, armed at :2223, consumed at :1234-1235). firetv (does NOT): DetailActivity.kt:1570 `// NOTHING STARTS HERE. This used to auto-start the film the moment a ...`. web (does NOT): app.js:1023 `// OPENING A TITLE NEVER STARTS THE FEATURE ANY MORE.`; guarded in CI by blazing-web/detail-autoplay.smoke.mjs:1 `// THE DETAIL SHEET NEVER STARTS THE FILM, AND THE SOURCE LIST IS ONE PAGE.`

### M106 — tvOS is the only client whose source list is not on the detail page

- **Missing on:** Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:1774-1790 (renderSourceChoices, incl. `onMarkDead = { markStreamDead(s) }` at :1784); web app.js:3962ff for the row shape`

tvos (STILL A SIDE PANEL): /Users/markususche/Desktop/blazing-tvos/Sources/DetailSheet.swift:759-760 `SheetButton(title: "Sources", icon: "list.bullet", primary: false, action: onChooseSource)` is the only sources surface on the sheet — a grep of DetailSheet.swift for StreamRow|RankedStream|sidecar returns no inline list. The button still destroys the sheet: Sources/RootView.swift:693-697 `func chooseSourceFromDetail() { guard let d = detail else { return }; detail = nil; chooseSource(...) }`, and the destination is Sources/Sidecar.swift:21-31 "A panel on the RIGHT over a scrim", mounted as a ZStack sibling at RootView.swift:1313-1319. Sources/StreamSourcesView.swift (new, audit item B2) is NOT this — its own header at :2-4 says it is the read-only add-on/aggregator directory, not the ranked streams for a title. roku (HAS inline): components/screens/DetailsScreen.xml:512 `id="sources"` MarkupList inside the scrolling stack, populated by DetailsScreen.brs:1252 `sub buildSources()`. firetv (HAS inline): DetailActivity.kt:1588 `renderSourceChoices(ordered) { play(it) }` (episode path at :2072), builder at :1774. web (HAS inline): index.html:1010 `<div id="detail-streams" class="detail-streams"></div>` inside the dialog, filled at app.js:3962 `const container = $('#detail-streams')`.

### M1 — A focused search result previews its trailer on tvOS and on web, and never on Roku or Fire TV

- **Missing on:** Roku, Fire TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/HomeFragment.kt:200 + CardPresenter.kt:56 (pass the release callback instead of the default); roku: components/screens/HomeScreen.brs:82-83,117-124`

firetv (STILL MISSING): /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:72 `private val cardPresenter = CardPresenter()` — the bare constructor, taking the no-op default documented at CardPresenter.kt:48-49 "Defaulted to a no-op: SearchActivity builds a bare CardPresenter and has no dwell preview at all" (param at :56 `private val onCardRecycled: (View) -> Unit = {}`). DwellPreview is still constructed only at HomeFragment.kt:200 `dwell = DwellPreview(requireActivity(), lifecycleScope, ::addonBase).also { it.attach() }`; a grep of SearchActivity.kt for DwellPreview returns nothing. roku (STILL MISSING): components/screens/SearchScreen.xml:138 `itemComponentName="PosterItem"`; a grep of components/screens/PosterItem.brs and PosterItem.xml for trailer|dwell|preview returns ZERO hits, while components/screens/HomeScreen.brs:82-83 owns `m.trailerTimer` + `onTrailerDue` and :117-124 the whole dwell scrim/anim set. A grep of SearchScreen.brs for dwell|trailer also returns nothing. tvos (HAS): Sources/Sections.swift:404 `FocusedPosterPreview(model: cardHero, seed: searchSeed, shell: shell)` above the results grid, with the comment at :401-403. web (HAS): app.js:2434 `if (!card.closest('.row-hero, .search-results')) return;`, grids built at app.js:3628 and :3825 `el('div', 'search-results')`.

### M2 — Device and codec reporting differs on every client that has it

- **Missing on:** Web, Fire TV, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/SettingsScreen.brs:273-286 (and especially :286, the sentence). Web needs only a caller for caps.js:1307 describe() from settings.js.`

web (STILL SHOWS NOTHING): /Users/markususche/Desktop/blazing-web/caps.js:1307 `function describe(c)` and :1350 `describe: describe` on window.BlazingCaps — a repo-wide grep for `describe(` finds only that definition plus settings.js:180 `function describe(raw)`, which is an unrelated per-stream label helper (see settings.js:202,225). The web Settings screen now exists (settings.js) but has no device/codec section at all: grep of settings.js for ABOUT|About this|codec-report returns nothing but source-FILTER rows (e.g. :419 `Skip HEVC / H.265: ${onOff(f.noHevc)}`), which is the viewer's preference, not the box's capability. roku (HAS, the donor): components/screens/SettingsScreen.brs:273 `' ---- device ----`, :275 model + Roku OS, :277-279 the four codec flags + max height, :286 the plain-English sentence `"This TV cannot decode HEVC, so those streams are filtered out - otherwise the list looks full and plays nothing."` firetv (MODEL ONLY, unchanged): MainActivity.kt:43-45 `"Blazing TV · ${BuildConfig.CHANNEL} · v...\n${Build.MODEL} · Fire OS ${fireOsVersion()}"`; the codec data exists unused at Device.kt:104-112 `private fun decodesAt4k(mime: String)`. tvos (PLAYER SENTENCE ONLY, unchanged): Sources/Sections.swift:614 `Field("Player", "AVPlayer handles HLS, MP4 and MOV. VLCKit handles MKV...")`; DeviceCaps is probed at RootView.swift:144 `DeviceCaps.probeOnce()` and consumed only by StreamRanker — a grep of Sections.swift for DeviceCaps returns zero hits.

### M3 — Every section is capped at a different number

- **Missing on:** Fire TV, Apple TV, Web
- **Fix size:** small
- **Copy from:** `No single donor — the fix is one agreed number per section written once. Nearest to a house standard: /Users/markususche/Desktop/roku channels/components/screens/DetailsScreen.brs:1048 `function CapTo12` (cast 12, studios 12, related 12) with reviews 2 already matched by tvos RichMeta.swift:39 and web app.js:3384.`

The drift is real and unchanged, but the audit's WEB evidence is now stale — web has grown the missing sections. Live numbers: CAST — roku 12 (components/screens/DetailsScreen.brs:1030 `kept = CapTo12(cast)`, fn at :1048), firetv 12 (DetailActivity.kt:850 `rich.cast.take(12)`), tvos 20 (Sources/DetailSheet.swift:865 `ForEach(people.prefix(20))`), web 12 (app.js:3382 `const RICH_CAST_MAX = 12` applied :3471). STUDIOS — roku 12 (DetailsScreen.brs:853 `kept = CapTo12(items)`), firetv 16 (DetailActivity.kt:868 `rich.companies.take(16)`), tvos 6 (DetailSheet.swift:887 `ForEach(companies.prefix(6))`), web 12 (app.js:3383 `RICH_COMPANY_MAX = 12` applied :3532). REVIEWS — roku 2, firetv 8 (DetailActivity.kt:894 `rich.reviews.take(8)`), tvos 2 (Sources/RichMeta.swift:39 `static let reviewMax = 2`, applied :193), web 2 (app.js:3384 `RICH_REVIEW_MAX = 2` applied :3577). DIRECTORS — roku 6, firetv 6 (DetailActivity.kt:1108 `clean.take(6)`), tvos 2 (DetailSheet.swift:724 `ForEach(Array(d.prefix(2))`), web 3 (app.js:3434 `].slice(0, 3)` in renderCrew). RELATED — roku 12 (DetailsScreen.brs:1115 `CapTo12(RichArr5(r.recommended))`), firetv 20 (DetailActivity.kt:909 `items.take(20)`), tvos 12 (RichMeta.swift:40 `static let relatedMax = 12`), web 12 (app.js:3385 `RICH_RELATED_MAX = 12`). So reviews are now agreed at 2 on three of four (firetv 8 is the outlier); cast agrees at 12 on three of four (tvos 20 is the outlier); studios 12/16/6/12 and directors 6/6/2/3 are still four-way and three-way splits.

### M4 — Fire TV ships a cloud-gaming screen that nothing can open

- **Missing on:** —
- **Fix size:** trivial

firetv (STILL DEAD CODE): /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/CloudGamingActivity.kt:34 `class CloudGamingActivity : AppCompatActivity()`, with its own factory at :127 `Intent(ctx, CloudGamingActivity::class.java)` and log tag at :115. A grep for CloudGaming across the whole of client/app/src/main/java returns ONLY those three self-references — no caller in RetroGamesActivity.kt (grep of that file for Cloud returns nothing), DrawerView.kt, DrawerRows.kt, GameActivity.kt or anywhere else. Manifest entry has moved from the audit's :157 to client/app/src/main/AndroidManifest.xml:193 `<activity android:name=".CloudGamingActivity" android:exported="false"` (comment at :191-192), and `exported="false"` means nothing outside the app can launch it either — so it is unreachable by construction. Other clients confirmed absent: grep -i for cloudgaming|"cloud gaming" returns zero hits in roku components/ and source/, zero in blazing-web index.html and *.js, zero in blazing-tvos Sources/.

### M5 — Fire TV ships a whole delete-confirmation screen that nothing can reach

- **Missing on:** Fire TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/MainScene.brs:5657`

firetv /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/ProfileGateActivity.kt:1888 `private fun showDeleteConfirm(p: ProfileClient.Profile, unlockToken: String?) {` — `grep -rn "showDeleteConfirm" client/app/src/` returns EXACTLY ONE line, the definition itself. Still dead code. | firetv ProfileGateActivity.kt:1654-1685 the edit hub is unchanged: `editHubButton("Name")`, `editHubButton("Icon")`, `editHubButton("PIN")`, `editHubButton("Parental controls")`, then `doneBtn` — no Delete row. | firetv ParentalSettingsActivity.kt:171-200 the reachable delete is still the arm-then-press button: `var deleteArmed = false` … `text = "Press again to delete ${p.name}"`, guarded by `if (ProfileGateRules.canDelete(profileCount))` at :213. | roku /Users/markususche/Desktop/roku channels/components/MainScene.brs:5657-5663 `sub openProfileDeleteConfirm()` … `d.title = "Delete Profile"` / `d.buttons = ["Delete", "Cancel"]` — the real dialog the audit named, still there.

### M6 — On-screen key hints exist on two clients and not the other two

- **Missing on:** Fire TV
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/LiveView.swift:367`

roku /Users/markususche/Desktop/roku channels/components/screens/LiveTvScreen.brs:375-381 — four focus-zone hints, e.g. :379 `m.hint.text = "OK watches   •   * stars a favourite   •   LEFT for the groups"`; the node is declared at components/screens/LiveTvScreen.xml:194 `<Label id="hint" …>`. | tvos /Users/markususche/Desktop/blazing-tvos/Sources/LiveView.swift:364-368 `ScreenHeader(title: "Live TV", status: statusLine, hint: "OK watches   ·   PLAY/PAUSE stars a favourite")`. | firetv /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/LiveTvActivity.kt — still 347 lines and still has NO hint view: grepping the file for hint text returns only ListRow/HeaderItem("Guide") lines (:182, :314). The screen has no title bar at all; GuideActivity.kt:79-91 is the only Live-TV title bar with a status TextView. | web /Users/markususche/Desktop/blazing-web/index.html:375-378 is the .page-heading only (pointer device; the audit's own fix says leave web alone).

### M7 — Only Roku shows an "up next" programme on the channel list

- **Missing on:** Fire TV, Web, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/LiveTvScreen.brs:597`

roku /Users/markususche/Desktop/roku channels/components/screens/LiveTvScreen.brs:597-599 `line = line + "  ·  Next: " + EpgClock(e.nextStart)` / `line = line + " " + ThemeNz(e.nextTitle)`; the cache shape is documented at :56 `channelId -> { nowTitle, nowStart, nowStop, nextTitle, nextStart }`, and source/lib/Epg.brs:201-204 states why: "Only /live/guide is used. It answers 'what is on now' AND 'what is next' in one round trip". | firetv LiveTvActivity.kt:163-166 `val nowMap = … LiveClient.now(…)` then `ChannelItem(ch, nowMap[ch.id]?.title)` — now only. | tvos Sources/FleetAPI.swift:390-394 `struct LiveNowProgramme` still holds `title, category, start, stop` and nothing else; `grep -n "nextTitle|upNext|next:" Sources/LiveGuide.swift Sources/LiveView.swift` returns NOTHING, so the channel tile still draws now only (the file header at LiveGuide.swift:3 says "NOW/NEXT" but no next field exists). | web /Users/markususche/Desktop/blazing-web/livetv.js:494-498 `function nowLine(programme)` returns `${clock(programme.startMs)}  ${programme.title}` only; paintCard at :606-612 sets that one line.

### M8 — Only the web puts a page heading over Home; the TV clients use a hint line or nothing

- **Missing on:** Roku, Fire TV, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-web/app.js:1960`

web /Users/markususche/Desktop/blazing-web/index.html:375-379 the `.page-heading` block (`<h1 id="browse-title">Home</h1>`, blurb "Everything on the household's shelves."); app.js:1960-1965 `const BROWSE_HEADINGS = Object.freeze({ home: ['Browse', 'Home', 'Everything on the household’s shelves.'], … })` and app.js:1992-1995 writes eyebrow/title/blurb on every browse route — so the heading is still what distinguishes Home/Movies/Shows/Anime and cannot simply be deleted. | roku components/screens/HomeScreen.brs:157 `m.hint.text = "OK opens   ·   LEFT for the channels   ·   UP for the bar"` — a hint, not a title. | firetv HomeFragment.kt:107-108 `title = null` / `setOnSearchClickedListener(null)`. | tvos Sources/HomeView.swift:802-804 `private var loading { … ScreenHeader(title: "Blazing", status: model.status, hint: "OK opens the title") }` — the header is still inside `loading`; `private var home` begins at :814 with a bare ScrollView and no header. Unchanged since the audit; still needs Markus's decision on which way parity goes.

### M10 — The "no stream" failure message names the channel on three clients and not on Roku

- **Missing on:** Roku
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/LiveView.swift:525`

roku STILL ANONYMOUS — /Users/markususche/Desktop/roku channels/source/lib/LiveTV.brs:339 `out = { kind: "liveplay", ok: false, name: name, error: "No live stream is available." }` and :345 `out.error = "The live service did not return a stream."` — both fixed literals, and `name` is on the contract at :339 and never interpolated. It reaches the viewer un-named at components/MainScene.brs:6883-6888 `else if r.kind = "liveplay" … else showToast(Nz4(r.error))`. `FleetConfigured()` is still tested at LiveTV.brs:340, so the missing-credential case can still be told apart. | firetv LiveTvActivity.kt:227 `Toast.makeText(requireContext(), "No stream available for ${channel.name}", …)`. | tvos Sources/LiveView.swift:525 `model.status = "\(ch.name) has no stream the fleet can resolve right now."`. | web livetv.js:944 `say(storedCredentials() ? `${name} has no playable stream right now.` : NO_PROFILE_COPY)` — and it still carries the credential-vs-dead-channel distinction.

### M11 — The Adult shelf is forced to the bottom of Home on the Fire TV and left wherever the feed puts it on the Roku

- **Missing on:** Roku
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/HomeFragment.kt:630`

firetv HAS IT — HomeFragment.kt:630-641 `// Adult (18+) LAST, deliberately. It is the one row nobody should land on by accident … it sits below everything including Settings` then `if (ProfileClient.canShowAdult()) { val adultRow = Catalogs.adultRow(base) … }`. | roku STILL UNPINNED — components/screens/HomeScreen.brs:794-806 `function OrderHomeRows(rows as Object)` with `priorities = ["watch party", "continue watching", "latest releases", "trending movies", "latest tv shows"]` — no adult entry anywhere in the ordering; :328-333 `function dropAdultRows` only REMOVES the row (`isAdult = (… r.title = "Adult (18+)")`) when `m.allowAdult` is false, it never repositions it. | tvos — `grep -n "adult" Sources/HomeView.swift` returns one prose comment at :59 only; Home has no adult shelf, so nothing to pin. | web — no adult Home row; app.js:2646 `const ADULT_CATALOG = /adult|nsfw|jav|hentai|porn|xxx|18\+/i` is a catalogue filter, not a shelf. Roku is the only client that can act on this, and only if the fleet ever serves it that row.

### M12 — The MrWorldPremiere source is named on two clients, hidden on one and mislabelled on none of the rest

- **Missing on:** Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-web/app.js:603`

tvos STILL UNLABELLED — /Users/markususche/Desktop/blazing-tvos/Sources/Sections.swift:458 `async let mrWorldPremiere = BlazingAPI.mrWorldPremiereSearch(query: q)` and :463-465 `let all = ((try? await movies) ?? []) + ((try? await series) ?? []) + ((try? await mrWorldPremiere) ?? [])` — concatenated into ONE flat array, deduped at :466-467, drawn by the same PosterTile. Sources/Chrome.swift:62-76 `struct PosterTile` still declares only `art, caption, sub, width, height, progress, heroCard, action` — no source/badge parameter exists. | roku components/screens/SearchScreen.brs:152 `{ label: "Other sources", items: [] }` — a separate row. | firetv SearchActivity.kt:308 `results.add(ListRow(HeaderItem("MrWorldPremiere"), a))` — kept separate on purpose (see the comment at :261). | web app.js:603 `function sourceBadge(meta)` and :2529 `const badgeText = sourceBadge(meta);` — a per-card badge.

### M13 — The Play button says what it is about to play on two clients and just 'Play' on the other two

- **Missing on:** Web, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/DetailsScreen.brs:1367`

roku HAS IT — components/screens/DetailsScreen.brs:1367-1375 `function PlayLabel() as String` with the five states: `"Source page only"`, `"Play"`, `"Play  ·  " + m.playHint`, `"No stream found"`, `"Finding best stream…"`. | firetv HAS IT — DetailActivity.kt:1556-1560 `append("▶  Play best  ·  $q")` / `if (best.cached) append("  ·  ⚡ INSTANT")` / `if (extra.isNotBlank()) append("\n$extra")`. | web STILL 'Play' — index.html:982 `<button id="detail-play" class="primary-button" type="button">Play</button>`; every detailPlay reference in app.js (:1093 lookup, :3228 insertAdjacentElement, :3290 hidden, :3291 disabled, :3345 disabled=false, :3349 focus) touches visibility/disabled/focus only — `detailPlay.textContent` is never assigned anywhere in the file, including inside the `fullMeta.finally()` block at :3341-3350. | tvos STILL 'Play' — Sources/DetailSheet.swift:754 `SheetButton(title: "Play", icon: "play.fill", primary: true) {` — a fixed string.

### M14 — The Roku's Reading catalogue screen is built, gated and unreachable from any menu

- **Missing on:** Roku
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/NavBar.swift:29`

roku STILL UNREACHABLE — components/MainScene.xml:70 `<ReadingCatalogScreen id="reading" visible="false" />` is still mounted; the ONLY route in is components/MainScene.brs:1520-1534 `else if name = "reading" or name = "manga" or name = "comics" … navTo("reading")` inside `sub onHomeActionNamed` (:1481). I read the full drawer build — `sub fillDrawer()` at components/MainScene.brs:1836-1965 — and listed every `addDrawerRow`: Search, Home, Movies, TV Shows, Anime & Manga, Books, Roadmaps, Library, Live TV, YouTube, Games, Calendar, Watch Party, My Library, My Locker, Emby Library, Adult, Sources, Settings, then the MOVIES / TV SHOWS / CHANNELS & APPS filter and provider rows. There is NO Reading, Manga or Comics row. The only caller that can supply those names is the developer deep link at MainScene.brs:711-719 `launch/dev?action=screen&name=…` → `onHomeActionNamed(nm)`. The Anime hub's Manga/Comics rail rows (components/screens/AnimeHubScreen.brs:88-89, `{ kind: "cat", label: "Manga", section: "manga" }`) do NOT navigate there — `sub requestSection` at AnimeHubScreen.brs:521-543 sets `m.section = section` and repaints the hub's own shelves; only `search` and `books` escape via `m.top.chosen`. | tvos the deliberate opposite is intact — Sources/NavBar.swift:29-34 `/// `home` is in the DRAWER only. `reading` is in NEITHER list` and `case home, reading` is excluded from both orders.

### M15 — The Search entry in the top navigation is icon-only on tvOS and labelled everywhere else

- **Missing on:** Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/NavBarView.kt:75`

tvos (STILL MISSING): /Users/markususche/Desktop/blazing-tvos/Sources/NavBar.swift:388-391 — `if section != .search && section != .settings { Text(section.barTitle) }` still suppresses the word for both chips; the icon at :386 always draws. The width reason is still there at :395-397 ('The bar carries eleven chips and is `.fixedSize()`, so it cannot compress') and .fixedSize() is still on the bar at NavBar.swift:321. Section.title for .search is 'Search' at NavBar.swift:163, and the accessibility label DOES use it (NavBar.swift:366 `.accessibilityLabel(section.barTitle)`) — so the spoken bar and the seen bar still disagree. | roku (HAS IT): /Users/markususche/Desktop/roku channels/components/MainScene.brs:7820 `m.navOrder.Push("search") : labels.Push("Search") : icons.Push("search")`, identical shape to every other chip at :7813-7821 (line moved from the audit's 7289; behaviour unchanged). | firetv (HAS IT): /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/NavBarView.kt:75 `SEARCH("Search")` in `enum class Dest(val label: String)` at :60, added to the bar at :159 `dest(Dest.SEARCH)` through the same `fun dest(d: Dest) = list.add(Entry(d.label) {...})` at :153 that labels every other entry. | web (HAS IT): /Users/markususche/Desktop/blazing-web/index.html:153 `<button type="button" data-view="search">Search</button>` in .topnav; index.html:155 is the separate magnifier icon-button.

### M17 — The result-count line says something different on every client, and says nothing on Fire TV

- **Missing on:** Fire TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/Sections.swift:480`

firetv (STILL SAYS NO NUMBER): /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/SearchActivity.kt:31-62 — the whole layout is `root` FrameLayout + `fragContainer` + one `ToggleButton` ('AI Search: ON'/'OFF'); there is no status TextView. `grep -n 'status' SearchActivity.kt` returns nothing in the file's 337 lines. The result path at :279-333 only ever adds `ListRow(HeaderItem(label), a)` row titles (:180, :254, :291, :308, :327, :333) — no count is ever composed. | roku: components/screens/SearchScreen.brs:186-189 builds `line = Stri(titles).Trim() + " titles"` plus `"  ·  " + videos + " YouTube videos"`, written at :209 with the two remote hints. | web: /Users/markususche/Desktop/blazing-web/app.js:1529-1531 — still `${cards.length} result(s) in ${searchActiveGenre}.` / `... in movies and TV.`; the audit's note that this wording is wrong (the grid also holds Emby and scraped-site hits) is unactioned. | tvos: /Users/markususche/Desktop/blazing-tvos/Sources/Sections.swift:480 and :495 — `"\(found.count) results for \"\(q)\""` / `"Nothing matched ..."`.

### M21 — The status line says a different thing, or nothing, on each client

- **Missing on:** Fire TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/GuideActivity.kt:84`

firetv (STILL SILENT): /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/LiveTvActivity.kt:80 `title = "Live TV"` and nothing else — `grep -n 'status' LiveTvActivity.kt` returns no match in its 347 lines, and the row headers at :173-176 `rowsAdapter.add(ListRow(HeaderItem(title), listAdapter))` carry no count either. The data IS already in hand: LiveClient.kt:58 `data class ChannelPage(val total: Int, val channels: List<LiveChannel>)` with :128 `ChannelPage(j.optInt("total", list.size), list)`, and LiveTvActivity.kt:156-160 already holds the `page` object. The sibling widget to lift is still there: GuideActivity.kt:84-91 `status = TextView(this).apply { text = "Loading channels…" ... }` added to the title bar at :91. | roku: components/screens/LiveTvScreen.brs:343-347 `sub showStatus()` → `line = Str(m.totalChannels).Trim() + " channels"`, plus ' so far   ·   still loading…' while partial. | web: /Users/markususche/Desktop/blazing-web/livetv.js:709 `status.textContent = \`Showing ${shown} of ${state.total.toLocaleString()} channels${where}.\`;`. | tvos: /Users/markususche/Desktop/blazing-tvos/Sources/LiveView.swift:492 `parts.append("\(model.total) of \(unfiltered) channels not already known dead")`, with the unfiltered totals kept at :133-138.

### M22 — The trailer is a small picture-in-picture box on Roku and a full-bleed backdrop on the other three

- **Missing on:** Roku
- **Fix size:** large
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:206`

roku (STILL THE PIP, AND STILL MARKED DO-NOT-TOUCH): /Users/markususche/Desktop/roku channels/components/screens/DetailsScreen.xml:122 `<Rectangle color="0x0A0A0BFF" width="1920" height="1080" />` — one flat plate, no backdrop Poster, no scrim (the memory reason is at :118-121). The PIP box is unchanged at :193-197 `<Poster id="trailerStill" translation="[1324,78]" width="500" height="281"`, :198 `<Video id="trailerVideo" translation="[1324,78]" width="500" height="281" visible="false" />`, :204-205 `<Rectangle id="trailerRule" ... translation="[1324,359]" />`, and the preference note at :188-192 is verbatim still there: '500x281 at [1324,78] is byte for byte what it was, because Markus picked it ("i love that the trailer loads on the details page in the PIP box") and DESIGN-V2.md 3.3 marks it do-not-touch.' | THE OTHER THREE ARE STILL FULL-BLEED — firetv: client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:206-213 full-screen backdrop ImageView with `alpha = 0.22f`, added at :213; the hero trailer TextureView at :219 (field at :173, bound to the player at :582). tvos: /Users/markususche/Desktop/blazing-tvos/Sources/DetailSheet.swift:531 backdrop `.opacity(0.18)`, :538-539 `BackgroundVideo(player: p).opacity(0.30)`. web: index.html:972-973 `<div id="detail-art" class="detail-art">` wrapping `<div id="detail-trailer" class="detail-trailer">`. THIS ONE NEEDS MARKUS'S DECISION BEFORE ANY EDIT — the audit is explicit that Roku's geometry is his own named preference, so a fixer must not 'close' it by changing Roku.

### M26 — There is no explicit Search button on tvOS, and no way to clear the query except on web

- **Missing on:** Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-web/app.js:1660-1671 (the three-line clear path plus the removable query chip); /Users/markususche/Desktop/roku channels/components/screens/SearchScreen.xml:46 for the explicit submit button`

tvos: /Users/markususche/Desktop/blazing-tvos/Sources/Sections.swift:379-397 is the entire search input — `HStack(spacing: 20) { TextField("Title", text: $query) ... .onSubmit { Task { await run() } }` at :380-387 and `TextField("AI Search", text: $aiQuery) ... .onSubmit { Task { await runAI() } }` at :389-396. Two bare TextFields, no Button, no X, no clear affordance; a grep of Sections.swift for TextField|onSubmit|Clear|clear|xmark returns only those four lines. `query`, `items` and `status` are declared at :366-369 and nothing resets them but a new run. STILL EXACTLY AS THE AUDIT DESCRIBED. roku HAS it: components/screens/SearchScreen.xml:46 `<Button id="goBtn" text="Search" translation="[90,872]" minWidth="432" height="64" focusable="true">`, wired at components/screens/SearchScreen.brs:4 `m.goBtn.ObserveField("buttonSelected", "onGoPressed")` with onGoPressed at :106. web HAS both: index.html:394 `<button class="primary-button" type="submit">Search</button>` and the removable chip at app.js:1669-1671 `const chip = $('#search-chip'); chip.textContent = `${query} ✓...`` (app.js:1670 `chip.textContent = `${query} ✕`;`), whose comment at app.js:1665-1668 says "clicking it clears via the same path an empty submit already takes" — that path is app.js:1660-1664 `if (!query) { clearSearchResults(); status.textContent = 'Type a title first.'; return; }`. firetv: SearchActivity.kt:65 `class SearchFrag : SearchSupportFragment(), SearchSupportFragment.SearchResultProvider` with onQueryTextChange/onQueryTextSubmit at :146-153 — submission and clearing are Leanback's own search bar, exactly as the audit's fix note said; the only added control is the AI ToggleButton at SearchActivity.kt:45-57. So Fire TV inherits a framework affordance and is not a source-level gap; tvOS is.

### M27 — Update checking and installing exists only on Fire TV

- **Missing on:** Roku, Apple TV, Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/MainActivity.kt:42-43 (the channel · versionName · versionCode subtitle). Landing sites: roku components/screens/SettingsScreen.brs:275 beside the device line, tvos Sources/Sections.swift:609-612 as a fifth Field, web settings.js:490-494 as a fifth dt/dd pair.`

firetv HAS both halves: MainActivity.kt:42-43 is the version line — `text = "Blazing TV  ·  ${BuildConfig.CHANNEL}  ·  v${BuildConfig.VERSION_NAME} (${BuildConfig.VERSION_CODE})\n" + "${Build.MODEL}  ·  Fire OS ${fireOsVersion()}"`; the installer is MainActivity.kt:59-60 `text = "Check for updates" / setOnClickListener { checkForUpdates() }`, :93 `checkForUpdates()` on open, and :96-134 the five states (Updater.fetchManifest at :100, Updater.isNewer at :106, Updater.downloadAndInstall at :110, Submitted/AlreadyPending/Error at :112-128). THE VERSION LINE — which the audit's own fix calls the parity worth having — IS STILL MISSING ON ALL THREE OTHERS. roku: components/screens/SettingsScreen.brs:275 `m.deviceName.text = Nz3(d.modelDisplay) + "   ·   Roku OS " + Nz3(d.osVersion)` — that is the DEVICE's OS, not the channel's version; the channel version is read only for telemetry at source/lib/Telemetry.brs:95-96 `ai = CreateObject("roAppInfo") / m.telAppVersion = TelNz(ai.GetVersion())` and is never rendered, and grep over source/ and components/ finds no 'check for update' or Updater token. tvos: the About block is Sources/Sections.swift:602-612 — `Text("ABOUT THIS APPLE TV")` then `Field("Sources", BlazingAPI.base)`, `Field("Fleet", FleetAPI.base)`, `Field("Adult", ...)`, `Field("Player", ...)` — four fields, no version; the bundle version is read only in Sources/Telemetry.swift:41-42 `(Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String) ?? "unknown"` and posted at Telemetry.swift:160, never shown. web: the new settings screen's About block is settings.js:486-498 — `sectionTitle(root, 'About')` then ['Profile'], ['Rating limit'], ['Fleet', FLEET_BASE], ['Sources', ADDON_BASE]; no version row, and grep for version|APP_VERSION|registration.update|updatefound over settings.js, app.js, sw.js and index.html finds only sw.js:154 self.skipWaiting() — service-worker activation, not a user-facing check, exactly as the audit recorded.

## PARTIAL — 28

### M5 — "Play next episode automatically" is a setting on the two TV clients only — and the behaviour itself is missing on the other two

- **Missing on:** Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-web/app.js:5696`

WEB IS NOW DONE (behaviour AND setting): /Users/markususche/Desktop/blazing-web/app.js:5658 `video.addEventListener('ended', () => {` ... :5666 `autoNextEpisode();`; :5696 `async function autoNextEpisode() {` :5697 `if (!window.BlazingSettings || !window.BlazingSettings.autoNext()) return;`; settings.js:51 `const AUTO_NEXT_KEY = 'blazing-auto-next-episode-v1';`, :147 `const autoNext = () => readStore(AUTO_NEXT_KEY) !== '0';`, :409 `row(root, \`Play next episode automatically: ${onOff(autoNext())}\`, () => setAutoNext(!autoNext()))` — its own key, not inside the source-filter blob, as the fix line required. TVOS STILL MISSING BOTH: rg for playNext|autoNext|nextEpisode|'next episode' over /Users/markususche/Desktop/blazing-tvos/Sources/*.swift returns zero matches; the only autoplay hits are trailers (CardMeta.swift:477 `"action": "trailer_autoplay"`, DetailSheet.swift:252 same, TopShelfPublisher.swift:194). ROKU HAS IT: components/MainScene.brs:7374 `if playNextEpisode() then return` inside onPlaybackFinished, :7494 `function playNextEpisode() as Boolean` / :7495 `if not AutoNextEnabled() then return false`; SettingsScreen.brs:109 the row, :413 `SetAutoNext(not AutoNextEnabled())`; source/lib/SourceFilter.brs:161/166 its own registry key. FIRETV HAS IT: DetailActivity.kt:140 `if (playedToEnd && SourceFilters.autoNextEpisode(this)) playNextEpisode()`, EpisodesActivity.kt:53 same, MainActivity.kt:252-253 the row.

### M7 — A kids profile with no permitted channels gets a network-error message on Roku and a correct explanation on Fire TV

- **Missing on:** Roku, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/LiveTvActivity.kt:329`

WEB IS NOW DONE: /Users/markususche/Desktop/blazing-web/livetv.js:662 `const KIDS_CAPPED_COPY = 'No kids channels. This profile's rating limit hides Live TV.';`, :241 `function kidsCapped()`, branched at :680 `status.textContent = kidsCapped() ? KIDS_CAPPED_COPY : ...` inside describe(), plus the capped count wording at :698-704. FIRETV MODEL UNCHANGED: LiveTvActivity.kt:329-330 `card.titleText = "No kids channels"` / `card.contentText = "This profile's rating limit hides Live TV"`. ROKU STILL WRONG: components/screens/LiveTvScreen.brs:219-222 the empty case still says `m.status.text = "Could not load the playlist. Check the network, or set a different M3U in Settings."` and `showEmpty("Live TV is unavailable", "The channel service did not answer. Check the network and Live TV settings, then open this screen again.")` with no kids branch; the snapshot still carries no kids flag — source/lib/LiveTV.brs:723-724 computes `if kids then srv = 0` but returns only `{ groups, byGroup, total, partial, serverTotal }`, so LiveTvScreen cannot tell the two cases apart; rg for kids|capped|'rating limit' in LiveTvScreen.brs finds only local variables named `kids` (:153, :752) that are GetChildren arrays. TVOS STILL HAS NO CAPPED STATE: LiveView.swift applies the cap (:176 `RatingRules.liveTvGroupAllowed(...)`, :201 `guard cap == RatingRules.kidsCap`) but the only empty/failure strings are :232-234 (favourites) and :244 `status = "The live service did not answer."` / :252 `status = ""` — nothing names the rating limit.

### M13 — A turned-off profile behaves differently on all four clients

- **Missing on:** Fire TV, Roku
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:2120`

TVOS HAS IT: Sources/RootView.swift:2120-2121 `if profile.isDisabled { Text("Turned off by the owner")` on the focused row; rows stay reachable (the only refusal is RootView.swift:1846 `guard !profile.isDisabled else { return }`), with RootView.swift:2075 also giving the a11y label "…, turned off". WEB HAS IT: profile.js:943 `const meta = profile.disabled ? 'Not available' : …` — an always-visible sub-label. FIRETV STILL MISSING: ProfileGateActivity.kt:1110 `if (p.disabled) {` then :1115 `art.isFocusable = false`, :1116 `art.isFocusableInTouchMode = false`, :1118 `row.alpha = 0.35f` — the D-pad still skips the row and no reason is drawn on it. ROKU STILL MISSING the always-visible label: ProfileGate.brs:288 `if p.disabled = true then bits.Push("Off")` in GateBadge() is DEAD — its only consumer is ProfileGate.brs:249 `entries.Push({ … badge: GateBadge(p), … })` and makeCard (ProfileGate.brs:470-563) draws only avatar, initial, name and pencil; `rg 'e.badge|c.badge' components/screens/ProfileGate.brs` returns no reader. The only explanation is still the post-press hint at ProfileGate.brs:843 `m.hint.text = "This profile is turned off. Ask the owner."`.

### M17 — Add-on / source management is a different screen on each client, or absent

- **Missing on:** Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-web/settings.js:507`

WEB IS NOW CLOSED, both halves. Read-only directory: blazing-web/sources.js exists (header lines 1-66 name this as audit item B2), is wired — index.html:1167 `<script src="./sources.js" defer></script>`, index.html:244 `<button type="button" data-view="sources" hidden>Stream Sources</button>`, index.html:946 `<section id="sources-view">`, app.js:2098 `if (route === 'sources') window.BlazingSources && window.BlazingSources.mount();`. Account-wide WRITE surface: settings.js:506 comment `firetv MainActivity.kt:333-386 — field, Save and sync, Use default, note.` then settings.js:507 `function renderAddonForm(root)` (called at settings.js:482), PUT at :578 `secret(ADDON_SECRET, { method: 'PUT', value })` against settings.js:55 `const ADDON_SECRET = 'stremio_addon_url'` — i.e. exactly Fire TV's account-level store the audit recommended. TVOS IS HALF DONE: Sources/StreamSourcesView.swift exists and is wired (Sources/RootView.swift:1217 `case .sources:  StreamSourcesView(shell: shell)`), but it is read-only by design and there is still NO write surface — Sources/BlazingAPI.swift:9 `static let base = "https://addon.lyreosai.com"` is still a compiled-in constant and `rg 'stremio_addon_url|accountSecret' Sources/` returns zero hits. DONORS ALIVE: roku components/screens/AddonsScreen.brs:24 hint, :42 status, :101 `sub onToggle()`; firetv MainActivity.kt:346/354/371 (field, "Save and sync", "Use default") and AddonStoreActivity.kt present.

### M22 — Apple TV and web keep no local progress copy and never merge; Roku and Fire TV do both

- **Missing on:** Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/ProgressStore.swift:207`

APPLE TV IS NOW FIXED — a new file landed. /Users/markususche/Desktop/blazing-tvos/Sources/ProgressStore.swift:27 `enum ProgressStore`, :39 `private static let defaults = UserDefaults.standard`, :243 `private static func key(_ profileID: String) -> String { "continue.\(profileID)" }` (per-profile bucket, matching Progress.kt:64), :144 `static func write(_ entries: [ContinueEntry], profileID: String)`, :154 `static func upsert(...)`, :207 `static func merge(local:remote:)` with its own comment naming Fire TV's mergeEntries (Progress.kt:353) as the source. It is REACHED: Sources/Progress.swift:120-127 `let local = ProgressStore.entries(profileID:)` … `guard let remote = ... else { return local }` … `let merged = ProgressStore.merge(local: local, remote: remote); ProgressStore.write(merged, ...)`. The read-modify-write race is gone too — Progress.swift:82-84 `if !ProgressStore.seeded(profileID: profileID) { _ = await load(...) }` then :86 `let items = ProgressStore.upsert(fresh, profileID: profileID)`. WEB IS STILL EXACTLY AS THE AUDIT DESCRIBED: app.js:5314 `async function loadContinueWatching(...)`, :5320-5321 `let entries = await fleetProgressEntries(profileId).catch(() => []); if (!entries.length) entries = await addonProgressEntries(profileId).catch(() => []);` with the comment "Settled, not raced … Merging the two would double every title" — fleet-then-addon, never merged, never cached. The complete set of localStorage keys in app.js is 'blazing-household-approved', 'blazing-signed-out-v1' and 'profileId' — there is no progress key at all. ROKU/FIRE TV reference still stands: source/lib/Store.brs (ContinueKey / MergeContinueFromFleet) and Progress.kt:45/:103-105.

### M25 — End of an episode: Roku shows a Play-next button, Fire TV auto-advances behind a preference, the web and Apple TV do neither

- **Missing on:** Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-web/app.js:5696`

WEB IS NOW FIXED — this landed after the audit. app.js:5657-5667 the `ended` listener now ends `autoNextEpisode();` at :5666, and app.js:5696 `async function autoNextEpisode()` is a real implementation: :5697 `if (!window.BlazingSettings || !window.BlazingSettings.autoNext()) return;`, :5704 `if (playingEpisodeId !== current.id) return;` (the arming id set at app.js:4166 `playingEpisodeId = state.selectedEpisode ? state.selectedEpisode.id : '';` and cleared at :4901), :5708 `const next = episodes.slice(at + 1).find((episode) => episode.season >= 1);`, :5713 `playingEpisodeId = next.id;` then resolveStreams. The toggle exists: settings.js:147 `const autoNext = () => readStore(AUTO_NEXT_KEY) !== '0';`, the Settings row at settings.js:409, exported at :731. APPLE TV IS STILL MISSING ENTIRELY: grep of Sources/PlayerView.swift for AVPlayerItemDidPlayToEndTime returns nothing (the only two uses in the repo are Sources/CardMeta.swift:468 and Sources/DetailSheet.swift:247, both trailer loopers); the VLC branch at Sources/PlayerView.swift:1068-1073 treats an end only as a possible DEAD LINK (`let ended = everOpened && (state == .stopped || state == .stopping); if state == .error || (ended && !monitor.hasPlayed) { ... onFailure(.dead) }`) and a genuine end after playing falls out with no handler; there is no autoNext/playNext anywhere in Sources/ (the only queue hook is Sources/RootView.swift:411 `func playEpisode(_ episode: EpisodeVideo, showName: String)`, called only from the detail sheet at :1273). ROKU/FIRE TV references still stand: components/screens/PlayerScreen.brs:646 `function ENDCARD_S()`, :697 `m.endCardBtn.text = "DOWN   Play next"`, :563 `sub warmNext(pct as Integer)`, MainScene.brs:7374 `if playNextEpisode() then return`.

### M26 — Every client draws the 'finished' line in a different place, or not at all

- **Missing on:** Web, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/Progress.kt:45`

THE FOUR NUMBERS STILL DISAGREE. FIRE TV 90: Progress.kt:45 `const val WATCHED_AT_PERCENT = 90`, :103-105 `fun isFinished(positionMs, durationMs) = ... >= WATCHED_AT_PERCENT`, acted on at PlayerActivity.kt:1019 `if (Progress.isFinished(pos, dur)) {`. ROKU 95, both sites unchanged from the audit (line numbers moved): components/MainScene.brs:993 `if m.lastTitle <> invalid and s.progress < 95` (the save guard) and :1023 `else if m.lastTitle <> invalid and s.progress >= 95` (the removal). TVOS PARTLY MOVED, NOT FIXED: a 95 constant now exists — Sources/ProgressStore.swift:37 `static let finishedPercent = 95` — but it only HIDES a finished entry from the resume lookups (:72 `guard entry.positionSecs > 0, entry.progress < finishedPercent`, :94 the same for the percentage fallback). There is still no finish branch on the write path: Sources/RootView.swift:875-890 `func recordProgress(...)` guards only `position >= 10` (:876), computes `let percent = duration > 0 ? min(100, position * 100 / duration) : 0` (:879) and unconditionally calls `ProgressSync.save(entry, ...)` (:889) — a 100% entry is still PUT and still kept by ProgressStore.upserted (:161-170, which never drops a finished entry), and Home draws the raw list (Sources/HomeView.swift:173 `rawContinue = await ProgressSync.load(profileID: profileID)` with no percent filter). The stray 95 the audit named is still stray: Sources/TopShelfPublisher.swift:86 `.filter { ($0.percent ?? 0) < 95 }`. WEB STILL HAS NO COMPLETION BRANCH AT ALL: the `ended` listener at app.js:5657-5667 fires telemetry `reason: 'finished'` (:5664) and autoNextEpisode() and nothing else; startSync (app.js:5382-5395) POSTs position/duration on a 30s interval with no threshold; grep of app.js for `>= 90` / `>= 95` / WATCHED_AT returns nothing, and there is no removal path.

### M34 — Fire TV refuses an adult raise on a PIN-less profile up front; Roku offers the button and lets the server say no

- **Missing on:** Roku
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/MainScene.brs:5541-5547 — the same guard already written in this file, just not applied to openProfileRatingDialog/onProfileRatingSelected; original at /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/ParentalSettingsActivity.kt:223-226`

HALF FIXED ON ROKU SINCE THE AUDIT. The allowAdult toggle now pre-checks: /Users/markususche/Desktop/roku channels/components/MainScene.brs:5541-5547 — `if idx = 0 and raising and Nz4(m.editToken) = ""` … `d.message = "Set a PIN first - adult access needs one to unlock."` (:5545), with the rationale at :5512-5540 naming Fire TV's requireUnlockThen. That matches firetv client/app/src/main/java/com/nyctailblazers/blazingtv/ParentalSettingsActivity.kt:223-226 `if (!p.hasPin) { showToast("Set a PIN first — adult access needs one to unlock."); return }`. STILL OPEN ON ROKU — the exact dialog the audit cited is untouched: MainScene.brs:5449 `sub openProfileRatingDialog()` builds MainScene.brs:5454 `d.buttons = ["General", "Teen", "Mature", "Adult", "Cancel"]` unconditionally, and onProfileRatingSelected at MainScene.brs:5462-5480 patches straight through — `f.maxRating = ratings[idx]` at :5479 then submitProfilePatch — with no hasPin and no editToken test. Roku's own header at MainScene.brs:5124-5127 records that the fleet `requires a valid unlock token to raise the rating cap to adult … and refuses (401) without it`, so a PIN-less profile pressing "Adult" in Rating Limit still gets the bare server 401 with no instruction. tvOS/web are unaffected — neither exposes an adult rating raise from a PIN-less profile path the audit named.

### M37 — Games search exists on Roku and web and is missing on tvOS

- **Missing on:** Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-web/games.js:143,:272 for the query shape; /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/GamesCatalogClient.kt:92-96 + RetroGamesActivity.kt:457-576 for the search field AND the paging tvOS also lacks`

FIRE TV HAS BEEN FIXED SINCE THE AUDIT; tvOS has not. firetv NOW HAS IT: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/GamesCatalogClient.kt:92 `fun catalog(page: Int, search: String = ""): Result` appending `&search=` at :95-96 against `/games/catalog`; driven from RetroGamesActivity.kt:457 `val field = EditText(this)` → :507 `catalogQuery = q` → :509/:512 `loadCatalogPage(1, append = false)` → :522 `GamesCatalogClient.catalog(page, catalogQuery)`, with paging at :576 `setOnClickListener { loadCatalogPage(catalogPage + 1, append = true) }` and an empty-state at :551. So the audit's `RetroGamesActivity.kt:33 is an emulator launcher` is stale. tvos STILL MISSING: /Users/markususche/Desktop/blazing-tvos/Sources/FleetAPI.swift:270 `static func games(page: Int = 1) async throws -> [GameItem]` hitting `/games/catalog?page=\(page)&pageSize=40` — no search parameter. Sources/Sections.swift:501-533 `struct GamesView` draws only a ScreenHeader and a LazyVGrid, no TextField, and Sections.swift:528-531 `.task { guard items.isEmpty else { return }; items = (try? await FleetAPI.games()) ?? [] }` fetches page 1 once and never asks for page 2. ROKU HAS IT: components/screens/GamesScreen.brs:20 `m.mode = "catalog"  ' "catalog" | "search"`, :45 hint `* search`, :230 `m.mode = "search"`. WEB HAS IT: /Users/markususche/Desktop/blazing-web/games.js:143 `if (state.mode === 'search' && state.query) params.set('search', state.query);` and :272 `state.mode = q ? 'search' : 'catalog';`.

### M41 — Live TV playlist entry (M3U / Xtream) exists only on Fire TV; Roku reads a key it gives no way to set

- **Missing on:** Roku, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-web/settings.js:633 (form) + /Users/markususche/Desktop/blazing-web/profile.js:3935 (POST /device/live/sources)`

Web CLOSED since the audit. /Users/markususche/Desktop/blazing-web/settings.js:633 `function renderPlaylistForm(root)` builds the four fields (:647 name, :648 'M3U URL, or Xtream server URL', :649 user, :650 password) and :674 `const kind = (username && password) ? 'xtream' : 'm3u';` — and it IS reached: settings.js:483 `renderPlaylistForm(root);` inside the grownUp branch of the Sources section. It posts through /Users/markususche/Desktop/blazing-web/profile.js:3935 `async function addLiveSource(payload)` → :3956 `request('/device/live/sources?deviceId=...', {method:'POST'...})`. Fire TV still has it: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/MainActivity.kt:179 `val kind = if (user.isNotBlank() && pass.isNotBlank()) "xtream" else "m3u"`, :184 `LiveClient.addSource(...)` → LiveClient.kt:209 `fun addSource(...)` → :226 `ProfileClient.deviceRequest(ctx, "POST", "/device/live/sources", body)`. ROKU STILL OPEN: /Users/markususche/Desktop/roku channels/source/lib/LiveTV.brs:19 `raw = StoreGet("iptv_m3u", ConfigIptvM3u())` and :17 the adult twin; `rg -n 'StoreSet' source/ components/` returns 40 hits and NONE of them is iptv_m3u or adult_m3u, so nothing writes either key. The false promise is still at source/lib/LiveTV.brs:5 'Settings can point this at a paid IPTV provider's M3U instead'. The only Live TV text in components/screens/SettingsScreen.brs is :297, still the read-only guide note pointing at .env.local. APPLE TV STILL OPEN: `rg -n 'device/live|addSource|xtream' Sources/` finds no POST and no form — FleetAPI.swift has only reads (/live/groups :66, /live/channels :98, /live/stream :164); the only m3u hits in Sources/ are EngineChoice.swift:40 and LiveView.swift:538/575, all playback-format strings.

### M43 — Mid-film seek recovery exists on the Roku only

- **Missing on:** Web, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/PlayerScreen.brs:417 (do not report a source that already played)`

The audit's headline consequence — 'on the other three the film restarts from the beginning or dies' — is now FALSE on all three; the narrow Roku behaviour is still Roku-only. (a) Same-source reopen, still Roku only: /Users/markususche/Desktop/roku channels/components/screens/PlayerScreen.brs:399-402 `if m.started and m.lastPos > 2 and m.recoveries < 2 ... m.top.resumeAt = m.lastPos`, budget reset per stream at :40-41 and :75-76, lastPos fed at :532. Nothing equivalent on the other three. (b) Position carry — NOW PRESENT EVERYWHERE: firetv PlayerActivity.kt:734-735 `player?.currentPosition?.takeIf { it > 0 }?.let { startAt = maxOf(startAt, it) }` inside `tryNextCandidate` (:732); tvos Sources/PlayerView.swift:429 `let position = max(dead.startAt, livePosition)` → :460 `startAt: carried.seconds` (NEW since the audit — audit said 'no recovery path'); web app.js:4634-4635 `const at = Number(video.currentTime); if (Number.isFinite(at) && at > candidateResume) candidateResume = at;` in tryNextCandidate (:4628), applied at app.js:4543-4546, plus the mid-film watcher `watchPlaybackFailure` at app.js:4583-4592 (NEW — audit said retryViaProxy restarts from the top). (c) The half the audit says is worth copying everywhere — 'a source that has already played is not a dead link and must not be reported' — is STILL MISSING on web and tvOS. Roku guards it: PlayerScreen.brs:417-419 `if m.started and m.lastPos > 2 ... link NOT reported`. Fire TV is moot: `rg -n 'dead_links' client/app/src/main/java/` hits only DetailActivity.kt:1533/1814/2021, all manual — the player never auto-marks. But web app.js:4639-4640 `const failed = playbackCandidates[candidateIndex]; if (failed && failed.url) markDeadLink(failed.url);` fires unconditionally, and tvos PlayerView.swift:436 `onSourceFailed(candidates[dead.index], reason.rawValue)` → RootView.swift:808-810 `func sourceFailed(...) { ... if !key.isEmpty { DeadLinks.mark(key) } }`, also unconditional. tvOS partly compensates for stalls only (Failover.swift:142-148 `stallAfterPlaying(hasPlayed:)` suppresses .stalled), but an engine error mid-film still reports .dead.

### M58 — Per-title audio/subtitle preferences and a quality dropdown exist only on web; Fire TV instead asks with a dialog on Play

- **Missing on:** Roku, Fire TV, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-web/stream-preferences.js:82`

The audit's headline is now factually wrong in two places. (1) "The Roku and Apple TV offer neither" is FALSE for Roku: components/screens/SettingsScreen.brs:81 `f.title = "Audio language: " + PrefValueLabel(StoreGet("pref_audio", "en"))` and :83 `g.title = "Subtitles: " + PrefValueLabel(StoreGet("pref_sub", "en"))`, with the header comment at :40-43 saying StreamPrefsQuery() sends both on every /stream request; the quality filter is there too at :92 `"Minimum quality: " + MinHeightLabel(f2.minHeight)` and :104 `"English audio only: "`. (2) The audit's own proposed fix — "one settings screen per TV client" — is ALREADY BUILT on all three TVs: firetv MainActivity.kt:255-259 `row("Minimum quality: "...)`, :278 `row("Hide SDH captions: "...)`, :281 `row("English audio only: "...)`; tvos Sources/Sections.swift:659 `ParentalRow(title: "Minimum quality: " + SourcePrefs.minHeightLabel(prefs.minHeight))` and :677 `"English audio only: "`. WHAT IS GENUINELY STILL MISSING: (a) the paired audio/subtitle LANGUAGE preference (web stream-preferences.js:82-83 `selectControl(fields, 'Preferred audio', [['english','English'],['any','Any language']] ...)` / `'Preferred subtitles', [['english'],['any'],['off']]`; Roku has the equivalent pair, firetv and tvos do NOT — grep of client/app/src/main and Sources/ for preferredAudio|preferred subtitle returns nothing); (b) a quality control ON the source list, which is still web-only — index.html:1002 `<select id="quality-select" class="quality-select" hidden aria-label="Stream quality">` driven by app.js:5417 `const qualitySelect = $('#quality-select')` and fillQualitySelect at :5448; Roku DetailsScreen.brs has no quality/audio/subtitle control (grep returns only unrelated `subtitle:` ContentNode fields at :1002 and :1039), tvos Sources/DetailSheet.swift has zero 'quality' hits and Sources/Sidecar.swift:209 only prints `chip(stream.qualityLabel, Ink.accent)` with no filter. (c) firetv still asks after the press: DetailActivity.kt:1564 `setOnClickListener { chooseQualityThenPlay(best) }`.

### M59 — Pressing a cast member does something different on all four clients

- **Missing on:** Fire TV, Apple TV
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:1280`

WEB IS NOW FIXED since the audit was written — the audit says "The web has no cast row to press" and that is no longer true: index.html:1028 `<div id="detail-cast" class="detail-section" hidden></div>`; app.js:3463 `function renderCast(rich)`, called live at app.js:3417; every face is a real control, app.js:3476 `const button = el('button', 'detail-person')` and app.js:3510 `button.addEventListener('click', () => openPerson(member.name, 'cast'));` with openPerson defined at app.js:3649. roku HAS it: components/screens/DetailsScreen.brs:158 `m.cast.ObserveField("rowItemSelected", "onCastChosen")` and :2408 `sub onCastChosen()`; components/MainScene.brs:6270 `sub openPersonPage(name, character, role)`. firetv STILL MISSING: DetailActivity.kt:980-1009 `private fun castCard(c: RichMetaClient.CastMember) = LinearLayout(this).apply { ... }` contains no isFocusable, no isClickable and no setOnClickListener — the isFocusable/isClickable at :1014-1015 and the click at :1054-1056 belong to companyCard(), which starts CompanyActivity. tvos STILL MISSING: Sources/DetailSheet.swift:408 `let onPlayEpisode` block passes onCast through, and Sources/RootView.swift:1279 is still `onCast: { shell.searchFor($0.name) }` while the line directly below at :1280 is `onDirector: { shell.openPerson($0, role: "director") }` — the person screen exists (RootView.swift:492 `func openPerson(_ name: String, role: String)`) and cast still does not use it.

### M61 — Production companies lead somewhere on two clients, are decoration on tvOS, and are absent on web

- **Missing on:** Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-web/app.js:3768 (openCompany + the /meta/company/<id>/catalog route), or /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:1012`

WEB HALF IS NOW FIXED — the audit's "absent on web" claim is stale. web: /Users/markususche/Desktop/blazing-web/app.js:3523 `function renderCompanies(rich)`; :3538 `const button = el('button', 'detail-company');`; :3555 `button.addEventListener('click', () => openCompany(company));`; :3768 `async function openCompany(company)` which fetches `${FLEET_BASE}/meta/company/${id}/catalog` at :3783; index.html:1029 `<div id="detail-companies" class="detail-section" hidden></div>`; index.html:1057 `<dialog id="company-dialog" ...>`. Wired and reached — app.js:3418 `renderCompanies(rich);` inside the rich-meta render path. roku: components/screens/DetailsScreen.brs:2442 `sub onCompanyChosen()`; components/MainScene.brs:6279 `sub openCompanyPage(company as Object)`; :6301-6305 onCompanyChosen -> openCompanyPage(c); :6615 openCompanyPage(w). firetv: client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:1012 `private fun companyCard(company: RichMetaClient.Company)`; :1014-1015 `isFocusable = true` / `isClickable = true`; :1056 `startActivity(CompanyActivity.intent(this@DetailActivity, company))`; :868 `for (company in rich.companies.take(16)) strip.addView(companyCard(company))`. TVOS STILL MISSING: /Users/markususche/Desktop/blazing-tvos/Sources/DetailSheet.swift:1452 `/// A production company. Not focusable: it is a credit, not a destination.`; :1453 `private struct StudioTile: View {` — plain View, no Button, no action, no .focused; :887 `ForEach(companies.prefix(6)) { StudioTile(company: $0) }`. `rg -n "CompanyView|openCompany|companyRoute" Sources/` returns nothing, and no company screen file exists in Sources/.

### M73 — Subtitle timing offset and SDH stripping exist on Fire TV only

- **Missing on:** Roku, Web, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/PlayerActivity.kt:1198-1215 (offset entries) and :1316-1331 (rewriteSubtitles)`

Two behaviours, and only one moved. SDH — now on Fire TV AND web: firetv client/app/src/main/java/com/nyctailblazers/blazingtv/SourceFilters.kt:103 `fun stripSdh(ctx: Context)`, PlayerActivity.kt:1212-1213 the 'Hide SDH captions' entry, :341-342 rewrite on load. web /Users/markususche/Desktop/blazing-web/settings.js:52 `STRIP_SDH_KEY`, :97 `SDH_LABEL` regex, :152-153 hideSdh/setHideSdh, :425 the 'Hide SDH captions' row; applied at /Users/markususche/Desktop/blazing-web/stream-preferences.js:38-42 `hideSdh()`/`isSdh()` and :151 `const wanted = (track) => english(track) && !(hideSdh() && isSdh(track));`. TIMING OFFSET — still Fire TV only: firetv PlayerActivity.kt:1206-1208 `val offset = "%+.1fs".format(subtitleShiftMs / 1000.0)` plus 'Subtitles later'/'Subtitles earlier' calling `nudgeSubtitles(SubtitleFilter.STEP_MS)`, rewritten and restarted at :1282-1331. Grep for subDelay|subtitleDelay|subtitleOffset|SubTitleDelay across roku source/+components/, blazing-web *.js (minus hls.min.js) and blazing-tvos Sources/+Shared/ returns ZERO hits. Roku components/screens/PlayerScreen.brs has no SDH or offset control (only hit for 'offset' is an unrelated resume comment at :167). tvOS Sources/VLCControls.swift now HAS subtitle track selection (:95-97 `subtitleTracks`, :165-177 `selectSubtitles`, :338-359 the Subtitles list) — so the audit's 'no subtitle control at all' cite is stale — but no offset and no SDH row.

### M74 — The Add Profile form asks for a different set of fields on every single client

- **Missing on:** Roku, Web, Apple TV
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/ProfileGateActivity.kt:1459-1580`

Nothing changed; the four forms still diverge. FIRETV (superset, the donor): client/app/src/main/java/com/nyctailblazers/blazingtv/ProfileGateActivity.kt:1459-1480 `showAddProfileForm()` with nameField, `var isKids/maxRating/allowAdult/avatar` and `newPin`, kidsBtn + adultBtn at :1476-1479; createProfile called at :1576. ROKU (name + isKids only): components/MainScene.brs:4815 `d.buttons = ["Create Adult", "Create Kids", "Cancel"]`; :4841 `runProfileTask({ kind: "create", name: name, isKids: (idx = 1) })`; source/lib/ProfilesApi.brs:121 `function ProfileCreate(name as String, isKids as Boolean)` — the two-argument signature the audit named, and :138-139 still hard-codes `avatar = "red"` / `"blue"`. ProfilePatch at source/lib/ProfilesApi.brs:190 does take a fields object, so the widening model is right there in the same file. WEB (name + Kids checkbox only): /Users/markususche/Desktop/blazing-web/profile.js:3545-3563 builds `ui.createProfile` from createTop + ui.createName + kidsRow + ui.createSubmit — no icon grid, no PIN field, no rating row; profile.js:2479 `const fields = newProfileFields(!!ui.createKids.checked, 'teen', false);` hard-codes the cap and adult=false; POST body at :2486-2490 is {name,isKids,maxRating,allowAdult}. Web DOES now have both halves of the missing parts elsewhere — ICON_CHOICES at profile.js:49 rendered at :1031-1038, and the PIN pad at :1417-1466 — they are just not on the create form. TVOS (name + kids + icon + PIN, no rating cap, no adult): Sources/FleetAPI.swift:1192-1197 `createProfile(name:avatar:isKids:pin:)` — four parameters, no maxRating and no allowAdult; called from Sources/RootView.swift:2301-2306.

### M89 — The picker's pencil is behind the profile's PIN on the three TV clients and wide open on web

- **Missing on:** Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/ProfileGateActivity.kt:1027 and 1221-1238`

HALF OF THIS LANDED SINCE 11 SEP. The 'browser pencil does less — picture only, no name' half is now WRONG: /Users/markususche/Desktop/blazing-web/profile.js:965-970 `pencil.addEventListener('click', (event) => { event.stopPropagation(); showParental(profile); });` — no longer showIconPicker. The sheet it opens (profile.js:1145 `function showParental(profile) {`) carries Kids toggle (:1192), rating ladder (:1206-1228), Allow adult (:1237-1241), Profile picture (:1244 `row('Profile picture', () => showIconPicker(profile), ...)`), Set/Change PIN (:1250-1258), Remove PIN (:1261-1263), Delete (:1270-1285). | THE PIN GATE ITSELF IS STILL OPEN ON WEB. showParental at profile.js:1145-1147 `if (state.busy || !profile) return; state.parentalProfile = profile; state.deleteArmed = false;` — no hasPin test, no PIN pad, for a PIN-protected profile. The avatar is still explicitly unguarded: profile.js:1242-1244 `// The picture ... No PIN: the fleet needs no unlock token for an avatar.` / `row('Profile picture', () => showIconPicker(profile), { id: 'bp-parental-icon' });`. Only LOOSENING passes through requireUnlockThen (:1222 raise cap, :1238 allow adult on, :1254 change PIN, :1262 remove PIN, :1284 delete) — so tightening, Kids-on and the picture are still changeable by anyone at the keyboard. | The three TVs still gate the pencil itself: firetv ProfileGateActivity.kt:1027 `setOnClickListener { requirePinThen(p, PinIntent.EDIT) }` with :1221 `private fun requirePinThen(p: ProfileClient.Profile, intent: PinIntent) {` and :1238 `PinIntent.EDIT -> showEditProfileForm(p, token)`; roku "/Users/markususche/Desktop/roku channels/components/MainScene.brs":4776-4778 `if c.action = "editprofile"` -> `beginProfileEdit(c.profile)` and :5131-5143 `sub beginProfileEdit(p as Object)` / `if p.hasPin = true` / `m.pinEditFlow = true` / `navTo("pinpad")`; tvos /Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:1743-1745 `func requestEdit(for profile: FleetProfile) { if profile.hasPin { requestPIN(for: profile, purpose: .edit) }`, wired at :1776 `onEdit: { model.requestEdit(for: $0) },`.

### M90 — The roadmap watch-order control works on the Roku, is inert on Fire TV and tvOS, and those two label the order wrongly

- **Missing on:** Fire TV, Apple TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/RoadmapsScreen.brs:749-830 (BuildOrders / OrderedChapters / Renumbered); the web port at /Users/markususche/Desktop/blazing-web/roadmaps.js:479-521,697-705,872 is a shorter model`

WEB IS NO LONGER MISSING — the audit's 'index.html:500-517 has no buttons' is stale. /Users/markususche/Desktop/blazing-web/index.html:587 `<button id="roadmap-order" class="roadmap-order" type="button" hidden>Saga order</button>`; roadmaps.js:479-482 `function orderLabel(mode) { if (mode === 'release') return 'Release order'; if (mode === 'films') return 'Films first'; return 'Saga order'; }`; :501-521 `function orderedChapters(chapters, mode)` with byDate/dateKey (`:488 /** Release date, then year, then nothing — the Roku's DateKey, verbatim. */`); :697-705 `const modes = orders(gated.chapters); ... order.textContent = orderLabel(mode); order.setAttribute('aria-label', \`Order: ${orderLabel(mode)}. Press to change.\`); ... order.disabled = modes.length < 2;`; wired live at :872 `order.addEventListener('click', () => {`. | roku still the reference: "/Users/markususche/Desktop/roku channels/components/screens/RoadmapsScreen.brs":658 `m.orderPill.text = OrderLabel(OrderMode())`, :749 `sub BuildOrders()`, :783-786 `function OrderLabel(mode as String)` / `if mode = "release" then return "Release order"` / `return "Saga order"`, :793 `function Renumbered(...)`, :809 `function OrderedChapters()`, :912 `m.chapters = OrderedChapters()`. | firetv STILL INERT AND STILL MISLABELLED: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/RoadmapsActivity.kt:401 `private fun orderIndicator(): View {`, :403 `isFocusable = false`, :411 `text = "Release order"` — a fixed string over the fleet's chapter order; the doc comment at :392-394 still argues the toggle is absent on purpose. | tvos STILL INERT AND STILL MISLABELLED: /Users/markususche/Desktop/blazing-tvos/Sources/Roadmaps.swift:619 `/// The order toggle, with one axis and no focus.`, :621-623 the same argument, :630 `Text("Release order")` — a plain Text, not a Button, drawn over chapters rendered in the fleet's order (:253 `/// The chapters as numbered sections, in the fleet's order, empty ones ...`).

### M95 — The web client does not apply the profile cap to the fleet catalogue rows in search results

- **Missing on:** Web
- **Fix size:** medium
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/RatingGate.swift:287-307 (resolve-then-filter), called at Sources/Sections.swift:477`

Apple TV has been FIXED since the audit; web has not. tvOS now resolves before filtering: Sources/Sections.swift:477 "let found = await RatingGate.visible(cap: cap, items: unique) { ($0.type ?? \"movie\", $0.id) }" inside SearchView.run() (struct SearchView at Sources/Sections.swift:360), with the AI box gated identically at :490; RatingGate.visible is a real per-title fleet lookup at Sources/RatingGate.swift:287-307 — "let tier = await RatingCatalogue.shared.tier(type: pair.type, id: pair.id)" then "RatingRules.allows(cap: cap, tier: tier)", and "guard cap != nil else { return [] }" at :292. WEB is unchanged: app.js:1706-1711 "const catalogRows = []; for (const settled of [movies, series]) { for (const raw of searchMetasOf(settled)) { const meta = safeMeta(raw); if (meta) catalogRows.push([raw, meta]); } }" — still `if (meta)` only, while the Emby block at app.js:1704 and the site block at app.js:1729 both add "&& ratingAllowed(meta.contentRating)". The comment explaining why at app.js:1694-1700 is still in place. The card-then-vanish path is still live at app.js:2440 and :2447 "if (!ratingAllowed(meta.contentRating)) { stop(); card.remove(); return; }". Donors unchanged: roku source/lib/RatingApi.brs:85 "function RatingFilterItems(s as Object, items as Object) as Object" called from components/tasks/AddonTask.brs:676; firetv RatingClient.kt:133 "suspend fun visibleTo(cap: String?, metas: List<AddonClient.Meta>): List<AddonClient.Meta>" called at SearchActivity.kt:171, :248, :283, :299.

### M100 — Two source filters exist only on Fire TV: "Skip 3D releases" and "Hide SDH captions"

- **Missing on:** Roku, Apple TV
- **Fix size:** medium
- **Copy from:** `3D: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/SourceFilters.kt:360 (title regex) + MainActivity.kt:275. SDH: /Users/markususche/Desktop/blazing-web/settings.js:97,152-153,308,425 is the smallest complete model for tvOS.`

Web has since gained BOTH and tvOS has gained ONE; Roku still has neither. FIRE TV (donor, unchanged): MainActivity.kt:275 'row("Skip 3D releases: ${onOff(f.no3d)}")' and :278 'row("Hide SDH captions: ${onOff(SourceFilters.stripSdh(this))}")'. WEB now has both: settings.js:423 'row(root, `Skip 3D releases: ${onOff(f.no3d)}`, () => saveFilters({ no3d: !f.no3d })).id = \'settings-no-3d\'' and settings.js:425 'row(root, `Hide SDH captions: ${onOff(hideSdh())}`, () => setHideSdh(!hideSdh())).id = \'settings-hide-sdh\'', backed by settings.js:74 "no3d: false,", :115 "no3d: source.no3d === true,", :232 "if (f.no3d && THREE_D.test(blob)) return '3d';", :52 "const STRIP_SDH_KEY = 'blazing-hide-sdh-v1';", :97 SDH_LABEL regex, :152-153 hideSdh/setHideSdh, :308 isSdhLabel — and settings.js is loaded at index.html:1155 '<script src="./settings.js" defer></script>'. APPLE TV has 3D only: Sources/Sections.swift:680-682 'ParentalRow(title: "Skip 3D releases: " + SourcePrefs.onOff(prefs.no3D)) { change { $0.no3D.toggle() } }' with storage at Sources/SourcePrefs.swift:33 keyNo3D, :57 "var no3D = false", :75, :87 and enforcement at :176 'if prefs.no3D, matches(threeD, in: blob) { return "3d" }' — but grep for sdh/hearing/[cc] across Sources/ returns ZERO hits, so the SDH filter does not exist on tvOS. ROKU has neither: source/lib/SourceFilter.brs:26-37 SourceFilterDefaults() still returns exactly seven keys (minSeeders, maxSizeGb, minHeight, noHevc, noAv1, noUncached, noForeign) and SourceFilterLoad() at :39-60 parses the same seven; grep -i for no3d/stripsdh/sdh and for '3D' across source/ and components/ returns nothing. The renumbering hazard the fix note warns about is still real: components/screens/SettingsScreen.brs:124-132 says onButton "dispatches on HARD-CODED INDICES — 0..17, written out one by one".

### M101 — Undo for sources marked "wrong film" exists only on Roku

- **Missing on:** Fire TV
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/screens/SettingsScreen.brs:68,77,384,388 (the pair); for the simpler one-row version, /Users/markususche/Desktop/blazing-tvos/Sources/Sections.swift:687-688`

roku (HAS, pair intact): /Users/markususche/Desktop/roku channels/components/screens/SettingsScreen.brs:68 `i2.title = "Undo sources marked 'wrong film' in the last hour"`, :77 `i3.title = "Undo EVERY source I marked 'wrong film'"`, wired at :384 `n = UnmarkRecentBadLinks(3600)` and :388 `n = UnmarkAllBadLinks()`. tvos (NOW HAS — audit stale): /Users/markususche/Desktop/blazing-tvos/Sources/SourcePrefs.swift:240 `enum DeadLinks` with :256 `static func forget(...)`; surfaced in Settings at Sources/Sections.swift:687 `ParentalRow(title: "Forget \(deadCount) source... I marked as dead")` → :688 `DeadLinks.forget()`, count seeded at :563 and refreshed at :634. It is the clear-all half only; there is no 1-hour variant and no TTL (DeadLinks stores a bare stringArray, SourcePrefs.swift:242-244). firetv (STILL NONE): /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/DetailActivity.kt:1813-1815 `private fun markStreamDead(s)` → `prefs.edit().putBoolean(s.url, true).apply()` permanent, :1817 `ranked = ranked.filter { !prefs.getBoolean(it.url, false) }` still removes the row rather than demoting it; re-read at :1533 and :2021; grep of client/app/src/main/java for dead_links returns only those four sites and no unmark/forget path. web (audit's own evidence already refuted the claim, still true): app.js:222 `DEAD_LINK_TTL_SECONDS = 86400`, :334 `function unmarkDeadLink(key)` and :344 `function clearDeadLinks()` both still defined with zero callers anywhere in *.js/*.html.

### M16 — The picker headline and wordmark differ on every client

- **Missing on:** Web, Roku
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/ProfileGateActivity.kt:822`

AGREED PAIR PRESENT ON TWO — tvos: /Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:1931-1934 `Image(systemName: "play.fill")` + `Text("BLAZING STREAM")`, :1940 `Text("Choose a Profile")` (audit cited :1509/:1518 — file moved, strings unchanged). firetv: /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/ProfileGateActivity.kt:822 `text = "▶  BLAZING STREAM"`, :830 `text = "Choose a Profile"` (also :356 for the second gate). | STILL DIVERGENT ON TWO — web: /Users/markususche/Desktop/blazing-web/profile.js:1722 `ui.kicker.textContent = 'Blazing Stream';` and :1723 `ui.heading.textContent = 'Who is watching?';` — title case AND the other headline; the second picker at profile.js:3375 also builds `element('h2', 'bp-heading', 'Who is watching?')`. roku: /Users/markususche/Desktop/roku channels/components/screens/ProfileGate.xml:81 `<Label id="kicker" text="BLAZING"` — still one word, not 'BLAZING STREAM', and still no play mark; the title is correct at :86 `text="Choose a Profile"`. The Roku font reason the audit cites is still on record (ProfileGate.brs:571-580: 'there is no colour emoji font on the box at all... so the twenty ship as BUNDLED PNGs'), so the play mark must be a Poster there.

### M18 — The same chapter list carries a different heading and different facts on each client

- **Missing on:** Roku, Fire TV, Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/ReadingView.swift:339`

The audit header ('Missing: Web') is mislabelled — web DOES give a count; the real outlier is Roku, which the audit's own Fix text names. FOUR DIFFERENT HEADINGS, LIVE: roku: /Users/markususche/Desktop/roku channels/components/screens/MangaChaptersScreen.xml:86 `<Label text="English chapters"` — still a hardcoded string, no count, no source credit. tvos (THE MODEL, count + via): /Users/markususche/Desktop/blazing-tvos/Sources/ReadingView.swift:339 `"\(model.chapters.count) chapters" + (model.via.isEmpty ? "" : "   ·   via \(model.via)")`, with `@Published var via` at ReadingView.swift:108 filled from `r.via` at :167. firetv (count, NO via): /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/MangaActivity.kt:397 `heading("$readable chapter${if (readable == 1) "" else "s"}", 20f, ...)`. web (readable-of-total, NO via in the dialog): /Users/markususche/Desktop/blazing-web/manga.js:346-348 `${readableCount} readable chapter${...} of ${list.length}` inside openChapters (manga.js:305-348), with the source credit only on the card, not the dialog.

### M19 — The same remote press does different things: seek step and the key that leaves

- **Missing on:** Fire TV, Roku, Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/PlayerView.swift:233`

THE SEEK STEP IS STILL UNALIGNED — only tvOS sets one. tvos (5s, as chosen): /Users/markususche/Desktop/blazing-tvos/Sources/PlayerView.swift:233-234 `case .left:  vlc.skip(-5)` / `case .right: vlc.skip(5)` inside .onMoveCommand; the reason is on record at Sources/VLCControls.swift:71-74 ('FIVE seconds a press — he asked for 5 rather than 10 after trying it'), implementation at VLCControls.swift:75-82. roku (platform bar, unchanged): components/screens/PlayerScreen.xml:70 `<Video id="video" width="1920" height="1080" translation="[0,0]" />` with no enableUI — the note at :81 still says so, so the stock trickplay bar owns LEFT/RIGHT. Voice seek still defaults to 30s: components/screens/PlayerScreen.brs:941-943 `secs = 30`, :951 `seekBy(30)`, :953 `seekBy(-30)` — still the only client with a spoken transport. firetv (platform bar, defaults): client/app/src/main/java/com/nyctailblazers/blazingtv/PlayerActivity.kt:174 `useController = true`; `grep -rn 'SeekBackIncrement|SeekForwardIncrement|setSeekParameters' client/app/src/main/java/` returns NOTHING, so ExoPlayer's asymmetric defaults (5s back / 15s forward) still stand — the one place the audit's fix could land and hasn't. web (browser controls): index.html:1081 `<video id="video" class="video" controls playsinline ...>`; dpad.js:1-40 uses the arrow keys to move focus between focusables only — no currentTime arithmetic on arrow keys anywhere.

### M20 — The same resume position is worded three different ways

- **Missing on:** Web, Roku
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/HomeView.swift:1093`

HAS THE COMBINED STRING — tvos: /Users/markususche/Desktop/blazing-tvos/Sources/HomeView.swift:1093-1094 `guard percent > 0 else { return "CONTINUE WATCHING  ·  not started yet" }` / `return "CONTINUE WATCHING  ·  \(percent)% watched"`. firetv: client/app/src/main/java/com/nyctailblazers/blazingtv/HeroCardPresenter.kt:433-441 `eyebrowFor` returns `"CONTINUE WATCHING  ·  $pct% watched"` or plain `"CONTINUE WATCHING"`, set at :404. | STILL MISSING — web: /Users/markususche/Desktop/blazing-web/app.js:835 `$('#home-hero-eyebrow').textContent = (opts && opts.eyebrow) || 'Featured';` — the percent arrives at app.js:847 `const percent = opts && typeof opts.percent === 'number' ? opts.percent : null;` and is used ONLY for the bar; the caller already passes it at app.js:5373-5376 `seedHomeHero(first.meta, { priority: 2, eyebrow: 'Continue watching', percent: first.percent })`, so the number is in hand and thrown away. The markup default is still the bare string at index.html:341 `<p class="eyebrow" id="home-hero-eyebrow">Continue watching</p>`. roku: components/screens/HomeScreen.brs:1794 `m.heroApproval.text = "CONTINUE WATCHING"` and :1797 `m.heroMeta.text = ResumeLabel(item)` — two separate labels, and ResumeLabel at :2184-2197 still returns 'N% watched' only when p >= 1, else 'not started yet' (secs < 15) or 'resumes at Nm Ns'. That sub-1% fallback is the deliberate improvement the audit says to port outward, not delete.

### M23 — The two edit keys on the PIN pad are labelled differently on each client, and only Roku has a shortcut key

- **Missing on:** Roku, Fire TV, Web
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:2683`

FOUR VOCABULARIES, ALL UNCHANGED — roku: /Users/markususche/Desktop/roku channels/components/screens/PinPad.brs:273 `["DELETE", "0", "CLEAR"]` as the fourth row, handled at :196 `if label = "DELETE"` and :202 `if label = "CLEAR"`; the remote shortcut still exists at :476 `pressKey("DELETE")` on the * key. firetv: client/app/src/main/java/com/nyctailblazers/blazingtv/PinActivity.kt:95 `val labels = listOf("1",...,"9", "⌫", "0", "×")`, handled at :144-145 — and `grep -n 'onKeyDown|KEYCODE_DEL|KeyEvent' PinActivity.kt` returns NOTHING, so the hardware-shortcut half of the fix is also untouched. tvos (THE PROPOSED MODEL): /Users/markususche/Desktop/blazing-tvos/Sources/RootView.swift:2683 `private let keys = ["1",...,"9", "⌫", "0", "Clear"]`, handled at :2733 and :2794, with accessibility labels at :2783. web: /Users/markususche/Desktop/blazing-web/profile.js:3655 `ui.clear = element('button', 'bp-action', 'Clear');` and :3663 `ui.delete = element('button', 'bp-action', 'Delete');` — still two .bp-action buttons outside the digit grid (styling at profile.js:493-495).

### M24 — The web caps the saved list at 100 entries; nobody else caps a saved list

- **Missing on:** Web
- **Fix size:** trivial
- **Copy from:** `/Users/markususche/Desktop/roku channels/source/lib/Fleet.brs:300`

THE NAMED BUG IS GONE, A SMALLER ONE REMAINS. The audit's `app.js:683 .map(safeMeta).filter(Boolean).slice(0, 100)` inside readList no longer exists — `grep -rn 'slice(0, 100)|slice(0,100)' /Users/markususche/Desktop/blazing-web/*.js` hits only media-library.js:120 and stream-preferences.js:16, neither of which is a saved list; there is no readList/persistList in app.js at all, and app.js:705-708 records why: 'NO myList. The saved list is not this object's to hold any more: it is the fleet's, cached by lists.js'. The cap moved and doubled: /Users/markususche/Desktop/blazing-web/lists.js:49 `const MAX_ITEMS = 200;` with the comment at :47-48 naming the old one ('The old readList() capped at 100 for the same reason'), applied at lists.js:184-185 `items.sort((a, b) => (b.addedAt || '').localeCompare(a.addedAt || '')); return items.slice(0, MAX_ITEMS);` and again at :391. The DROP-THE-OLDEST-SILENTLY fault the item describes is fixed — lists.js:161-174 records that capping before the sort kept the 200 oldest and threw the newest away, and the sort now comes first — but a web-only cap still exists and still tells the viewer nothing. STILL UNCAPPED ELSEWHERE: roku source/lib/Fleet.brs:282-289 FleetIds and :300-312 FleetEntries walk every row with no truncation; firetv ListsClient.kt:88 parseSnapshot de-duplicates only. THE AUDIT'S LAST EVIDENCE LINE IS NOW STALE: 'tvos: no saved list exists' is false — /Users/markususche/Desktop/blazing-tvos/Sources/SavedLists.swift exists (:3 'Watchlist, Collection and Watched', :28 `case watchlist, collection, watched`, routes at :221-230) and carries no cap.

### M25 — The web top bar carries a "Books & Audio" chip that no television client has, and drops Settings

- **Missing on:** Web
- **Fix size:** small
- **Copy from:** `/Users/markususche/Desktop/roku channels/components/MainScene.brs:7821 (Settings last on the bar) and /Users/markususche/Desktop/roku channels/components/MainScene.brs:1855 (Books as a manga-gated drawer row rather than a bar chip)`

BOOKS CHIP — still web-only. web: index.html:131 `<button type="button" data-view="books">Books &amp; Audio</button>` sits fourth in <nav class="topnav"> (index.html:127-154), pushing Roadmaps to :132 and Library to :133, exactly as the audit described. roku: components/MainScene.brs:7801-7821 is the whole navOrder build — browse/browsetv/animehub/roadmaps/mylists/livetv/youtube/games/adult/search/settings; no books push anywhere. Books is DRAWER-ONLY and manga-gated at components/MainScene.brs:1855 `if manga then addDrawerRow(c, "Books", { kind: "screen", name: "books" })`. firetv: NavBarView.kt:154-169 entries() adds MOVIES, SHOWS, anime, ROADMAPS, LIBRARY(gated), LIVE, youtube, GAMES, SEARCH, SETTINGS — no Books/Reading chip; NavBarView.kt:47-50 states it outright: "READING stays in this enum ... but it is a DRAWER row now and not a chip ... The bar does not carry it — DESIGN.md dropped it" and DrawerView.kt:183 is the only caller. tvos: Sources/NavBar.swift:131 `[.movies, .shows, .anime, .roadmaps, .library, .live, .youtube, .games, .adult, .search, .settings]` — no books case; books live inside the reading hub (Sources/ReadingView.swift:1042 BooksShelfView). SETTINGS — TV side unchanged and correct, web bar still lacks the chip, but the audit's stated reason is now STALE. TVs carry Settings last: roku components/MainScene.brs:7821 `m.navOrder.Push("settings") : labels.Push("Settings")`; firetv NavBarView.kt:169 `dest(Dest.SETTINGS)` is the last entry; tvos Sources/NavBar.swift:131 ends `.search, .settings`. web topnav index.html:128-153 still has no Settings chip. HOWEVER the audit's fix text says "the web currently has no settings surface at all" and that is now FALSE: B32 landed a full settings screen — index.html:955 `<section id="settings-view" class="view" hidden>` with #settings-host at :961, the router branch at app.js:2064 `if (settingsView) settingsView.hidden = route !== 'settings'` and app.js:2092 `if (route === 'settings') window.BlazingSettings && window.BlazingSettings.mount();`, settings.js is 749 lines and is loaded at index.html:1155, and the door is a DRAWER button at index.html:250 `<button type="button" data-view="settings">Settings</button>` whose own comment (index.html:245-249) argues deliberately that Settings "is not in the canonical eleven, so it is not a .topnav chip". So the gap that remains is bar POSITION only, not a missing screen.

## CLOSED — 8 (survived the adversarial pass)

### M47 — No remove control on the web list screen, and no confirmation before removing anywhere on the web

- **Missing on:** —
- **Fix size:** none

Fixed since the audit, and built to the exact pattern the audit's fix specified. /Users/markususche/Desktop/blazing-web/app.js:1793 `function renderLibrary()` now draws through :1835 `results.appendChild(buildListSection(spec, visible))` → :1866 `grid.append(...metas.map((meta) => buildListItem(spec, meta)))` → :1881-1887 `function buildListItem(spec, meta)`, whose comment names ListsActivity.addItemRow as the donor and which appends `buildCard(meta)` plus an actions div. The per-row control is app.js:1889-1896 `function drawListRemove(actions, spec, meta)` — :1892 `remove.textContent = 'Remove';`, :1893 the aria-label, :1894 the click handler. The confirm is app.js:1908-1924 `function drawListRemoveConfirm(...)`: :1910 `Remove ${meta.name} from your ${spec.title}?`, :1911-1915 a real 'Keep' button, :1916-1921 the confirming 'Remove', and :1923 `keep.focus();` with the comment 'The safe choice gets the focus, the same as ListsActivity's dialog.' It is drawn in-row rather than as window.confirm() precisely so a webOS/Tizen remote can answer it (comment at :1902-1906). The write goes to the fleet, not localStorage: app.js:1930 `const removed = await window.BlazingLists.remove(spec.list, meta.id);`. Fire TV donor still at ListsActivity.kt:281-311.

### M71 — Source filters (7 rows) exist only on the two TV clients

- **Missing on:** —
- **Fix size:** none

Landed on both missing clients since the audit. WEB: /Users/markususche/Desktop/blazing-web/settings.js:57-59 `HEIGHT_LADDER=[0,720,1080,2160] / SEEDER_LADDER=[0,5,10,20,50] / SIZE_LADDER=[0,2,5,10,20]` (comment: 'firetv SourceFilters.kt:41-43, value for value'); settings.js:67-75 DEFAULTS holds minHeight/minSeeders/maxSizeGb/noUncached/noHevc/noAv1/no3d/noForeign; settings.js:411-427 renders all eight rows; settings.js:227-233 the reject predicate; settings.js:241-242 `active`; settings.js:251-255 the summary sentence. It is REACHED, not dead: /Users/markususche/Desktop/blazing-web/app.js:4076 `const wanted = window.BlazingSettings.filterStreams(streams);` and app.js:5739-5741 `streams = window.BlazingSettings.filterInfos(ranked.streams)...` / `filterStreams(streams).streams` — and app.js:4079-4082 prints the 'filters dropped everything' sentence the audit asked for. Fed BESIDE caps, not replacing them (app.js:5735-5739 probes caps.js first, then filters). TVOS: /Users/markususche/Desktop/blazing-tvos/Sources/SourcePrefs.swift:42-44 same three ladders, :46-62 `struct Prefs` with the same eight values and `active`, :123-125 summary, :140-172 `reject`. Rows at Sources/Sections.swift:659-681 (Minimum quality / Minimum seeders / Maximum size / Instant sources only / Skip HEVC / Skip AV1 / English audio only / Skip 3D). REACHED: Sources/StreamRanker.swift:105 `prefs: SourcePrefs.Prefs = SourcePrefs.load()` and :170-173 `let rejected = hardware.isEmpty ? SourcePrefs.reject(...) : hardware`, called from Sources/RootView.swift:373 `StreamRanker.rank(streams, caps: caps, title: title)`; Sections.swift:703-709 re-ranks the sidecar on a settings change. Donors unchanged: roku components/screens/SettingsScreen.brs:92-104; firetv client/app/src/main/java/com/nyctailblazers/blazingtv/SourceFilters.kt:41-43,45-60.

### M72 — Stall watchdog: three different timeouts, and none on Apple TV

- **Missing on:** —
- **Fix size:** none

tvOS now has a watchdog on BOTH engines. AVPlayer branch: /Users/markususche/Desktop/blazing-tvos/Sources/PlayerView.swift:654 `@State private var watch = SourceWatchdog()`, armed at :682 `armWatchdog()` inside `.onAppear`, defined :822-836 (`watch.start(onDead: onFailure)`, `.failed` -> `.dead`, otherwise `.at(position:wantsToPlay:)`), timer class at :862-895 (1s `Timer.scheduledTimer`, one-shot via `fired`). VLC branch: Sources/PlayerView.swift:965 `private var monitor = Failover.StallMonitor()` fed by the same 1s poll. The number: Sources/Failover.swift:42 `static let firstFrameTimeout = 30` with the comment 'which is Fire TV's stallTimeoutMs'; Failover.swift:133-138 `let limit = hasPlayed ? rebufferTimeout : firstFrameTimeout` and :149 `stallAfterPlaying(hasPlayed:) { hasPlayed }` so a mid-film rebuffer is not a failover. Expiry advances the source — `onDead: onFailure`. Other three still in place: roku components/screens/PlayerScreen.brs:269 `m.stallTimer.duration = 40`; web /Users/markususche/Desktop/blazing-web/app.js:17 `PLAYER_STALL_TIMEOUT = 25000` armed at app.js:4575; firetv PlayerActivity.kt:776 `stallHandler.postDelayed(task, stallTimeoutMs(isLive, currentWasReady))` with the table at :1097. RESIDUAL (not the gap this item named): the fleet still runs four numbers — Roku 40s, web 25s, Fire TV 30s, tvOS 30s — so the 'pick one number' half of the fix note is unfinished.

### M91 — The same nav word opens two completely different screens

- **Missing on:** —
- **Fix size:** none

Fixed on both blamed clients since the audit. ROKU: "components/MainScene.brs:7812  m.navOrder.Push("mylists") : labels.Push("Library")" with the comment at :7805-7811 — "'Library' opens the three SAVED LISTS … The debrid browser is still one press away, in the drawer, under the name it has always carried there: 'My Library'"; drawer split is live at components/MainScene.brs:1858 (addDrawerRow(c, "Library", { kind: "screen", name: "mylists" })) vs :1868 (addDrawerRow(c, "My Library", { kind: "screen", name: "library" })). APPLE TV: Sources/NavBar.swift:159 "case .library: return \"Library\"" and :160 "case .debrid: return \"My Library\""; :51 "var barTitle: String { title }" so bar, drawer and screen now speak one word — the old 'Watchlist' bar label is gone and :44-49 records why. Routes agree: Sources/RootView.swift:1206 "case .library: SavedListsView(shell: shell)" and :1207 "case .debrid: LibraryView(shell: shell)"; the debrid screen's own header now reads "My Library" at Sources/LiveView.swift:745. Donors unchanged: firetv HomeActivity.kt:213-ish Dest.LIBRARY -> ListsActivity; web index.html:133,186 data-view="library" -> app.js:2004 libraryView. tvOS three-names-one-screen is gone: Sources/SavedListsView.swift:19 struct SavedListsView is the saved-lists screen and is what .library opens.

### M99 — Three list buttons on the TV clients, one on the web, zero on Apple TV

- **Missing on:** —
- **Fix size:** none

Both blamed clients now have all three. WEB: index.html:997 '<button id="detail-save" class="secondary-button" type="button" data-list="watchlist">+ Watchlist</button>', :998 '<button id="detail-collection" … data-list="collection">+ Collection</button>', :999 '<button id="detail-watched" … data-list="watched">Mark watched</button>'; driven by the spec table at app.js:32-36 — :33 "{ list: 'watchlist', button: '#detail-save', … out: '+ Watchlist', in: '✓ In watchlist' }", :34 collection, :35 watched — bound at app.js:6497-6500 "for (const spec of LIST_SPECS) { const button = $(spec.button); if (button) button.addEventListener('click', () => state.selected && toggleList(spec.list, state.selected, button)); }" and labelled at app.js:1158-1166 updateSaveLabels(). APPLE TV: Sources/DetailSheet.swift:807-817 "HStack(spacing: 14) { ForEach(SavedList.allCases) { list in SheetButton(title: listTitle(list), icon: listIcon(list), primary: false, done: listState[list] == true) { toggleList(list) } .focused($focused, equals: .list(list)) } }", with listTitle at :982-985, toggleList at :1017-1036 and the fleet call at :1026 "let nowIn = await savedLists.toggle(list, item: item, profileID: profileID)"; focus case exists at Sources/DetailSheet.swift:437 "case list(SavedList)". Donors unchanged: roku components/screens/DetailsScreen.brs:1423-1425 specs.Push for watchlist/collection/watched; firetv DetailActivity.kt:784-786 Triple(ListsClient.WATCHLIST/COLLECTION/WATCHED, …).

### M103 — Watch Party reaches Home three different ways, and not at all on the Fire TV

- **Missing on:** —
- **Fix size:** none

firetv (NOW HAS — audit stale, landed in commit c3b0938 "Watch Party on the Fire TV, both halves"): /Users/markususche/Desktop/firetv/client/app/src/main/java/com/nyctailblazers/blazingtv/DrawerView.kt:153 `addRow("Watch Party") { open(WatchPartyActivity.intent(context)) }`; the target is WatchPartyActivity.kt:39 `class WatchPartyActivity : AppCompatActivity(), PartySession.Listener` with its factory at :333 `fun intent(ctx: Context)`; registered at client/app/src/main/AndroidManifest.xml:109 `<activity android:name=".WatchPartyActivity" android:exported="false"`. The protocol is a full local stack now: PartyApi.kt, PartySession.kt, PartySignal.kt, PartyRules.kt, PartyWebSocket.kt, PartyOverlayView.kt, PartyViews.kt. roku (HAS): components/screens/HomeScreen.brs:533 `title: "Watch Party"`, :537 `name: "Join Watch Party: " + m.partyActive.title`, OK at :2408 `m.top.chosen = { action: "joinparty", code: item.code }`. tvos (HAS): Sources/Drawer.swift:214-215 `DrawerRow(label: "Watch Party", icon: "person.2.fill") { shell.openParty() }`. web (HAS): watch-party.js:1268-1270 builds the 'Party' label and `topbar.insertBefore(ui.launch, searchButton)`. The secondary point — that the placement differs on each client — is a design decision for Markus, not a missing behaviour.

### M9 — Playback error text: a coloured on-screen strip, a Toast, an inline message, or silence

- **Missing on:** —
- **Fix size:** none

tvos NOW HAS IT — the gap the audit named is filled. /Users/markususche/Desktop/blazing-tvos/Sources/PlayerView.swift:151 `@State private var notice: String?`; :299-310 `// THE FAILOVER TOAST — B10 … if let notice { VStack { RatingNoticeBanner(text: notice) … } }`; driven from `sourceFailed` at :445-446 `show(Failover.exhausted(tried: tried))` and :458 `show(Failover.advancing(reason, next: next, total: candidates.count))`; the 6-second banner helper is `private func show(_ text: String)` at :557-566. Copy lives in Sources/Failover.swift:84-86 `"\(reason.rawValue) - trying \(next + 1) of \(total)…"` and :94-97 `"Tried \(tried) source… and none of them started. Pick one from the list."` — the agreed wording. | roku components/screens/PlayerScreen.brs:817 `sub showError(text as String)`, called e.g. at :1001. | firetv PlayerActivity.kt:645 `Toast.makeText(this, "Couldn't play this — every source failed.", …)` and :741 `"$reason Trying backup ${next + 1} of ${candidates.size}…"`. | web index.html:1084 `<p id="player-msg" class="player-msg" hidden></p>`, painted at app.js:4397-4403 (`playerMsg.hidden = (kind !== 'error' && kind !== 'loading') || !message; playerMsg.textContent = message …`).

### M28 — Wording: the two TV clients name the STATE, the web names the ACTION, and the screen copy disagrees

- **Missing on:** —
- **Fix size:** none

FIXED ON WEB, and the audit's cited lines no longer exist. web: app.js:32-36 LIST_SPECS is now state-worded — `{ list: 'watchlist', button: '#detail-save', title: 'Watchlist', description: 'Titles you plan to watch', out: '+ Watchlist', in: '✓ In watchlist' }` (app.js:33), collection at :34 `out: '+ Collection', in: '✓ In collection'`, watched at :35 `out: 'Mark watched', in: '✓ Watched'`; the comment at app.js:25-28 names the rule and the tick divergence explicitly ("The Roku carries the same pair of labels without the tick glyph, because Barlow on a Roku draws U+2713 as a missing-character box"). The old 'Remove from list' / 'My list' pair the audit quoted at app.js:1123 is gone — grep for "Remove from list" over app.js returns nothing. firetv: DetailActivity.kt:775-778 states the rule ("each showing the state it is IN, not the action — a button that reads '+ Watchlist' after you added it is the single most common way this kind of control lies to people") and :784-786 the labels `Triple(ListsClient.WATCHLIST, "+  Watchlist", "✓  In watchlist")`, `Triple(ListsClient.COLLECTION, "+  Collection", "✓  In collection")`, `Triple(ListsClient.WATCHED, "Mark watched", "✓  Watched")`. roku: components/screens/DetailsScreen.brs:1423-1425 `specs.Push({ list: "watchlist", inLabel: "In watchlist", outLabel: "+ Watchlist" })` and the two siblings. tvos HAS IT NOW TOO, which the audit's footnote said it did not: Sources/DetailSheet.swift:977-985 `/// THE LABEL SHOWS THE STATE IT IS IN, NOT THE ACTION` and `return listState[list] == true ? list.inLabel : list.addLabel`, with the strings at Sources/SavedLists.swift:57-71 (addLabel Watchlist/Collection/Mark watched, inLabel In watchlist/In collection/Watched). SCREEN COPY NOW MATCHES ListsActivity: web index.html:426 `<h1>Library</h1>` (not 'My list') and :427 `<p id="library-subtitle">Saved titles</p>` vs firetv ListsActivity.kt:51 `text = "Library"` and :55-56 `ProfileClient.activeProfile?.name?.let { "$it's saved titles" } ?: "Saved titles"` — web app.js:1810 `subtitle.textContent = name ? `${name}’s saved titles` : 'Saved titles'` is that line word for word; web app.js:1843 'No saved titles yet. Add a title from its details page.' and :1845 '1 saved title' / '${total} saved titles' match ListsActivity.kt:165-169; web app.js:1861 `empty.textContent = 'This list is empty.'` matches ListsActivity.kt:217. The audit's index.html:383-384 'Titles saved on this device.' is gone (grep returns nothing).
